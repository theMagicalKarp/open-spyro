# Persistent triage verdicts — merged over the auto-heuristics by `open-spyro triage`.
#
# One entry per function, append-only from match sessions. Format (constrained
# YAML subset — keep to exactly this shape, the parser is deliberately small):
#
#   FunctionName:
#     verdict: wall            # wall | parked | attempted | viable
#     class: "B9 clamp"        # cookbook wall / tie class
#     date: 2026-07-23
#     note: "free text"
#
# wall      = never attempt (documented toolchain wall); removed from candidates.
# parked    = a .c.wip exists or the residue is a named tie class; harvest mode.
# attempted = tried and abandoned without a full wall diagnosis; score penalty.
# viable    = hand-vouched good candidate; score boost.
#
# Auto-detected blockers (%gp_rel, $at literal loads, BIOS trampolines,
# switchdata jr-tables in main, handwritten tags) need no entry here.

# --- AddPrim mask family (mask constants in swapped arg regs; ccpsx-built) ---
func_8005ECDC:
  verdict: wall
  class: "B4 AddPrim mask"
  date: 2026-07-10
func_8005ED18:
  verdict: wall
  class: "B4 AddPrim mask"
  date: 2026-07-10
func_8005ED54:
  verdict: wall
  class: "B4 AddPrim mask"
  date: 2026-07-10

# --- Split-label functions (need a text_layout.json merge first) ---
SpuGetVoiceVolume:
  verdict: wall
  class: "B5 split-label"
  date: 2026-07-23
memchr:
  verdict: wall
  class: "B5 split-label"
  date: 2026-07-23

# --- get_cs/get_ce clamp family (w-1 recompute / copy coalescing) ---
BuildDrawAreaTopLeft:
  verdict: wall
  class: "B9 clamp"
  date: 2026-07-08
BuildDrawAreaBottomRight:
  verdict: wall
  class: "B9 clamp"
  date: 2026-07-08
func_800606C8:
  verdict: wall
  class: "B9 clamp (inline)"
  date: 2026-07-19
SetDrawEnv:
  verdict: wall
  class: "B9 clamp (inline)"
  date: 2026-07-19
PutDispEnv:
  verdict: wall
  class: "B9 clamp (inline)"
  date: 2026-07-19
func_80060FD8:
  verdict: wall
  class: "B9 clamp (inline)"
  date: 2026-07-23
func_80061470:
  verdict: wall
  class: "B9 clamp (full-screen variant); DMA body transcribes"
  date: 2026-07-23
func_80061234:
  verdict: wall
  class: "B9 clamp (full-screen variant, twin of func_80061470)"
  date: 2026-07-23

# --- Retry/status-wait loops (compare constants hoisted callee-saved) ---
CdInit:
  verdict: wall
  class: "B10 compare-const hoist"
  date: 2026-07-08
CdControl:
  verdict: wall
  class: "B10 compare-const hoist"
  date: 2026-07-12
CdControlForce:
  verdict: wall
  class: "B10 compare-const hoist"
  date: 2026-07-12
CdControlBlocking:
  verdict: wall
  class: "B10 compare-const hoist"
  date: 2026-07-12
func_80064A20:
  verdict: wall
  class: "B10 compare-const hoist (CdSync sibling)"
  date: 2026-07-23
CdCommandSync:
  verdict: wall
  class: "B10 compare-const hoist (wait loops)"
  date: 2026-07-23

# --- Lagged-mult pipeline leaves ---
func_8005D628:
  verdict: wall
  class: "B11 lagged-mult"
  date: 2026-07-06
ComputeSpuPitchFromNoteDelta:
  verdict: wall
  class: "B11 lagged-mult"
  date: 2026-07-10
func_8005D7F4:
  verdict: wall
  class: "B11 lagged-mult"
  date: 2026-07-10
IntegrateSpyroBodyEuler:
  verdict: wall
  class: "B11 lagged-mult + B-i fold"
  date: 2026-07-16

# --- -g3 batch-read/scatter scheduling (main only) ---
LoadCameraTargetAnglesFromMode:
  verdict: wall
  class: "B12 batch-scatter"
  date: 2026-07-08
RestartSpyroAnimWithState:
  verdict: wall
  class: "B12 batch-scatter (byte-store variant)"
  date: 2026-07-08

# --- Held-base fold rule 1 / shared-base-no-call ---
AdvanceSpyroSpeedTowardTarget:
  verdict: wall
  class: "B7 shared-base no-call"
  date: 2026-07-17
CameraSettle_Update:
  verdict: wall
  class: "B7 shared-base halfword block, no call"
  date: 2026-07-08
ProbeSpyroLedgeForward:
  verdict: wall
  class: "B-i rule 1 fold (+4)"
  date: 2026-07-14
NudgeSpyroFromWallProbes:
  verdict: wall
  class: "B13 lh+sra shift-consumer"
  date: 2026-07-15

# --- Block-layout / constant-hoist ties (source-invariant) ---
func_80058AE8:
  verdict: wall
  class: "B17 free-slot block layout"
  date: 2026-07-07

# --- Deferred multi-idiom from-scratch targets (not walls; retry when veins dry) ---
func_800557CC:
  verdict: attempted
  class: "B13 lh+sra expected; multi-idiom, no ghidra seed"
  date: 2026-07-12
func_8005E680:
  verdict: matched
  class: "libgpu DMA IRQ dispatcher; debug-trace + volatile-HW-reg vein (§G)"
  date: 2026-07-24
  note: "0x8005e680, 388b. MATCH -> C. A74 while(1)+top-break keeps ch<7 at loop top; split loop-local dicr ptr homes callback arg in a0; explicit base local (A20) for s5+per-iter copy; A93 explicit const locals (bit=1/keep=0xFFFFFF) force F2 preheader emit order."

# --- actor waypoint-follow step (2026-07-24-1 session) ---
func_8001228C:
  verdict: wall
  class: "main's unreachable epilogue fragment"
  date: 2026-07-08
func_80017294:
  verdict: wall
  class: "GTE-neighbor epilogue fragment (jr ra; srl v0,t0,12)"
  date: 2026-07-08
func_80017A9C:
  verdict: wall
  class: "GTE-neighbor epilogue fragment"
  date: 2026-07-08
func_800229E8:
  verdict: wall
  class: "handwritten register-restore blob"
  date: 2026-07-08
setjmp:
  verdict: wall
  class: "PSY-Q runtime primitive, not compilable C"
  date: 2026-07-23
__do_global_dtors:
  verdict: wall
  class: "SN CRT prebuilt object ($t0 scratch, no arg-save area)"
  date: 2026-07-17
__main:
  verdict: wall
  class: "SN CRT prebuilt object"
  date: 2026-07-17
MEMCHR_OBJ_48:
  verdict: wall
  class: "B5 split-label (memchr family)"
  date: 2026-07-23

# --- Parked tie classes with no .c.wip on disk (residue documented in cookbook §F) ---
UpdateCameraEulerAngles:
  verdict: matched
  class: "matched 2026-08-10-3 (A146 union allocno: one scratch local carries roll then yaw so both share a0; A161 for the 0x800-A-B reassociation)"
  date: 2026-08-10
func_80038494:
  verdict: parked
  class: "F2 sched tie: andi bit-index vs sll bitmap-offset (2/34, length-exact; wip carries the pointer-subtraction and value-temp levers)"
  date: 2026-08-10
func_8003A9EC:
  verdict: matched
  class: "matched 2026-08-10-2 (literal 0xA00000 page base, one exit, -n split out of the address sum)"
  date: 2026-08-10
func_8005E378:
  verdict: matched
  class: "matched 2026-08-10-2 (HW-reg pointer locals: the volatile store kills cse's copy of the pointer global)"
  date: 2026-08-10
func_8005FC6C:
  verdict: parked
  class: "F4 return-copy hoist (7/38, length-exact). Refined 2026-08-10-3: write the mask on the LEFT (0xFFFFFF & (int)&sym) to get the original's li-before-la order. Residue: the `move v0,ot` copy must sit BETWEEN the mask li and the symbol la (A164 straggler source order), but copy propagation deletes every C form of it; duplicating the tail in both debug-if arms does NOT cross-jump back (+7 insns)"
  date: 2026-08-10
func_8005F98C:
  verdict: matched
  class: "matched 2026-08-10-3 (ClearImage clone + A106: the 0x80000000 needs its own (g<<8)|C temp, declared after the ValidateGpuRect call)"
  date: 2026-08-10
func_8005F428:
  verdict: matched
  class: "matched 2026-08-10-2 (sized alias g_abGpuReverseFlagBlock holds the flag base)"
  date: 2026-08-10
WriteSpuVoiceAddrRegister:
  verdict: matched
  class: "matched 2026-08-10-3 (A64 asm-order goto transcription of the -2/-1/store ladder; A166 split the round-up RMW)"
  date: 2026-08-10
RotateSpyroBodyTowardKnockbackDir:
  verdict: parked
  class: "F4 return-reg coalescing (1 insn, 98.0%)"
  date: 2026-07-10
func_80037F90:
  verdict: parked
  class: "F5 tail block order + delay-slot li"
  date: 2026-07-08
func_8003AAEC:
  verdict: parked
  class: "F6 constant-sb li placement"
  date: 2026-07-08
SetupFrameOT:
  verdict: parked
  class: "B18 constant-hoist layout tie"
  date: 2026-07-08
func_8003891C:
  verdict: parked
  class: "F5 ret0 block layout"
  date: 2026-07-09
# --- Clone siblings blocked on the level_5 template ---
func_level_11_8007B4B0:
  verdict: parked
  class: "F9 double knot; crack func_level_5_8007B4B0 first, then sed-clone"
  date: 2026-07-19
func_level_17_8007B4B0:
  verdict: parked
  class: "F9 double knot; via level_5 template"
  date: 2026-07-19
func_level_23_8007B4B0:
  verdict: parked
  class: "F9 double knot; via level_5 template"
  date: 2026-07-19
func_level_29_8007B4B0:
  verdict: parked
  class: "F9 double knot; via level_5 template"
  date: 2026-07-19

# --- libgpu bring-up cluster leftovers ---
ResetCallback:
  verdict: attempted
  class: "setjmp/held-base-heavy (libgpu bring-up cluster)"
  date: 2026-07-11
FUN_80062648:
  verdict: wall
  class: "B3 kernel trampoline (raw jalr $t2, $ra saved to a global)"
  date: 2026-07-10
longjmp:
  verdict: wall
  class: "PSY-Q runtime primitive, not compilable C"
  date: 2026-07-23
ActivateScriptedCameraView:
  verdict: wall
  class: "B12 batch-scatter tail"
  date: 2026-07-08
func_8005E804:
  verdict: parked
  class: "F1 arg copy-up: local-alloc must give a0 to the DICR value temp, not the shift amount (31/42, length-exact; wip carries the length + OR-grouping levers)"
  date: 2026-08-10

# --- save-block deserializer (2026-07-23-3 session) ---
func_80059594:
  verdict: parked
  class: "F9 double knot — a0/a1 d/g swap in the world-totals loop (F7 qty_compare tie); the add/store split that equalizes ranges cascades into the start=buf region"
  date: 2026-07-23
  note: "91.7% length-exact; .wip parked. Held-base SpuCommonAttr -0x320 solved via D_80076228_blk alias (A20). Permuter target."

# --- text-sprite builder (2026-07-23-4 session) ---
BuildTextSprites:
  verdict: parked
  class: "F1/F7 param_2<->flag live-length s0/s2 swap + F8 andi placement + F8 FillWord a2 delay-slot"
  date: 2026-07-23
  note: "81.5% LENGTH-EXACT (168/168). .c.wip parked. Sibling of BuildTextSpriteChain. param_1->s1 fixed via shared pen store; s0/s2 pair is a pure live_length tie (equal 6 refs). int-ch classification reload REQUIRED (A115). Permuter candidate (§E main recipe)."
AddPrimToOT:
  verdict: wall
  class: "B6 handwritten ($at-as-data: lw $at then sh/sb through it)"
  date: 2026-07-23
  note: "0x800168dc, 56b. Loads g_pOtActiveDepthSlot deref into $at and stores through it; gcc reserves $at for the assembler. Not compilable C."

# --- DMA IRQ dispatcher (2026-07-24 session) ---
func_8003A420:
  verdict: parked
  class: "F9 double knot (F4 ground v0-coalesce + F1 counter-split) + F-sched negu"
  date: 2026-07-24
  note: "0x8003a420, 768b. 94.8% LENGTH-EXACT (10/192), logic verified, .c.wip parked. Clean jal-chain (SubtractVector/VectorLength/ArcTan2/ApplyEulerRotation/RotateVectorByMatrix/AddVector). Inlining (int*)(actor+0xC) per-use (not a held pos local) fixed the arg0/1 s2/s3 + arg2 s7 homing. Residue: ground-probe result wants v1 (dies in-block) vs return counter wants s4 (survives); one shared `ret` -> both s1, split -> counter=s4 OK but call-return coalesces to v0 which cross-jumps the two vec[2] stores (-3 insns). Plus negu v0,v1 vs v0,v0 abs tie. Permuter target (double reg-perm, needs PERM_* macros)."

# --- scripted-camera vantage updater (2026-07-24-2 session) ---
UpdateScriptedCameraView:
  verdict: matched
  class: "camera jal-chain vein (§G straight jal chain)"
  date: 2026-07-24
  note: "0x800377a8, 632b. MATCH -> C. Three levers: (1) g_apScriptedCameraViewBlock alias at 0x80076ebc (A20/A25) so the record pointer's address is held once and g_anCameraPos comes out as addiu -0xC4 off it; (2) A3 volatile reload of g_nSpyroState for the g_abSpyroStateCameraMode[] index (the != chain CSEs otherwise); (3) A7 volatile on BOTH sides of the three body-euler field copies to stop gcc batching the 3 loads ahead of the stores. Random index written as two statements (mask, then *3>>12) to land the mask in the result reg."
ProbeSpyroGroundContact:
  verdict: wall
  class: "B-i rule 1 held-base fold (fall-through-block stores)"
  date: 2026-07-24
  note: "0x8003e318, 784b. 191/196 insns transcribe exactly via the g_anSpyroAirborneFramesBlock alias (base 0x80078af4: [0] frames, [2] normal, [5] slope, [0xa] cliff hint, [0x2d] run flag, [0x76] chunk id, [0x77] verts, MTX -0x1a, POS -0x27). Three fall-through-block stores (BLK[0x76] x2, BLK[0x2d]) fold to lui $at + %lo absolute = +1 insn each; 5 over the slot so it will not link. A22 sized alias, volatile store casts and a pointer-local base were all tried; the pointer local recovers only the post-label 0xB4 site (B-i rule 3). .c.wip parked."
RelocateActorMeshHeader:
  verdict: skip
  class: "re-confirmed 2026-07-28-1 from the 2026-07-24 screen (suspected non-gcc TU); do not re-open from the triage ranking"
  date: 2026-07-28
func_8003C358:
  verdict: matched
  date: 2026-07-24
  note: >-
    Letter-banner builder (908 b). Vein: jal-chain transcription. Levers: split
    each reused angle into its own short-lived local (a/b/ang/t) so global-alloc
    hands out s0/s1 in the original order; in-place `x = x + K; x = x * 16;`
    instead of one fused expression; `ch` reused as the spawn id (gives the
    original out-of-line else + `j`); explicit `if (len > 0) { p = name; do {} }`
    puts the string-cursor copy in the loop preheader; hoisted `skin` temp kills
    a trailing load-delay nop; 8 bytes of unused tail local to reach frame 0x80.
func_80038638:
  verdict: park
  class: F1
  date: 2026-07-24
  note: >-
    Actor "steer around the target" step (740 b). Parked at 71.4%,
    185/185 length-exact, +528 partial bytes. Single F1 register rotation:
    `deadzone` (5th param) must be the top-priority global allocno (s0) with
    the slt temp reusing its dying reg; ours ranks it last so every other
    callee-saved param shifts one. Permuter target — see the .wip header for
    the levers already tried.
ResetSpyroEntity:
  verdict: skip
  class: B8
  date: 2026-07-24
  note: switchdataD_800112E8 jump table in MAIN (build_main.sh rejects loadable rodata).
TickSpyroAnimStateMachine:
  verdict: skip
  class: B8
  date: 2026-07-25
  note: "switchdata jr-table in main (0x8003d194); build_main.sh rejects loadable rodata."
DispatchSpyroChargeStateChanges:
  verdict: skip
  class: B1
  date: 2026-07-25
  note: "%gp_rel access (0x80041270); already named in the cookbook G landscape."
UpdateGemPickupPathCamera:
  verdict: park
  class: F4
  date: 2026-07-25
  note: >-
    Gem-pickup spline path camera (776 b). Parked at 96.9%, 194/194
    length-exact, 6 insns / 2 sites. Both are the F4 shape (the original keeps
    a reg-reg copy ours copy-propagates away): the abs of the dot/span
    quotient (`mflo v0; bgez v0; move s0,v0` -- testing the QUOTIENT is what
    stops the following `if (step < 0)` being jump-threaded) and the
    max-step clamp tail (`move s0,s4; slt v0,v1,s0`, copy filling the lw
    load-delay). cse.c canon_reg substitutes qty_first_reg for every use, so
    no source ordering reaches it. Levers banked in the .wip: volatile
    `PATH` macro for the entry double-load, offset-first pointer
    parenthesisation, and non-volatile PATHDATA in the tangent arms so the
    load lands after the a0/sll setup.
func_8003BFC0:
  verdict: park
  class: F10/F7
  date: 2026-07-25
  note: >-
    Actor waypoint sub-step + cross-fade (920 b). Parked at 80.4%, 230/230
    length-exact. (1) F10 sched1 sinking: the cursor store and the entry
    `idx = path[1]` lbu have no in-block consumer, so our -g3 sched1 moves
    them to the end of the block (reorg then eats the store for the delay
    slot); the original build did not schedule them. A volatile lbu restores
    the entry order at the cost of one andi. (2) F7 divide-block register
    rotation (span a2 vs v1). Reusable levers proven here: offset-first
    `p + (i*0x10 + 8)`, `n = t - 1; span = legs - n` (else gcc reassociates to
    (legs+1) - *step), and reading dir[0] before span so it fills the *step
    load-delay.
UpdateLedgeGrabCamera:
  verdict: skip
  class: B1
  date: 2026-07-25
  note: "%gp_rel access at 0x80037a20+ (camera block); screened out before any C."
UpdateCameraFrame:
  verdict: skip
  class: B1
  date: 2026-07-25
  note: "%gp_rel access (0x80037bd4); the camera-frame driver is in the B1 cluster."
func_8003ABC0:
  verdict: defer
  class: "non-reducible flowgraph + indirect dispatch"
  date: 2026-07-25
  note: >-
    Actor drop/spawn placement (1308 b). Screens clean of every B wall, but
    m2c falls back to gotos-only: the ground-search block is a two-entry
    rotated loop (a0 += step accumulator entered both at the top and mid-body)
    and the level-overlay init goes through the g_pfnLevelOverlayInitHook
    function pointer with a per-arm constant. Real target, but budget it as a
    1300 b structural puzzle, not a jal-chain — do it with the whole context
    window free, not as a session's third function.
InitHudCounters:
  verdict: park
  class: F2
  date: 2026-07-26
  note: >-
    HUD init (904 b). Parked at 53.1%, 226/226 length-exact. sched1 (main,
    -g3) hoists the four counter loads (dragons/world eggs/level eggs/sparx
    food) to the top of the ~75-insn straight-line block; the original keeps
    them between the display-state byte block and the roll-phase zeros.
    Proven source-order invariant (moving the loads across the whole block
    gives byte-identical output; volatile makes it worse and costs an insn).
    Levers banked in the .wip: A85 goto-loop for the 12-icon loop (a real loop
    hoists the restPos la + the 0x20 constant into two extra callee-saved regs
    = slot overflow), user-local `white = 0xFF`, and the g_anHudIconAnchorBlock
    halfword alias at records+0x196.
DispatchSpyroTriggerEvent:
  verdict: skip
  class: B8
  date: 2026-07-26
  note: "0x80056f64, 1052 b: switchdataD_80011380 jr-table in main (build_main.sh rejects loadable rodata); also dispatches through jalr $a2."
TickCdMusicStream:
  verdict: skip
  class: B-i
  date: 2026-07-26
  note: "0x8002bbe0, 1024 b: the volume-fade tail holds g_nMusicVolumeFadeTarget in a1 and accesses -0x2C0 (D_80075F18 lh/sh) and +0x58 (g_nMusicVolumeFadeStep lw/sw) off it in fall-through blocks — B-i rule 1 held-base fold, no derived address passed to a call (so the A25 escape does not apply). The CD state-machine head above it transcribes fine; revisit only if the fold is ever cracked."
EnqueueLoadingScreenSprites:
  verdict: park
  class: F2
  date: 2026-07-26
  note: >-
    Loading-screen HUD sprite queue (920 b). Parked at 82.6%, 230/230
    length-exact. Residues: (1) the gem-digit loop's roll-timer address folds
    to `lw 0xC(s2)` where the original hoists `addiu a1,s2,0xC` into the
    preheader (A32 does not reach it and costs an insn); (2) F2 preheader
    rotation of the divide-magic constant vs the anchor/quad base moves in
    BOTH quad loops, in opposite directions, source-order invariant; (3) the
    egg-loop UV address reassociates the +0x108 onto the modulo index. Levers
    banked in the .wip: seven per-flag-byte aliases, per-block pointer locals,
    in-place base advance, A86 goto-loops for the 3-iteration record loops,
    signed cursor compare, 16-byte dead local for the 0x58 frame.
TickActiveSoundVoices:
  verdict: park
  class: "94.1% LENGTH-EXACT (was 88.1%) — B16 retired via A142 (inline volatile cast-deref of the block alias, no pointer local). Residues: 9-insn prologue `la` rotation (B14/F10, source-order invariant), the g_abSpuCommonAttr-vs-D_80075F30 base split on the out-of-range halving store pair (byte-offset form overflows the slot via a loop.c giv), and the F2 ArcTan2 owner/camera load order (A120 temps make it worse)"
  date: 2026-07-29
AdvanceSpyroSubstepState:
  verdict: wall
  class: "B8 switchdata jr-table (main)"
  date: 2026-07-26
  note: "0x8004888c, 784 b. The per-substep state dispatch is a dense
    switch(g_nSpyroState) through switchdataD_80011230 — loadable .rodata in
    the main EXE, which build_main.sh rejects. Screened while looking for a
    fresh 250-900 b main candidate: it and func_80066F34 (memcard family) are
    the only untriaged ones left in that band, so main at mid size really is
    exhausted."
TickSpyroHornStrikeAttack:
  verdict: park
  class: F1
  date: 2026-07-26
  note: >-
    Horn-strike (charge attack) frame driver (1084 b). Parked at 760/1084
    partial bytes, 272 vs 271 insns (+1, so it overflows its slot). Logic and
    block layout transcribe cleanly from the jal chain; the residue is an F1
    whole-function s0/s1 rotation (the original holds the strike-state block
    pointer in s1 and gives s0 to the short-lived pseudos — the `1` constant,
    the tip-offset base, &vel, the composed-matrix base) plus one extra
    `lui/addiu` for the second tip-offset base. Banked in the .wip: five
    allow_duplicated alias blocks (state +0/+0xC/+0x30/-0x198, timer RMW +
    ZeroVector at +4, anchor mode, collision-active with the slot ages at -0x78
    into FillWord per A25, composed matrix with world pos at -0x1C8), a distinct
    tip-offset alias at +0xC (A24), and the two-complete-calls form of the
    variant select that cross-jumps (A80) instead of if-converting to `sltu`.
GamestateUpdate:
  verdict: matched
  class: ""
  date: 2026-07-26
  note: >-
    0x8003385C, 1012 b, matched in 2 iterations. If-ladder on g_nGamestate with
    the state-0 gameplay frame as the trailing `else` arm; the only lever was
    A66b (write the 4/5 arm as two duplicate else-ifs so they cross-jump —
    `== 4 || == 5` folds to a range test and diverges).
GamestateDraw:
  verdict: matched
  class: ""
  date: 2026-07-26
  note: >-
    0x8001ED5C, 1020 b, matched in 2 iterations. Same ladder shape as
    GamestateUpdate (duplicate else-if arms for the 1/9, 2/3/6 and 4/5 groups).
    The frame-env flip derives the second env as `char *env = (char *)
    &g_abFrameDrawEnv0; if (g_pActiveFrameDrawEnv == env) env += 0x84;` and the
    +0x70/+0x74/+0x78 cursor republish needs explicit batched load locals in
    load order (0x74, 0x78, 0x70) — per-store reloads give the B12 shape.
Gamestate0C_Draw:
  verdict: park
  class: "B-i rule 1 (held-base nonzero-offset store fold)"
  date: 2026-07-26
  note: >-
    0x8001E24C, 1132 b. Parked at 281/283 insns (mismatch 2, +1 length). The
    whole title-card build, divide-magic name lookup, sprite alpha-pulse loop,
    fade clamp and frame-submit tail match; the residue is the two-store OT bin
    head/tail init, where the original holds one `la g_pOtDepthBinHead0` and
    stores at +4 and +0 while our cse folds the +4 store to `lui $at`. New
    levers banked from this attempt and folded into the cookbook: A18 (volatile
    CLAMP STORE, plain reads, duplicates the second compare into the delay
    slot), A20b's volatile-cast addendum, and A66b (duplicate arms / per-arm
    call sites instead of folded guards and shared tails).
Initialize:
  verdict: park
  class: "F7 qty_compare allocation ties (x2) + F10/F8 loop-top increment schedule"
  date: 2026-07-27
  note: >-
    0x800127C0, 1328 b. Parked at 332/332 insns (LENGTH-EXACT), 37 differ,
    88.9% / 1156 partial bytes. Boot bring-up + four 8-frame AddBiasToColorRuns
    fades around the four sync CD loads. Everything structural matches: the
    jal chain, all four fade bodies, both 210-vblank hold loops, the
    TickWorldBundleLoadStream pump, the ClearImage/LoadImage tail and the
    isrgb24-derived PutDispEnv arg (`p = &g_abFrameDispEnv0.isrgb24; *p = 0;
    ... PutDispEnv((DISPENV *)(p + 0x73))`). Three residues remain:
    (1) F7 tie in the CD-read block — the original holds the literal 600 in s0
    and `&g_nCdBaseLba` in s1, ours swaps them; measured priorities (cc1 -dl)
    are 600 = 2*5/47 = 0.213 vs lba = 2*5/38 = 0.263, so lba legitimately wins
    s0 and no source form reorders it (direct-array access, a named `marker`
    local and decl-order changes all const-propagate or leave it unchanged);
    ~10 insns. (2) F10/F8: in the two fade-OUT loops (`-i << 5`) our counter
    increment is the first insn of the loop-top thread, so reorg copies it into
    the back-branch delay slot where the original keeps a nop; the original
    schedules the a0/a1 arg moves first. Embedded `-(j += 1) << 5` / `-(++j)`
    and moving the statement do not change it; ~8 insns. (3) The fade-1
    counter/bias inits sit in the 2nd/3rd jal delay slots in the original;
    hoisting them to the function top costs +1 insn (the s0 prologue save has
    to precede the init) unless the base register tie in (1) also falls.
    Banked levers (all in the .wip): the per-loop local rule, the mixed
    loop-form rule (fade 1 do/while, fades 2-4 goto-loop), block-scoped
    single-use volatile temps for the CdReadSyncSectors arg order, and the
    three new symbol_addrs alias blocks.
DespawnActorRecord:
  verdict: skip
  class: B6 handwritten
  date: 2026-07-27
  note: >-
    0x80052568, 320 b. Screened with the rest of the 0x8004E000-0x80053500
    actor/render band: 27 splat `/* handwritten instruction */` markers (raw
    `addi`). The whole band is hand-asm — same screen rejected
    AccumulateAnimRootMotionDelta (0x80052F38, 29 markers),
    EncodeCachedVecToActorDirCode (0x800533D0, 34), func_80051FEC (41),
    func_80052D64 (40), func_80050068 (10) and TickGemCutsceneActorAnims
    (0x800522C0, 20). Confirms the §G landscape claim that main 250-900 b is
    empty: every remaining function in that size band is either a named §B wall
    or hand-written.
UnpackWorldDataChunks:
  verdict: park
  class: "F1 global-allocation cascade (cursor a2 vs s0 preference knot)"
  date: 2026-07-27
  note: >-
    0x80012d58, 1240 b. Parked at 302/310 insns with all section/loop/tail
    shapes matching; see the .wip header for the five solved sub-classes
    (param-as-cursor + block-scoped sects beat the cse held-copy fold;
    two-statement reloc temp; A22 array externs for pointer-read counts; A12
    volatile header reloads; dead-init bank promote for the tail). Remaining:
    cursor gets s0 via the bank-copy preference where orig has cursor=a2 and
    every sect colored s0 — a global.c find_reg cascade, not source-reachable
    by any form tried (read order swap trades the tail copy for the a2
    assignment). Revisit with a fresh window using cc1 -dg preference/conflict
    dumps against both orderings.
AnimateGemTallyParticles:
  verdict: matched
  class: "matched 2026-07-28 (A106 inner-term naming on the spawn phase)"
  date: 2026-07-28
Gamestate08_Draw:
  verdict: matched
  class: "matched 2026-07-28 (A36 shared OT block symbol, A86 goto-loops, A3 volatile first read, A127 frame slack)"
  date: 2026-07-28
Gamestate0C_Update:
  verdict: skip
  class: "B-i rule 1 held-base fold — 4 held-base setups, 22 nonzero-offset accesses off the D_800777EC state base (-0x4/+0x10/+0x34/+0x3C/+0x4C/+0x50/+0x54/+0x64) in fall-through blocks"
  date: 2026-07-28
RespawnOrGameOver_Update:
  verdict: park
  class: "F14f/F10 arg-block order, 370/373 length-exact (99.2%) — was 369/373"
  date: 2026-08-07
  note: >-
    1492 B. Routing the volatile size read through the arm's loop counter `i`
    (already 3 sets) makes it non-birthing, so it no longer sinks below the
    second held base's `la` and the 0x10(sp) stack-arg store. It now lands one
    slot too EARLY: `lba` is single-set and therefore boosted to the back, where
    the original loads it first. Every second-set carrier for lba costs more
    elsewhere (`count` breaks the mesh-count block by 4; a function-scope
    carrier written by the mode-1 arm does not clear the boost at all — that
    load is a rematerialised `lui/lw` into a0, not a block pseudo; pointer
    carriers and the intro-index/(id/10)*10 routings overflow the slot). See the
    .wip header. 2026-08-10: LUID cannot break it — `rank_for_schedule` only
    reaches `INSN_LUID` on a priority TIE, so swapping the two source statements
    leaves 3/373; making both defs birthing again ties them but costs 4 (`lba`
    first) or 6 (size first); routing `lba` through the loop counter too
    overflows the slot (the counter is live later, so the copy survives).
Gamestate0D_Update:
  verdict: matched
  class: "matched 2026-07-28-1 (A19-free ladder: 3 held-base aliases at offset 0 + distinct Y/Z scalar aliases for the nonzero-offset stores, A81 per-arm Z store)"
  date: 2026-07-28
PauseMenu_Update:
  verdict: skip
  class: "B8 .rodata switch jump table in MAIN (jr via switchdataD_80010dd0, 6-way substate ladder)"
  date: 2026-07-29
SaveLoadMenu_Draw:
  verdict: skip
  class: "B8 .rodata switch jump table in MAIN (jr via switchdataD_80010d58, 8-way)"
  date: 2026-07-29
Gamestate0B_Update:
  verdict: skip
  class: "B8 .rodata switch jump table in MAIN (jr via switchdataD_80010e08, 8-way)"
  date: 2026-07-29
DrawLevelTransitionOverlay:
  verdict: matched
  class: "matched 2026-07-29 (single scalar reused for the digit-count loop AND the icon index: the merged live range flips the F7 qty_compare cascade so i wins s0 and i*16 takes s1)"
  date: 2026-07-29
DrawMotionTrailRibbons:
  verdict: matched
  class: "matched 2026-07-29-1 (A141: one variable for both the IntegerSqrt ARGUMENT and its result gives that pseudo an a0 copy-preference, which prune_preferences then strips from the competing z-load's pass-0 candidates -> len keeps a0)"
  date: 2026-07-29
func_8002B390:
  verdict: wall
  class: "B6 handwritten (addi markers + $at as a data register; world-anim keyframe flag walker)"
  date: 2026-07-29
func_8002B3F4:
  verdict: wall
  class: "B6 handwritten (addi markers + $at as data)"
  date: 2026-07-29
func_8002B444:
  verdict: wall
  class: "B6 handwritten (addi markers + $at as data)"
  date: 2026-07-29
ResetWorldChunkAnimations:
  verdict: wall
  class: "B6 handwritten (73 addi markers, 15 $at-as-data accesses over 328 insns)"
  date: 2026-07-29
TickWorldChunkAnimations:
  verdict: wall
  class: "B6 handwritten (220 addi markers, 63 $at-as-data over 805 insns)"
  date: 2026-07-29
BuildActorDrawList:
  verdict: wall
  class: "B6 handwritten (159 addi markers, 15 $at-as-data over 400 insns)"
  date: 2026-07-29
BuildSecondaryActorDrawList:
  verdict: wall
  class: "B6 handwritten (154 addi markers, 12 $at-as-data over 398 insns)"
  date: 2026-07-29
PollPadAndDistributeInput:
  verdict: park
  class: >-
    B-i rule 1 wall, +4 insns (419 vs 415) so it does not even LINK (28 bytes
    over its slot). Everything else transcribes exactly: the rumble/pad-id
    ladder, the 3-case switch tree on g_abPadRawReport[1] (0x41/0x53+0x73/
    default), the Saturn button remap, the stick synthesis, the idle flags and
    the whole substep-ring fan-out all align (17 of 415 in the masked shape
    diff, 8 of them this fold). The wall is the g_nFrameTicks!=0 arm's
    `lw/sw -8(a2)` + `-4(a2)` RMW pair off a2 = &g_dwPadHeld: our cse folds both
    to absolute. Proven irreducible from the gcc 2.7.2 sources (cse.c fold_rtx
    `case '2'` returns simplify_binary_operation(const_arg0, const_arg1) with no
    cost check) and from cc1 -dr/-ds dumps. Swapping the arms so the |= block
    sits after a CODE_LABEL does NOT help (cse_end_of_basic_block follows
    conditional jumps under -fcse-follow-jumps); neither does A25 (a derived
    address off the same alias IS passed to MapPadAxisToCalibratedRange) nor the
    two-pointer form (update_equiv_regs substitutes the weak pseudo's REG_EQUIV
    and deletes its def). Park kept at src/c/PollPadAndDistributeInput.c.wip
    with the full analysis; reopen only if B-i rule 1 is retired.
  date: 2026-07-30
PlaySoundEffect:
  verdict: matched
  class: "matched 2026-07-29-2 (A142: volatility from the DECLARED types — volatile row nVolL/nVolR/anPanCache fields + volatile override globals — pins the 5 override reloads and their program-order slots while keeping every store fused as lui $at/%lo; plus A145, a third empty `case 0x10` to make the mode ladder a split switch tree)"
  date: 2026-07-29
func_800658FC:
  verdict: retry
  class: "B16 half of its F9 double knot is now crackable — re-try the post-jal `lui;addiu;lw 0(r)` sites with an inline volatile cast-deref of the alias (A142/B16 retirement 2026-07-29-2); the held-base derive is the other half"
  date: 2026-07-29
TickWorldBundleLoadStream:
  verdict: park
  class: "F7 la-vs-load allocation swaps (x4) + F2 arm-0 constant order + the state-6 cse-canon store register"
  date: 2026-07-30
  note: >-
    0x80014564, 1548 b. Parked LENGTH-EXACT (386/386 insns), 64/387 words
    differ = 83.5%, +1260 partial bytes. The whole 10-stage CD ladder, both
    arm-9 loops, the mesh-relocate loop and every StartCdReadAsync arg block
    transcribe exactly. Levers that landed: A142 volatile cast-deref for the
    in-flight gate; four alias blocks (chunk-desc scratch, g_pDrawBufA,
    g_pPathTableBuffer A21 store+reload, actor-mesh list with [-16] reaching
    g_nWorldDataExtChunkOffset per A25); A4 volatile arg temps in the
    original's load order for arms 1/3/4/5/7/8 (61 -> 30 shape mismatches, the
    highest-yield lever here); A85 goto-loops for both arm-9 loops (loop.c
    otherwise SRs the path walker and the i*0x58 actor offset); A23 absolute
    re-read of g_pPathTableBuffer after the rebase loop; A114 store-i+1-then-
    increment; A127 RECT + dead int pad[4] for the 0x40 frame. Residues are
    regalloc/schedule ties — see the .c.wip header for the full list and the
    exhausted source forms for the state-6 arm (its `g_nCdStreamState = 6;`
    self-store cannot reach the original's `beq -> shared tail` because cse
    canon_reg rewrites the stored constant to the STATE register; the arm is
    elided in the parked form, which is behaviourally identical).
DispatchSpyroPhysicsByState:
  verdict: skip
  class: "B8 switchdata jr-table (main) — switchdataD_80011178 dispatch on g_nSpyroState"
  date: 2026-07-30
TickGemPickupAssetStream:
  verdict: skip
  class: "B8 switchdata jr-table (main) — switchdataD_80010a70 stream-state dispatch"
  date: 2026-07-30
CdProcessInterrupt:
  verdict: skip
  class: "B8 switchdata jr-table (main) — switchdataD_80011e70 on the CD interrupt code"
  date: 2026-07-30
ApplySpyroStateTransition:
  verdict: skip
  class: "B8 switchdata jr-table (main) — switchdataD_80010e28"
  date: 2026-07-30
ApplySpuVoiceAttrRange:
  verdict: skip
  class: "B-i rule 1 held-base fold — 22 nonzero-offset accesses off the voice-attr base, no held-base setup"
  date: 2026-07-30
TickSpyroGameplayFrame:
  verdict: matched
  class: "vein: 0x8004xxxx Spyro frame-tick drivers; 5 offset-0 held bases, no gp_rel/switchdata"
  date: 2026-07-30
func_80038FC8_retry_2026_07_30:
  verdict: attempted
  class: "F7 anim-arm old-vs-const16 v0/v1 mirror: two new levers tried and both FAILED — (a) one function-scope `old` reused for the arm compare (4 refs, floor_log2 jump) drops it to 91.4%; (b) a long-lived `mark = 0x10` local to lower the constant's priority costs +1 insn (slot overflow). Still parked at 94.1%; permuter-only."
  date: 2026-07-30
TickSpyroAttackEffects:
  verdict: parked
  class: "F7 spill-slot hole + F1 preheader hoist order + F1 arc-path v0/v1 + F10 sfx arg block"
  date: 2026-07-30
  note: "0x80048d10, 2248 b. 562/562 insns, 28 words differ (95.0%, +2,056 partial bytes), every block positionally aligned; the whole flame-breath half (129 insns) is byte-identical. Residues, in size order: (1) the original's spill block is [0x58 empty][0x60 sfxDone][0x68 UNUSED][0x70 burst giv] — no source form makes a 4th spilled pseudo (a constant-equivalent pointer local is deleted by update_equiv_regs; expand-time slots always precede reload's, so a dead local can only pad at 0x58), `int pad[2]` buys the 0xA0 frame at the cost of 5 insns on the two counters; (2) 9-insn preheader hoist order — the original's age/state/pt come after loop.c's hoisted invariants (they are givs off slotAge[i]/slotState[i]) but the giv form costs more elsewhere; (3) 8-insn arc-path table v0/v1 rotation; (4) 5-insn PlaySoundEffect arg order. Levers that got it to 95%: `i = 0` in the merge block (loop.c needs the biv initial value or every giv init is computed generically), A142 volatile on the three pt[-0xf] variant reads (else cse folds them absolute and pt loses s7 to the burst giv by 0.068 vs 0.067 priority — measured with the D-2c probe), &pt[i*3] for the trail cursor (its giv init is then `move s5,s7`), per-arm SPAWN calls for the variant flag (a ternary if-converts to sltu), and `n = 0` before the `if (steps > 0)` guard so reorg gets the blez delay slot."
func_level_33_80082F24:
  verdict: matched
  class: "per-level actor-spawn dispatcher family (see cookbook §G)"
  date: 2026-07-31
  note: >-
    0x80082F24, 1996 b. First member of the family matched. 18-node sparse
    switch tree over the moby type id. Levers: A151 single exit, A66b
    duplicated call in both arms of the parent-or-Spyro position copy, A70
    constant-select per arm, A153 array-declared g_anSpyroVelocity, shared
    constant materialised at its first use.
spawn_dispatcher_family:
  verdict: vein
  class: "per-level actor-spawn dispatcher (one per level overlay, 2.0-3.7 KB each, ~35 total)"
  date: 2026-07-31
  note: >-
    13 matched 2026-07-31 (levels 33/21/10/9/2/20/0/1/4/18/24/28/30, +30,756
    exact bytes). Arms are shared verbatim between levels; only the switch case
    list differs. Rebuild the decoder/matcher/generator per cookbook §G and the
    remaining members are mostly mechanical. Remaining work: level_32
    (0x1CA..0x1CB arm), level_31 (0xCA/0xCF arms + a variant mortar), level_11
    (six new arms), level_34 + level_26 (case decoder needs their root shape),
    and the 17 larger members at 2.9-3.7 KB (levels 3/5/6/7/8/12/13/14/15/16/
    17/19/22/23/25/27/29) which were never screened.
func_level_32_80083108:
  verdict: matched
  class: "per-level actor-spawn dispatcher family (see cookbook §G)"
  date: 2026-07-31
  note: >-
    0x80083108, 2628 b. First generator-built member: 11 of 12 arms came from
    the library verbatim, only 0x1CA..0x1CB was new (parent-type-selected
    random velocity through func_80037EA0 = RandomInRange, cross-jumped third
    call).
func_level_8_80088B88:
  verdict: matched
  class: "per-level actor-spawn dispatcher family"
  date: 2026-07-31
  note: >-
    0x80088B88, 2956 b. New arm 0x120..0x121 (thrown shard, ShardState block:
    byte 0xC/0xD + short 0xE + int 0x10). The one residue was a signed/unsigned
    compare: `(func_8006272C() & 0xFFF) >= 0x800` gives sltiu, the original has
    slti — cast the roll to `(int)`.
func_level_7_8008A4D0:
  verdict: matched
  class: "per-level actor-spawn dispatcher family"
  date: 2026-07-31
  note: >-
    0x8008A4D0, 3312 b. Pure clone — zero new arms, generated from the library
    (0x120 from level_8, 0x1CA from level_32) and matched first build.
func_level_3_800872A4:
  verdict: matched
  class: "per-level actor-spawn dispatcher family"
  date: 2026-07-31
  note: >-
    0x800872A4, 2940 b. New arm 0x1F6 (0x120-family variant: unconditional
    +0x20 lift on vel[2], a 0xFF re-roll gate and a second vel[2] clamp).
func_level_13_8008A36C:
  verdict: matched
  class: ""
  date: 2026-07-31-3
  note: >-
    3048 b. Unparks the 99.3% F2 tie with A154 (both operand globals volatile);
    no other change to the parked source.
func_level_12_8008B2C0:
  verdict: matched
  class: ""
  date: 2026-07-31-2
  note: >-
    UNPARKED and MATCHED. The `mode` local was an artifact: the original 0x26
    arm has no such variable. Its high branch (parent->unk3F >= 6) ends with
    its own func_800526A8(rec) + break — the asm jumps one insn past the shared
    block so jump.c folds the `move a0,rec` into the delay slot — and the other
    two branches store their constant directly (`*st = 3;` / `*st = 2;`).
    Cross-jumping then merges the shared `sw v0,0(s2); j` tail and leaves the
    `li v0,K` in the delay slot, which is why the constant lands in v0.
func_level_15_80086DD8:
  verdict: matched
  class: ""
  date: 2026-07-31-3
  note: >-
    3432 b. Same one-line A154 fix as level_13; the park note's exhausted lever
    list was correct that volatile on D_80078A5C ALONE gives 5 words — the point
    is that the other operand's global must be volatile too.
func_level_34_80083690:
  verdict: matched
  class: ""
  date: 2026-07-31-2
  note: >-
    2668 b. Three arms decoded: 0x11 aimed launch (direction byte read once into
    a local so the first cosine index reuses the stored value while the second
    reloads rec->unk46; delta normalised via func_800171FC/func_800175B8), and
    the two debris variants (0x1E1..0x1E3 adds the parent velocity
    unconditionally and has no 0x135 colour tail; the 0x43.. family keeps both).
func_level_31_80085664:
  verdict: matched
  class: ""
  date: 2026-07-31-2
  note: >-
    2784 b, first-build match. New arms 0xCA/0xCF (homing shot: SeekState with
    the owner pointer at +0x04, the shared -1 constant, the short type tag at
    rec+0x38 — needs `short unk38` in the Actor struct — and a ground-probe tail
    that jump.c cross-joins between the two arms). The 0xFB mortar arm is the
    library body with different stack offsets: take fuzzy library hits down to
    ratio ~0.90, the linked diff is the gate.
func_level_11_800819BC:
  verdict: wall
  class: "F7 call-arg copy hoisted above the stores (loses a0 to the third temp)"
  date: 2026-07-31-2
  note: >-
    2372 b, 98.3% length-exact (583/593, the same 5-word tie twice). Four arms
    decoded (0x159..0x15B slot-guarded spawn-or-release via D_80078618[D_8007595C]
    with `rec = 0` on the release path, 0x176 and 0x177..0x182 parent-attitude
    copies, 0x183..0x185/0x1EB fixed-position arm cross-jumped into its
    neighbour's tail). The attitude copy needs BOTH `signed char` fields at
    0x44..0x46 AND three int temps (a field-to-field copy is a QImode move and
    emits `lbu; nop; sb` one at a time). Residue: sched1 (which runs before
    register allocation) hoists the `move a0,rec` argument copy above the three
    stores, so the third temp conflicts with a0, takes a1, and the delay slot
    gets the store instead of the copy. Tried: temp declaration/load/store
    order, interleaving, register/unsigned/short/signed-char temps, an int[3]
    temp array, moving the unk4B store into the block. These two arms are shared
    verbatim by level_17/23/29/5 — one tie gates ~15 KB, the best permuter
    target in the family.
    B19 is PROVED UNREACHABLE 2026-08-06-2 (read off `cc1 -dS`, block 85 of
    func_level_25_80084B94): the arg copy `move a0,rec` has INSN_PRIORITY 1
    (its operand is live-in to the block) while every `sb` of the batch has 2
    (fed by an in-block `lb`, latency 2). The original's order needs that one
    static priority to be < 2 at T-3 (so the `sw st->owner` wins) and >= 2 at
    T-4 (so the copy beats the stores) — unsatisfiable. Probed:
    `func_800526A8(&rec->posX)` folds the copy to an `addiu` off the same
    live-in reg and the priority stays 1; all five positions of
    `st->owner = parent;` measured (5 -> 6/8/10 insns, all worse). Do NOT aim
    the permuter at this — the residue is not a tie. Reclassified park -> wall.
func_level_16_80083AB4:
  verdict: parked
  class: "B13 signed-short load feeding >> (lh;sra unreachable)"
  date: 2026-07-31-2
  note: >-
    3452 b, decoded but 2 insns over (865 vs 863) so it does not link. The 0x25
    whirlwind arm reads the unit-circle tables with `lh; sra 2`; our cc1 emits
    `lhu; sll 16; sra 18` for shift consumers (+1 per lookup). Tried an int temp,
    an explicit cast and a one-short struct wrapper. B13 was recorded on
    main/-g3 — this proves it holds in overlays too, and that it is specific to
    `>>` (a short field feeding a LEFT shift does come out as `lh`). The rest is
    decoded: the 0x25 3-case switch on parent->unk48 and the 0x27 turret arm
    (ArcTan2 toward D_80076DF8/DFC, owner pointer at state+0x04).
func_level_14_8008883C:
  verdict: matched
  class: ""
  date: 2026-07-31-3
  note: >-
    3096 b. One new arm (0x130 homing shot: parent's state block needs its own
    local so its unk00 loads early, ZeroVector on state+0x08, ground probe at
    +0x400) plus the A127 frame-hole move (the 8-byte dead local that sizes the
    frame belongs to the LAST block that declares an array, not the mortar arm).
    Closed by A154: declaring BOTH ArcTan2 operand globals volatile.
func_level_26_80084620:
  verdict: matched
  class: ""
  date: 2026-07-31-3
  note: >-
    2688 b, first-build match once the decoder learned the `beq $s0,$v0` root
    shape. Its one new arm is 0x37 (aimed shot: horizontal distance + Z delta
    through ArcTan2 clamped to +-0x10, then a yaw from Spyro's position, state
    +0x00 = 0x8C).
func_level_6_8008A258:
  verdict: matched
  class: "F14d"
  date: 2026-08-06
  note: >-
    756-insn level_6 spawn dispatcher, 3,024 B. The 7-word park was one
    set-count question: splitting the lobbed-debris arm's reused `drop` local
    into drop + nudge makes the call-result copy single-set, so sched1 boosts
    it into the func_8003A720 delay slot.
func_level_22_800874FC:
  verdict: matched
  class: ""
  date: 2026-07-31-4
  note: >-
    3196 b, MATCHED. The 0x5D arm is the standard debris burst plus an aimed
    variant taken when parent->type == 0x23: heading from the parent state
    block's +0xC with a +-0x32 jitter, speed = abs(cos(pitch) >> 4), and an
    unconditional Spyro-velocity add (the plain debris arm gates that on
    parent->flags & 0x20000). Spin rates are read back as (unsigned char) of the
    short state fields.
func_level_27_8008590C:
  verdict: matched
  class: ""
  date: 2026-07-31-4
  note: >-
    3656 b, MATCHED. One new arm (0xD7): random speed 0x78..0x96 along a heading
    of parent->unk45 +-0xF through the unit-circle tables, three func_80037F10
    spin rolls, an int store at +0x4C plus the cross-jumped rec->unk4B |= 0x80
    tail, and st->unk10 = parent->posZ - (short)parent->unk38 - 0x40 (needs a
    short at Actor+0x38, so split the int unk38[3] filler).
func_level_19_8008465C:
  verdict: matched
  class: ""
  date: 2026-07-31-4
  note: >-
    3408 b, MATCHED once level_22 supplied the 186-insn 0x5D arm. The two small
    arms are 0x20 (func_80052D64 attach point, facing from D_80076E1E and
    (D_80076E20 + 0x400) >> 4) and 0x157 (owner pointer + two zeroed state
    words). Both use a TrackState block {Actor *owner; int; int}.
func_level_25_80084B94:
  verdict: wall
  class: B19
  date: 2026-07-31-4
  note: >-
    3176 b, PARKED at 99.4% length-exact (5/794). All four arms decoded: 0x38 and
    0x9B are aim-at-Spyro with the +-0x10 turn clamp (0x9B additionally holds the
    parent's facing when the delta exceeds 0x10 — needs abs() for the bgez/negu
    pair and a two-statement mask, since (unsigned char)(rec->unk46 - pdir) in
    one expression makes gcc drop the field's own 0xFF mask), 0x7C is the
    attitude copy. The residue is only 0x7C's batched signed-byte copy: B19.
    B19 is PROVED UNREACHABLE 2026-08-06-2 (read off `cc1 -dS`, block 85 of
    func_level_25_80084B94): the arg copy `move a0,rec` has INSN_PRIORITY 1
    (its operand is live-in to the block) while every `sb` of the batch has 2
    (fed by an in-block `lb`, latency 2). The original's order needs that one
    static priority to be < 2 at T-3 (so the `sw st->owner` wins) and >= 2 at
    T-4 (so the copy beats the stores) — unsatisfiable. Probed:
    `func_800526A8(&rec->posX)` folds the copy to an `addiu` off the same
    live-in reg and the priority stays 1; all five positions of
    `st->owner = parent;` measured (5 -> 6/8/10 insns, all worse). Do NOT aim
    the permuter at this — the residue is not a tie. Reclassified park -> wall.
func_level_17_80081F0C:
  verdict: wall
  class: B19
  date: 2026-07-31-4
  note: >-
    2964 b, PARKED at 98.7% length-exact (10/741). Case list and every arm
    decoded (incl. a new 18-insn fixed-position hidden-actor arm). The residue is
    the two attitude-copy arms (0x176, 0x177..0x182) only: B19. Solving B19 lands
    this, level_25, level_23, level_5 and the level_11 park.
    B19 is PROVED UNREACHABLE 2026-08-06-2 (read off `cc1 -dS`, block 85 of
    func_level_25_80084B94): the arg copy `move a0,rec` has INSN_PRIORITY 1
    (its operand is live-in to the block) while every `sb` of the batch has 2
    (fed by an in-block `lb`, latency 2). The original's order needs that one
    static priority to be < 2 at T-3 (so the `sw st->owner` wins) and >= 2 at
    T-4 (so the copy beats the stores) — unsatisfiable. Probed:
    `func_800526A8(&rec->posX)` folds the copy to an `addiu` off the same
    live-in reg and the priority stays 1; all five positions of
    `st->owner = parent;` measured (5 -> 6/8/10 insns, all worse). Do NOT aim
    the permuter at this — the residue is not a tie. Reclassified park -> wall.
func_level_23_8008223C:
  verdict: wall
  class: B19
  date: 2026-08-01
  note: >-
    3352 b, PARKED at 98.8% length-exact (10/838). Zero unknown arms once
    level_29 contributed the 0x167..0x169 scatter burst. Residue is the two
    attitude-copy arms (0x80082D2C/0x80082DA4) only: B19.
    B19 is PROVED UNREACHABLE 2026-08-06-2 (read off `cc1 -dS`, block 85 of
    func_level_25_80084B94): the arg copy `move a0,rec` has INSN_PRIORITY 1
    (its operand is live-in to the block) while every `sb` of the batch has 2
    (fed by an in-block `lb`, latency 2). The original's order needs that one
    static priority to be < 2 at T-3 (so the `sw st->owner` wins) and >= 2 at
    T-4 (so the copy beats the stores) — unsatisfiable. Probed:
    `func_800526A8(&rec->posX)` folds the copy to an `addiu` off the same
    live-in reg and the priority stays 1; all five positions of
    `st->owner = parent;` measured (5 -> 6/8/10 insns, all worse). Do NOT aim
    the permuter at this — the residue is not a tie. Reclassified park -> wall.
func_level_5_8008249C:
  verdict: wall
  class: B19
  date: 2026-08-01
  note: >-
    3732 b, PARKED at 98.9% length-exact (10/933). Zero unknown arms once
    level_29 contributed the 0x167..0x169 and 0x16A arms. Residue is the two
    attitude-copy arms (0x80083108/0x80083180) only: B19.
    B19 is PROVED UNREACHABLE 2026-08-06-2 (read off `cc1 -dS`, block 85 of
    func_level_25_80084B94): the arg copy `move a0,rec` has INSN_PRIORITY 1
    (its operand is live-in to the block) while every `sb` of the batch has 2
    (fed by an in-block `lb`, latency 2). The original's order needs that one
    static priority to be < 2 at T-3 (so the `sw st->owner` wins) and >= 2 at
    T-4 (so the copy beats the stores) — unsatisfiable. Probed:
    `func_800526A8(&rec->posX)` folds the copy to an `addiu` off the same
    live-in reg and the priority stays 1; all five positions of
    `st->owner = parent;` measured (5 -> 6/8/10 insns, all worse). Do NOT aim
    the permuter at this — the residue is not a tie. Reclassified park -> wall.
func_level_29_80082028:
  verdict: matched
  class: ""
  date: 2026-08-01
  note: >-
    3436 b, MATCHED on the 2nd iteration. Four new arms decoded into the library:
    0x4E (bare init straight into the shared grid insert), 0xE8 (spawn 0x400 up,
    ArcTan2 at Spyro through func_80038178; the `sw $s1` store is the type-0x78
    compare constant cse-reused, write the literal 0x78), 0x167..0x169 (random
    8-bit heading out of D_8006CC78/D_8006CBF8 shifted down 7 -- declare the
    tables `short[]` and use `>>`, B13's lhu;sll;sra IS the original form), and
    0x16A (four-quadrant launch). The one iteration cost: the inner 4-way switch
    needs an explicit (int) cast on the func_8006272C() roll -- an unsigned index
    makes case 0 type-bounded, so gcc skips the balanced tree's range test and
    emits a linear ladder (4 insns short, `slti` missing).
func_level_11_80082F58:
  verdict: matched
  class: vein
  date: 2026-08-02
  note: >-
    MATCHED (was parked F2). The preheader movable-order residue was fixed by
    writing `arg3 >> 24` INLINE at its three case-0xC use sites instead of in a
    named local: loop.c emits hoisted movables in scan order, and scan_loop only
    accepts a movable whose set is unconditional (case 1) OR whose dest is not a
    user variable (case 2). A named local at the top of the loop body is case 1
    and therefore always lands ahead of the 0x80 / 0xFF colour constants the
    arms hoist; a compiler temp inside the 0xC arm is case 2 and is scanned
    after them. Sed-cloned to func_level_17_800836F8 and func_level_23_80083BAC
    (9,672 exact bytes).
func_level_5_80084128:
  verdict: matched
  class: vein
  date: 2026-08-02
  note: >-
    3496 b / 874 insns, the levels 5 / 29 particle emit dispatcher. Same as the
    levels 11/17/23 copy plus the 0xD ember arm and the 0x13 distance-faded
    haze arm (func_80017990 gate at 0x7FFF, word velocity at +0x18, grey ramp
    (0x8000 - d) >> 6 above 0x6000 -- reuse the distance local for the colour to
    keep it in s0, and make the ramp the if-arm). Cloned to
    func_level_29_80083B8C.
func_level_3_80088F68:
  verdict: wall
  class: "F14b birthing/allocation mutual exclusion (0x18 arm)"
  date: 2026-08-04-2
  note: >-
    Re-parked, still 981/985 (99.6%) length-exact. The 4-insn 0x18-arm residue
    is now ROOT-CAUSED and should not be re-attempted: local-alloc only takes a
    pseudo when reg_n_deaths == 1 (local-alloc.c reg_qty = -2 gate) and only the
    local path reaches t0 here -- global_alloc cannot, because the whole
    function has exactly three allocnos with a hard-reg preference and all three
    are v0/v1 (measured with cc1 -dg), so a0..a3 are never in
    regs_someone_prefers[], and a hard conflict with a0..a3 would need a 4-arg
    call (this family's widest is 3, so a3 is never live). Meanwhile sched.c
    birthing_insn_p boosts a def iff reg_n_sets == 1, so the correct POSITION
    needs >= 2 sets. In one basic block 2 sets always means 2 deaths, so the two
    requirements are unsatisfiable together. Measured both ends: plain literals
    give `li t0,46` (register CORRECT) at the wrong slot, 7 diffs; a twice-set
    local gives the right slot in a0, 4 diffs. Also disproved this session -- a
    separate single-set local (swept over six statement positions: the boosted
    def stays a fixed 7 slots ahead of its store, and it tips loop.c into a
    third hoisted invariant, +8 frame) and a carrier shared with arm 0x15
    (same +8 frame, 917 diffs). See cookbook F14b.
func_level_1_800892C4:
  verdict: wall
  class: "F14b birthing/allocation mutual exclusion (0x18 arm)"
  date: 2026-08-04-2
  note: >-
    1081/1085 (99.6%) length-exact, same 0x18-arm residue as
    func_level_3_80088F68 and the same root cause (cookbook F14b). Heads the
    five-overlay 1085-insn group (levels 1/2/4/10/28 = 21,700 b). Do not
    re-decode and do not aim a permuter at the 0x18 arm.
func_level_34_80085230:
  verdict: matched
  date: 2026-08-04-1
  note: >-
    4148 b / 1037 insns, the Gnasty's-loot particle emit dispatcher; the second
    of only two overlays in this family with no 0x18 arm. Landed from the park
    with two arm-shape fixes: the 0x1A arm must write `arg3 * 8` as three
    separate expressions (a named local pins the `sll` above the seed store and
    the tail then no longer cross-jumps into the 0x1C colour block, +4 insns),
    and the 0x15 ribbon arm wants source order r, g, b, life, fx, unk10 --
    storing g first leaves the `lw` of the hoisted (arg3 & 0xF) with an
    unfilled load-delay slot.
func_level_33_80084634:
  verdict: matched
  date: 2026-08-04
  note: >-
    3712 b / 928 insns. Generated from the matched level_5 donor + three new
    arms (0x10 / 0x11 spark bursts taking their velocity from `pos + 3` and
    their colour from three ints at arg3, 0x19 = the 0x9 heat spark thresholded
    on pos[6] at 461/401/321) and the 0x50 streak burst rewritten against
    `pos >> 2`. One of only two overlays in this family with no 0x18 arm.
func_level_30_80084EA0:
  verdict: parked
  class: "mid-decode; ends on the F14b 0x18-arm wall"
  date: 2026-08-04-2
  note: >-
    3648 b / 912 insns, the smallest member of the particle emit dispatcher
    family (the level_3 arm set minus 0x15 and 0x4C). Parked at 910/912 insns
    from the level_3 donor with those two arms deleted; 116 divergent. Three
    things remain before it is length-exact -- one stack vector too many (+8
    frame, which also drags an extra loop-hoisted `addiu t0,sp,32` into the
    preheader), its OWN 0xC arm (a three-way sub-case over D_8006E4E0 /
    D_8006E570 / D_8006E498 at 0x8008527C, not level_3's), and the per-level
    fx-before-seed store order in arms 0x0/0x1/0x9. After all that it still ends
    on the 4-insn 0x18 knot (verified in this park), so it is groundwork, not
    bytes. See the .c.wip header.
emit_dispatcher_family_0x18_screen:
  verdict: note
  class: "vein screen"
  date: 2026-08-04-2
  note: >-
    Screened all 27 particle-emit-dispatcher jump tables: EVERY one of the 21
    remaining unmatched overlays contains arm 0x18. The only members without it
    are levels 5/29, 33 and 34, all already matched. So the entire ~90 KB
    remainder of this vein is gated on the F14b wall and caps at 99.6%
    length-exact. Do not open a new one expecting exact bytes.
overlay_clone_scan:
  verdict: note
  class: "clone mode exhausted"
  date: 2026-08-04-2
  note: >-
    Normalised every overlay function body (immediates and labels masked) and
    grouped by fingerprint: ZERO groups contain both a `matched` member and an
    `asm` member. Every free sed-clone has already been taken, so clone mode has
    no targets until a new overlay decode lands. Re-run the scan after each new
    overlay match.
func_level_11_80082300:
  verdict: matched
  class: "vein — emit-list advance pass (flight trio)"
  date: 2026-08-04-3
  note: >-
    MATCHED 2026-08-04-3 and sed-cloned to func_level_17_80082AA0 and
    func_level_23_80082F54 (+9,480 exact bytes). 3160 b / 790 insns each.
    The decode turned on four things: (1) A159 -- only `rec` is a source
    pointer; pos/vel and the rec+1 field base are all loop.c givs, and the
    `rec[1] != 0xFF` terminator test is what makes loop.c choose rec+1 as the
    shared address base (a `p = rec + 1` local costs a fifth induction
    variable). (2) A156 -- every `x - 1 + (rand & 2)` jitter site needs the
    three-way split AND its own name pair; the `- 1` must stay int-wide or it
    narrows to `+ 0xFFFF` and gets hoisted into an s-reg. (3) A157 -- runs of
    `lbu; addiu -K; sb` chains come out with the pair at positions 1&2 swapped,
    so write the source with that pair swapped back (keeping each constant with
    its field). (4) A160 -- `abs()` is a builtin; hand-rolling it costs a move.
func_level_17_80082AA0:
  verdict: matched
  class: "clone of func_level_11_80082300"
  date: 2026-08-04-3
  note: >-
    sed-clone of func_level_11_80082300 (norm-identical, branch displacements
    only). Matched first build.
func_level_23_80082F54:
  verdict: matched
  class: "clone of func_level_11_80082300"
  date: 2026-08-04-3
  note: >-
    sed-clone of func_level_11_80082300 (norm-identical, branch displacements
    only). Matched first build.


# --- emit-list advance pass, 894-insn flight variant (2026-08-05) ---
func_level_5_80083330:
  verdict: matched
  class: "vein — emit-list advance pass (894-insn flight variant)"
  date: 2026-08-05
  note: >-
    MATCHED 2026-08-05 and sed-cloned to func_level_29_80082D94 (+7,152 exact
    bytes). 3576 b / 894 insns each. The level_11 790-insn decode plus classes
    0xD and 0x13. Two idioms closed it: write step*4 INLINE (not as a `fade`
    local) so loop.c hoists 1 / step*2 / step*4 in first-use order, and give
    each value site in the 0x13 arm its own local (A156).
func_level_29_80082D94:
  verdict: matched
  class: "clone of func_level_5_80083330"
  date: 2026-08-05
  note: >-
    sed-clone of func_level_5_80083330 (norm-identical, branch displacements
    only). Matched first build.

# --- emit-list advance pass, 1163-insn ground variant (2026-08-05) ---
func_level_2_80083274:
  verdict: parked
  class: "F16 sched1 two-chain lead tie (arm 0x15 only)"
  date: 2026-08-05
  note: >-
    PARKED at 99.0%, LENGTH-EXACT 1163/1163 — 12 insns differ, all inside ONE
    12-insn window in arm 0x15 (was 95.9% / 47 insns). The old "F15 chain-order"
    diagnosis was wrong: the *6-vs-sine order IS reachable — the sine product
    needs its own local so the mult issues early while the mflo stays in the
    cross-jumped tail (A160), and both swirl arms must be re-shaped together.
    Arm 0x4C and arm 0x18 are now byte-exact (0x18 needed A161 operand order:
    HU(0x16) + 0x20 + HU(0x18)). Remaining residue = F16: the original
    interleaves `rec[0xB] += 4` with the `HU(0x8) += D_800756CC * 6` chain
    (byte value lives in a0 across the block); ours emits the byte chain
    complete-then-long. Invariant over 12 source forms incl. all 7 statement
    placements, load/add/store splits, a twice-set carrier and a step local;
    NOT an aliasing effect (same base, memrefs_conflict_p disambiguates; volatile
    can only force a full order). Next lever: permuter aimed at that one arm.
    Unparks 4 norm-identical siblings with it (23,260 B).
func_level_1_80088098:
  verdict: parked
  class: "clone of func_level_2_80083274"
  date: 2026-08-05
  note: "norm-identical sibling; lands with the level_2 park's F tie."
func_level_4_800826F0:
  verdict: parked
  class: "clone of func_level_2_80083274"
  date: 2026-08-05
  note: "norm-identical sibling; lands with the level_2 park's F tie."
func_level_10_80084EF0:
  verdict: parked
  class: "clone of func_level_2_80083274"
  date: 2026-08-05
  note: "norm-identical sibling; lands with the level_2 park's F tie."
func_level_28_80084028:
  verdict: parked
  class: "clone of func_level_2_80083274"
  date: 2026-08-05
  note: "norm-identical sibling; lands with the level_2 park's F tie."

# --- session 2026-08-05-2 ---------------------------------------------------
# F16 root-caused off the sched1 dump and PROVED unreachable: no store can be
# scheduled between a store and the def that feeds it (priority(def) ==
# priority(store), the def is ready the instant the store is scheduled, and the
# LUID tie-break is monotone in source order). See cookbook §F16 and the
# func_level_2_80083274 .wip header. The five 1163-insn ground advance copies
# stay parked at 99.0%; do not reopen without a new mechanism.
#
# The emit-list ADVANCE pass is a 35-function family (790-1815 insns, ~140 KB),
# not the 10 previously recorded. F16 gates every variant carrying arm 0x15 --
# fingerprint base-agnostically: lbu $X,0xA($B) .. addiu $X,$X,0x4 .. sb $X,0xA($B).
func_level_0_80086134:
  verdict: matched
  class: "advance pass, 1195 insns, F16-free"
  date: 2026-08-05-3
  note: >-
    MATCHED (4,780 B). Arms 0xE/0x16 decoded (the whirlwind decay pair) and
    0x1E (the mote that dies on age alone, no shared kill tail); everything
    else transferred verbatim from level_6 / level_2. The decay pair needs a
    `short` local with in-place a--/a++ (cookbook A162).
func_level_12_8008BE98:
  verdict: matched
  class: "F17 (retired)"
  date: 2026-08-06
  note: >-
    1294-insn magic-crafters-home advance pass, 5,176 B. The one-insn park fell
    to A93 ordering: name step * 4 and &v as explicit pre-loop locals in that
    order, which puts both preheader insns where the original has them and
    leaves the arm's last call reading v as a single-use frame address that
    reload rematerialises.
func_level_3_80087E20:
  verdict: skip
  class: "F16 (arm 0x15)"
  date: 2026-08-05
  note: >-
    1106-insn ground advance; norm-identical clone of func_level_20_80085084
    (8,848 B for one match). Arm set == level_2's minus 0x6, so it decodes
    almost for free -- but it carries the arm 0x15 F16 window and would park at
    ~99%. Revisit only if F16 ever falls.
func_level_20_80085084:
  verdict: skip
  class: "F16 (arm 0x15)"
  date: 2026-08-05
  note: "norm-identical clone of func_level_3_80087E20; same F16 gate."
SubMenu_Update:
  verdict: matched
  class: "stale park"
  date: 2026-08-06
  note: "708 B. Parked permuter output links exact as-is; the dead local it left behind was not load-bearing."
ResetCameraStateToTarget:
  verdict: matched
  class: "stale park (F3)"
  date: 2026-08-06
  note: "296 B. Links exact from the parked source; the double base assignment IS load-bearing."
StepSpyroOntoLocalFloor:
  verdict: matched
  class: "stale park (B-i rule 3)"
  date: 2026-08-06
  note: "436 B. Links exact from the parked source — the B-i classification came off a .o-based partial score."
StepSpyroAnimAndCommitTransition:
  verdict: matched
  class: "stale park"
  date: 2026-08-06
  note: "300 B. Links exact from the parked source."
BeginStoryIntro:
  verdict: matched
  class: "stale park (B-i)"
  date: 2026-08-06
  note: "184 B. Links exact from the parked source."
func_80054578:
  verdict: matched
  class: "stale park (B7)"
  date: 2026-08-06
  note: "136 B. Links exact; the do/while(0) state load is load-bearing."

# --- session 2026-08-06-1 (harvest: park sweep + A163/A164/F14e levers) ---
StopAllSoundExceptMask:
  verdict: matched
  class: "A163 guard-block delay-slot fill"
  date: 2026-08-06
  note: >-
    348 B. The sllv that fills the second beqz's delay slot cannot come from
    inside the if-body (sched1 puts the volatile key-off load first and reorg
    will not speculate it); splitting the && into nested ifs and computing the
    row bit between the guards lands it.
func_level_5_8007B68C:
  verdict: park
  class: "B14 RETIRED -> now length-exact (1610/1610), MISMATCH 78 content-aligned"
  date: 2026-08-06
  note: >-
    6440 B, x5 clone payoff 32,200 B. The +1 nop was A164 straggler ordering,
    not la-canonicalization: permuting the three dep-free defs to
    (img, img2, envBase) drops `addiu s3,s0,-8` into PutDrawEnv's delay slot.
    It LINKS now, so drive further work off the linked diff. Next residues:
    `la envBase` still takes the T-17 bubble (~20 insns late), a +8 frame
    (A127), the else-arm `move s5,zero` bne delay slot, and the &pos block
    pointer (original holds it in s2 via a block-scoped `int *pp = pos;` in
    each of four regions at 0x8007BB20/C010/C5DC/CCEC; we rematerialise
    `addiu a1,sp,56` per call site).
    2026-08-06-2: R1 (`la envBase`) is now FULLY probed — `img2 = img - 2`
    cannot precede `img`, so only three source orders exist and all three are
    measured: (envBase,img,img2) +1 insn, (img,envBase,img2) +1 insn (new),
    (img,img2,envBase) = the current length-exact state. A164 is exhausted here.
    Full 13-residue inventory with orig insn indices is in the .wip header;
    work R7 (quad-loop temp registers, ~25 of the 78) first, then R13 (A26
    scalar alias for &pos, untried), then the one extra 8-byte frame slot.
    The permuter body's `allclear = 0x15;` is LOAD-BEARING — replacing it with
    the literal offset is 78 -> 85. The hand-written body + A164 is also 85.
func_80059474:
  verdict: matched
  class: "A170 — inline arg spelling + cursor assigned after the call"
  date: 2026-08-07
  note: >-
    248 B. The last 2 insns fell to A170: spelling the merge source INLINE at
    the CopyWords call and assigning the loop cursor `src` AFTER the call keeps
    the one folded `addiu s0,s0,-44` but at the later insn's position, so
    `move a0,dst` schedules ahead of it. The F14e carrier for the anchor flag
    stays load-bearing.
AlignSpyroBodyToVelocity:
  verdict: matched
  class: "A169 side-effect operand order + A166 statement split"
  date: 2026-08-07
  note: >-
    284 B. Not F2 after all: the embedded `(pitch = g_nSpyroBodyPitch)` operand
    has side effects, so expand evaluates it BEFORE the d[0] array read and
    sched1 hoists the global's lui/lw above the stack reload. Reading d[0] into
    the local first, then the global, then subtracting and masking in separate
    statements (A166, which keeps the result in the loaded register) is exact.
func_8005FBB4:
  verdict: matched
  class: "A173 — parameters used directly fix the prologue homing ORDER"
  date: 2026-08-10
  note: >-
    184 B, was a 4/46 park. The `p = arg0; n = arg1;` local copies home a1
    before a0; deleting them and working on the parameters themselves emits the
    original `sw s0 ; move s0,a0 ; sw s1 ; move s1,a1`. (Declaration order is
    the separate A40 dial for the s0/s1 MAPPING — swapping it costs 13 insns.)
    The named 0xFFFFFF `mask` local inside the loop is load-bearing (it fixes
    the merge-constant materialization order); inlining the literal costs 20.
ComputeVoicePanAndFalloff:
  verdict: matched
  class: "A127 frame padding + parameter-vs-copy operand choice"
  date: 2026-08-06
  note: >-
    320 B. `int pad[4]` restores the 0x20 frame (the 5th arg then reloads from
    0x30(sp)); the wrap arm must negate the PARAMETER and mask the COPY, which
    is what assigns a2/v1 to the original's roles in the two delay slots.
EndSaveMenuToWorld:
  verdict: park
  class: "F3 head knot, 4/81"
  date: 2026-08-10
  note: >-
    324 B. Original emits `addiu s0,s0,-420 ; sw s0,0(s1) ; lui v1 ; lw v1` and
    we emit the global load pair first. Position-invariant, re-confirmed
    2026-08-10 over five more forms: swapping the two statements, dropping the
    `timer` local so the load sits at its own store, moving the load below the
    `dyn` setup, and naming the anchor address in its own single-set local are
    all byte-identical. A volatile load (the A142 lever) and a plain `*cam`
    deref in place of `cam[new_var]` both cost insns (build fails / 6 of 81) —
    the `cam[new_var]` index form is load-bearing. Permuter (aim it at the
    head, not the tail — the shared camera/HUD/music tail is already exact).
func_level_0_8007CFC0:
  verdict: park
  class: "mid-decode, 215/642 content-aligned"
  date: 2026-08-06-2
  note: >-
    2568 B, template for the 642-insn x10 hub balloonist dispatcher (25.7 KB).
    Measured this session with the content-aligned masked diff: 640 insns built
    vs 642 orig, MISMATCH 215 — this is a genuine mid-decode, not a tie, and
    needs a fresh decode session, not permuter time. Do not pick it up expecting
    a close.
func_level_0_8007B020:
  verdict: park
  class: "R1 cross-jump asymmetry (7 insns) + 4 addressing/sched ties; 2009/2024, 87 mismatched"
  date: 2026-08-06-3
  note: >-
    8096 B, and the single largest clone prize on the board — word-identical in
    the SIX hub overlays (level_0/6/12/18/24/30 = 48,576 B) and a near-clone in
    four more (level_1/3/7/8 = 32,384 B). Decode is COMPLETE this session: all
    eight arms of the balloonist cutscene tick transcribed and verified against
    the asm, 95.7% shape-aligned. Screens clean of every B wall (the 316 $at
    uses are ordinary `lui $at; sw %lo` global stores, not B2). Residues are
    mapped in the .c.wip header — R1 (the only structural one) is that the
    original keeps two copies of the `+ lerp` block at B2A8/B470 but shares one
    `- lerp` block, where our jump.c cross-jumps both pairs. Pick this up as a
    finish-the-ties session, not a decode session.
func_level_6_8007B9F8:
  verdict: clone
  class: "clone of func_level_0_8007B020 (strict-identical body)"
  date: 2026-08-06-3
  note: >-
    Same for func_level_12_8007B9F0 / func_level_18_8007B7E4 /
    func_level_24_8007BA18 / func_level_30_8007AF90 — all five are
    instruction-and-operand identical to func_level_0_8007B020 modulo %hi/%lo
    symbol names and branch labels (md5 of the masked body agrees). sed-clone
    them the moment level_0 lands. func_level_1_8007B0D0 /
    func_level_3_8007B0AC / func_level_7_8007B898 / func_level_8_8007B898 are a
    second 2024-insn group (also identical among themselves) that differs from
    the level_0 group in register allocation only.
DampenSpyroFreeFallAttitude:
  verdict: matched
  class: "A167 single-set result locals + statement order in the spring section"
  date: 2026-08-07
  note: >-
    220 B, was a 7-insn F2/K park. Three levers: each angle's final sum goes to
    its own single-set local (`out0`/`out1`), which restores the original's
    `addu a1,t0,a1` operand order instead of accumulating in place; the chain-1
    error shift is split (`<< 2` right after chain 0's spring update, `>> 4`
    after the D_80078CB4 load) so sched1 cannot hoist it to the block head; and
    the D_80078CB4 load is written ahead of the D_80078CB0 store.
func_8003A16C:
  verdict: matched
  class: "F14 disjoint-reuse carrier + carrier-free yaw (A146/A151 family)"
  date: 2026-08-10
  note: >-
    692 B, was a 10/173 park. Two coupled ties. (1) The permuter's `new_var`/
    `new_var2` yaw carriers gave the *yawp pseudo 5 refs (2 sets, dies in 2
    places) => global priority floor_log2(5)*5/12 = 833, above the negated
    clamp's 3/4 = 750, so yaw took a0 first. Writing the yaw as one plain
    local drops it to 4 refs and the clamp is allocated first (a0), yaw
    second (a1) - 7 insns. (2) The FindGroundHeightBelow result needs a
    carrier or it coalesces with v0 and the `move` disappears (-1 insn), but
    the carrier must be dead before the following CopyVector/Encode/func_
    800529E4 calls or it is forced to a callee-saved reg: `ret` and `speed`
    both cross those calls and land s2 (24/173); the stack param `clamp`
    lands s1 (5/173); reusing the turn-delta `d` - already multi-set inside
    the else arm, dead afterwards, and already living in v1 - matches.
    Generalise: pick a disjoint-reuse carrier by (sets >= 2) AND (dead
    before the next call) AND (already in the target register class).
RotateLightVectorXZ:
  verdict: park
  class: "slot overflow — the parked wip builds one insn too long (164 B slot)"
  date: 2026-08-07
  note: >-
    Do not enable this wip in a multi-function build: it overflows its text slot
    (`ld: cannot move location counter backwards ... 80058d68 -> 80058d64`) and
    every other diff in that build then reads stale bytes.
BeginGemCutscene:
  verdict: matched
  class: "A171 (cse canonicity) + A172 (allocno live-range demotion)"
  date: 2026-08-10
  note: >-
    264 B, was a 4/66 park that survived a 90k-iteration permuter sweep. Two
    coupled ties, both settled with the -dl/-dg probe: (1) the guarded
    `g_nLevelStormFlag = 1` store takes whichever of {const 1, world id} cse
    calls canonical, and the world id only wins that when it outlives the const
    — so it must NOT be reused as the call's arg0 (A171); (2) with the reuse
    gone the const (R=4, L=50, pri 1600) outranks the world id (R=3, L=20, pri
    1500) and steals a0, rotating 9 insns. Writing `D_800761DC = 1;` AFTER the
    two 0x3FFF volume stores lengthens the const's range past the flip point;
    sched2 restores the store's position (A172). Reading the world id later
    also flips the priority (L=16) but empties the branch delay slot — the
    `*head = 0;` store has to stay adjacent to the `if`.
func_80038C4C:
  verdict: park
  class: "F1 local-alloc ORDER, 6/66 (unchanged)"
  date: 2026-08-10
  note: >-
    264 B. `ay = abs(ry)` lands in v1 where the original has a1, and the
    `rec[3]` limit then takes v0 where the original has v1. Measured with -dg:
    ay is global allocno 81 (R=3, L=7, pri 4285), conflicts only {73, v0, a0},
    so v1 is simply the first free register in reg_alloc_order — the original
    must have given v1 to the limit load FIRST. Nothing source-side reorders
    that: an early-return tail, a `&&` return, reusing `ry` instead of a
    separate `ay`, and hoisting the limit into its own local score 25/34/18 of
    66. A141's preference lever does not apply (no copy to/from a hard reg in
    range). Permuter-or-never, and note its 78k-iteration sweep found nothing.
func_80038FC8_2026_08_10_1:
  verdict: park
  class: "F14b birthing/allocation mutual exclusion, 9/152 -> 4/152 length-exact"
  date: 2026-08-10
  note: >-
    608 B. The two anim arms need `old` in v0, the 0x10 const in v1, AND the
    `li` boosted next to its stores while the call-arg copy drifts into the
    beq delay slot. Single-set const => birthing boost (position right) but
    block-local, and local-alloc priority floor_log2(3)*3/6 = 5000 beats
    `old`'s floor_log2(2)*2/9 = 2222 so it takes v0 (9/152). A function-scope
    carrier written by both arms => reg_n_sets 2 => global_alloc, which runs
    after local-alloc, so `old` gets v0 and the const v1 (register right) -
    but >=2 sets also kills the boost, the `li` joins the priority-1
    straggler pool, and rank_for_schedule's INSN_LUID-descending sort puts
    the lowest LUID first, so the `li` (LUID 235) beats the arg copy (256) to
    the block front and reorg eats IT (4/152). Local-alloc lifetimes are
    measured POST-sched1, so no source order closes the 2.25x priority gap,
    and the const's set must precede its uses which precede the call, so no
    source form raises its LUID. See the .wip header for the full variant
    sweep.
func_80039E94_2026_08_10_1:
  verdict: park
  class: "F1 parameter live-length tie, was +1 insn/cannot-link -> 10/182 length-exact"
  date: 2026-08-10
  note: >-
    728 B. Solved: the cross-branch `li v1,255` delay-slot hoist (one shared
    `wrap` comparand local across both wrap arms makes it reg_n_sets==2 and
    therefore non-birthing, so it drifts to the front of the branch-TARGET
    arm and reorg lifts it into the delay slot) and the `ret` s4-vs-fp
    rotation (a single exit takes ret from 5 refs to 3, below every parameter
    allocno, so it is allocated last). Residue: `step`(@0x54) and
    `kind`(@0x5c) exchange s6/s7 - both 4 refs, but live lengths 202 vs 182
    give 396 vs 439 and kind is allocated first; kind only reaches 4 refs via
    flow's loop-depth weighting of its in-loop compare. Needs the arms
    reordered to close the 20-insn gap.
func_80039E94:
  verdict: matched
  class: "matched 2026-08-10-2 (do/while(0) on both turn call sites weights step/lim refs by loop_depth, lifting step over kind in global.c)"
  date: 2026-08-10
func_80038FC8:
  verdict: parked
  class: "F14b mutual exclusion (4/152) — the do/while(0) ref dial does NOT reach it: wrapping a statement inside the arm perturbs sched1 (11/152 wrapping the load, 49/152 wrapping the store); the dial only behaves when it wraps a whole block"
  date: 2026-08-10
FUN_80060e28:
  verdict: parked
  class: "F1 carried-accumulator register (43/44, block layout EXACT): the shared subu tail must live in CASE 1 with case 2 jumping backward into it, which only an explicit label+goto produces (jump.c's forward walk always keeps the LATER copy). The goto makes the 0x400 accumulator a GLOBAL allocno and global.c's find_reg picks the lowest local-density hard reg (a1) over the original's v0; the coalesced return copy leaves no v0 preference. Full residue map in src/c/FUN_80060e28.c.wip"
  date: 2026-08-10
RelocateActorMeshHeader:
  verdict: matched
  class: "index-form givs + local-alloc tying + for-increment placement"
  date: 2026-08-10
  note: >-
    620 B leaf, first attempt. Three levers, all new: (1) every table access is
    written as an INDEX off the base (`table[5+i]`, `hdr[14+i]`), never a
    pointer walker - loop.c reduces the DEST_ADDR giv but splits the constant
    term into the addressing mode, so the giv init is `move rd,base` with
    +0x14/+0x38 in the load/store, where `p[5]` + `p++` folds the constant into
    the init (`addiu rd,base,0x14`) and eliminates the biv. (2) the packed-word
    chain reuses ONE local (`lo = word & M; ...; lo = ((lo+delta)>>1) & M;`):
    a pseudo with 2 sets dies twice, so local-alloc's combine_regs bails at
    `reg_qty[ureg] < 0` and the chain stops being tied to its dying operand,
    reproducing the original's alternating v0/v1. (3) `w++` moved into the
    for-increment clause puts `j++` in the exit test's load-delay slot. Frame
    is 0x18 with ZERO locals - do not add a dead pad before checking.
RelocateMobActorTable:
  verdict: parked
  class: "F1 whole-function a0<->a2 rotation (find_reg pass-0 regs_someone_prefers); 108/108 insns, length- and schedule-exact"
  date: 2026-08-10
  note: >-
    432 B. Re-decoded from scratch (the 2026-07-25 permuter body is at f172dda)
    and every structural knot is closed: index-form table accesses, volatile
    MESH_TABLE reads (cse will not invalidate a scalar global at a store to
    `(plus reg const)`), volatile field STORES (sched.c's true_dependence needs
    BOTH refs volatile or sched1 fills 13 load-delay nops with the next
    statement's `lui/lw`), plain field READS (volatile reads cost `lh` ->
    `lhu;sll;sra`), an end-of-body `mesh` re-fetch in the inner loop, and the
    two-statement `count<<2 + 0x24` that blocks A46 reassociation. Only the
    register rotation is left: the loop-1 counter must win a0 from the param.
    See the .wip header for the -dg measurement and the four rejected dials.
