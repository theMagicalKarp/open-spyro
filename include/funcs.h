/* GENERATED by tools/import_ghidra.py from the one-time Ghidra dump.
 * The Ghidra import seeds this file ONCE; thereafter edit it BY HAND (do not
 * regenerate over hand edits). */

#ifndef OPEN_SPYRO_FUNCS_H
#define OPEN_SPYRO_FUNCS_H

#include "types.h"

/* All prototypes below are Ghidra-inferred seeds. Any that still mention an
 * `undefined*` type is flagged uncertain — refine it as the function is matched. */

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined main(undefined4 *param_1, int *param_2, uint param_3, int *param_4);

/* 0x800122a8: Stage-1 graphics bring-up, first call from Initialize. ResetGraph(0), then fills the two 0x84-stride frame-DB structs: DRAWENVs g_abFrameDrawEnv0/1 (clip y=8 / y=0xf8, ofs y forced 0 / 0xf0, dtd=1, isbg=1) via InitDefaultDrawEnv and DISPENVs g_abFrameDispEnv0/1 (disp y=0xf0 / y=0 — each shows the half the OTHER DB draws) via InitDefaultDispEnv; clears full 512x512 VRAM, display on. (Old plate had DRAWENV/DISPENV roles swapped.) */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined InitGraphicsHardware(void);

/* 0x800123c8: Pad subsystem init. PadInitDirect with the two raw report buffers g_abPadRawReport (port 1) / g_abPad2RawReport (port 2), seeds both pads' calibration blocks (ResetPadStickCalibrationDefaults + FinalizePadCalibration on the g_dwPadPressed block at 80077378 and the pad-2 mirror g_dwPad2Buttons at 800776d8), zeros the rumble timers, sets g_nPadSetMainModePending, PadStartCom, one priming PollPadAndDistributeInput call, then registers PollPadAndDistributeInput as the per-VBlank callback via InstallVSyncCallback. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined InitPadSystem(void);

/* 0x80012460: Possible UT_ROFF.OBJ/SsUtReverbOff */
void SpuInit(void);

/* 0x80012480: CD subsystem init. CdInit + CdControl(CdlSetmode, 0x80 = double-speed/data) + CdReadCallback(LAB_80016490). Initializes g_dwCdStreamState (DAT_800774b4) to 0x40 and seeds the 4-byte CD-DA mixer at DAT_800776d0 (Lvol=0x7f, Rvol=0x7f) before pushing it via CdMix. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined InitCdSystem(void);

/* 0x8001250c: Loads the boot-time asset directory from CD. Sets g_nCdAssetFileIndex (DAT_80076b90)=0x25; CdReadAndCopyToRam(0x25, PTR_DAT_800113a0, 0x800, 0, 600) reads one sector into the shared CD buffer; CopyWords copies 0x330 dwords into DAT_8007a6d0 — the asset directory whose fields (d0/d4/e0/e4/e8) Initialize then uses for the subsequent four CD loads (title art / world data / textures / CLUT). */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined LoadAssetDirectoryFromCd(void);

/* 0x8001256c: Builds the 48-entry CD-audio track range table g_anMusicTrackLbaTable (6 banks x 8 tracks). Each entry is a {u32 start_lba, u32 end_lba} pair: start = g_anCdAudioBankBaseLba[bank], end = start + g_anCdAudioTrackLengths[bank*8+track]. Sets g_dwMusicStreamStatus = 0x40. Read by the CDDA/XA streaming state machine to seek tracks via CdIntToPos + CdlSetloc + CdlReadN. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined4 BuildAudioTrackTable(void);

/* 0x80012604: Per-game-session zero-fill. Clears 0x12 dwords at DAT_80076fe8 + 0x24 dwords each at DAT_800772d8/DAT_80077420 + 0x24 bytes each at DAT_80078e78/DAT_8007a6a8 + 6-byte sentinel at DAT_800758d0..d5 (d0=2) + 6x5-byte struct array at DAT_80078618/DAT_80078680. Sets g_nLevelReadyFlag=3, g_nExtraLives=4, plus ~10 misc state flags. Called only from InitNewGameState. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined ResetLevelState(void);

/* 0x8001277c: Fresh-game state initializer. Sets DAT_8007596c=10 (likely Spyro starting hit count) / DAT_800757a4=1 / DAT_80075914=0x52, then calls ResetLevelState. Called from Gamestate0D_Update (StoryIntro) and DispatchSpecialCutscene — runs once at the start of a new game. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined InitNewGameState(void);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined Initialize(void);

/* 0x80012cf0: Sets up the per-level SPU sample-bank header pointers and rebases the per-sample SPU-RAM offset of each entry by adding the SPU RAM start address (0x1010). param_1=header base; DAT_800761d4 = header, DAT_800761d0 = header+0x104 (entry array), entry count at header+0x100, entry size 0x14. When param_2!=0, walks entries[count-1..0] adding 0x1010 to entry[i].first_int (the sample's SPU RAM byte offset). Called by UnpackWorldDataChunks (initial level load — param_2=1) and the level-transition state machine FUN_80015370 (re-init path). */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined RelocateLevelSampleBank(int param_1, int param_2);

/* 0x80012d58: Top-level world-data chunk unpacker — walks the loaded world bundle as a chain of length-prefixed sections (each section starts with a 4-byte byte_size that doubles as the link to the next section) and populates the engine's per-section globals + patches all relative file-offsets to absolute pointers. Sections in order: (1) DAT_800785c0/c4/c8 = section0 entry array (size 4-int per entry); (2) param_2==0 only — DAT_800785b0/b4 = section1 entry list + DAT_8007778c/77788 = sub-array; (3) DAT_800785b8/bc = section2; (4) DAT_800785d4 = section3 base + +3..+6 absolute-resolves; (5) DAT_80077780/84/90/91/92 = section4 header; (6) param_2==0 only — DAT_800758bc = mob-count, then per-mob a 32-byte+variable layout populating &DAT_80078640[i], with a sub-loop building the actor lookup at &DAT_80076278[mob_id*4], finally DAT_80076234 = sample-count and a tail call to RelocateLevelSampleBank(piVar3+1, 1). Returns ptr to the next-section after consumption. param_2=0 = full unpack (initial load); param_2!=0 = re-unpack a subset (level transition). Callers: FUN_80014564 (state machine — initial) + FUN_80015370 (state machine — transition). */
int *UnpackWorldDataChunks(int *param_1, int param_2);

/* 0x80013230: Patches the mob/actor table at DAT_80076378 from a per-mob update list. Pass 1: sanitizes the first 0x2e mob slot pointers at (DAT_80076378[+0x38][i]) — any slot whose stored offset is < the working-pool start (DAT_800785fc) or = 0xffffffff is zeroed. Pass 2: walks (mob_id, byte_size, data...) records until mob_id<0 — for each: stamps mob_table[mob_id*4+0x38] = data ptr, rebases the model body ptr at +0x14 and bound ptr at +0x18 by the global model-data base (DAT_80076378[+0x34]), rebases the per-vertex-anim packed-XY anchor table at +0x10 (lower 21 bits as a relative offset, top 11 bits preserved, result is the >>1 of the absolute byte addr). Callers: FUN_80014b70 (gem cutscene reload) + FUN_80015370 (level transition). */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined RelocateMobActorTable(int *param_1);

/* 0x800133e0: Relocates a freshly-loaded actor-mesh / vertex-anim header to absolute pointers. Two formats keyed by sign bit of *param_1: (A) negative: 3-ptr quick form — rebases +1, +2, +3 by header base, then strips the top bit. (B) non-negative: 8-entry parent header at +5 (each entry gets +0x14, +0x18, +0x20 rebased), then up to *param_1 child entries at +0xe each pointing to a sub-record (also rebased) — sub-records contain either (a) a byte-flag-cleared form: per-vertex-anim packed-XY at +0x12 walks N entries (lower 21 bits rebased by header_base, &0x1fffff masked, top 11 bits preserved) + an inline relative resolver on the 2nd int (rebased by header_base relative to the pointer position) — or (b) the byte-flag-set form: model body at +0x8 rebased + the packed-XY +0x12 walked with the additive >>1 absolute scheme. Returns the header base ptr with top bit cleared. Callers: M09 state machines (FUN_80014564 / FUN_80014b70 / FUN_80015370) + LoadActorMeshTables (M12 runtime load). */
int *RelocateActorMeshHeader(int *param_1);

/* 0x8001364c: Post-CD-stream level finalization driver — the "level-loader meat". Builds per-level runtime state from the freshly streamed bundle in g_pPathTableBuffer. Callers: LoadAndStartLevelFromCd + TickLevelTransitionStream (case 0xd). param_1 = full-setup flag (0 skips chunk-anim reset + overlay-hook/HUD init). Steps: (1) stop sounds, reset frame counters, g_nLevelStormFlag for levels 0x28/0x2a. (2) spawn select: g_nSpawnAnchorValid latch ? keep g_anLevelSpawnPos/Yaw(&gt;&gt;4) : read path-table head; gamestate 5 (game over, levels 0x28..0x3b) uses the continue tables via g_nGameOverLevelIntroIndex; g_nScriptedRespawnFlag keeps Spyro in place (exit nudge from g_anLevelSpawnNudgeZTable + sine radial). (3) ResetSpyroEntity. (4) flight branch (level%10==5): g_nFlightLevelActive=1, ChangeSpyroState(0x20)=fly, zero pitch/turn accums. Level 0x40 also seeds g_nGnastyLootFallRefZCap. (5) camera: fill g_anCameraActiveTargetParams from g_anCameraParamMode2Front ([2]=0x600), retarget g_pCameraTargetParams, ResetCameraStateToTarget. (6) gamestate=0, clear letterbox. (7) rebase 7 world chunk-anim subtables. (8) actor table: g_pActorListBase + pool setup (g_pActorPoolFreeHead/ DynBase, aux pool, capacity=(top-base)/0x70). (9) pickup table rebase. (10) g_pActorSpatialGridBase + relocation list. (11) death restore: g_anDeathReplaySpawnTable[g_nDeathTryIndex*4], g_nDeathRespawnPending=1, g_pDeathReplayCursor (replay mode zero-fills 0x80600000). (12/13) two actor passes applying g_anLevelKillBitmapTable[level*8] vs g_anRunningKillBitmap (despawn/hide). (14) snapshot running -&gt; committed bitmap. (15) zero g_anLevelTriggerStateSlots. (16) reset emit list (base/cursor/terminator/sentinel). (17) ResetWorldParticleTables. (18) full setup + non-flight: g_pLevelOverlayInitRecord = (*g_pfnLevelOverlayInitHook)(0x78,0), InitHudCounters(1). (19) HandleMusicCommand(g_nCurrentMusicTrack,1). */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined BuildLoadedLevelRuntime(int param_1, undefined4 param_2, int param_3, undefined1 *param_4);

/* 0x800144c8: Synchronous level load + start. Used by LevelIntro_Update once the "ENTERING WORLD" intro fade reaches its load point. Steps: (1) StopAllSoundExceptMask(0) — stop all SFX and music; (2) compute level archive offset = DAT_80078600 + (*(int*)&DAT_8007a720[level_intro_idx*0x10]) where level_intro_idx = DAT_80075964 — DAT_8007a720 is a per-level descriptor table (0x10-byte entries, first int = level's byte offset within the archive); (3) CdReadSyncSectors(LBA=DAT_80076b90, dst=DAT_800785e4, count=DAT_80078604, offset=level_off, marker=600) blocks the whole load; (4) FUN_8001364c(1, dst, count, level_off) = the level-loader-meat (unpacks the world data into RAM); (5) RestartSpyroAnimWithState(audio_initialized ? 0x20 : 0, dst, count, level_off); (6) g_nLevelReadyFlag = 3 — signals LevelIntro_Update that the load completed. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined LoadAndStartLevelFromCd(void);

/* 0x80014564: Multi-stage CD-streaming state machine for world bundle (re)load. Ticked from Initialize (boot), PauseMenu_Update, GemCutscene_Update, Gamestate0D_Update, CheckDeathPlane. State variable: _DAT_80075864 (0..10). Each tick: ticks RestartStalledCdRead, gates on (_DAT_80076bb8==0 in-flight clear && CdSync==2 && _DAT_800774b4 bit 0x40 callback-ready). When gate open, executes one stage and advances state. World ID at _DAT_8007566c indexes per-world LBA table &DAT_8007a6e8 (8-byte stride) and per-world chunk-size table &DAT_8006ee5c (4-byte stride). Stages: (0) StartCdReadAsync 0x800-byte header into _g_pDrawBufA at world's base LBA; (1) CopyWords 0x1d0 words of header to scratch &DAT_80076c00, kick read of full chunk into _g_pDrawBufA; (2) LoadImage (DMA→VRAM) of TIMs at framebuf (512,0) height (chunk_size>>10); (3) kick read of (_DAT_80076c04 - 0x80000) bytes at +0x80000 offset; (4) SetSpuTransferStartAddr(0x1010) + WriteSpuRam 0x7eff0 audio bytes + GetSpuTransferStatus busy-wait, then kick read into _g_pDrawBufB; (5) UnpackWorldDataChunks(_g_pDrawBufB, 1) — param 1 = transition subset; snapshot section4 header _DAT_80077780..90 to _DAT_80078a40..50; world==1 branches to state 6 (extra 0x60000 chunk), else state 8 directly; kick next read; (7) read remaining 0x60000 bytes; (8) walk actor-mesh count list at &DAT_80076c50, call RelocateActorMeshHeader on each, stash result ptrs at &DAT_8007637c+; kick read of gem-cutscene path table chunk; (9) walk path table at _DAT_800785e4 — set _DAT_80075680 head, rebase +0x10/+0x14 ptrs by base, call InitActorRenderDefaults on each actor at _DAT_80075828 (0x58-byte stride), stamp +0x50=0x20 default flag + +0x36=actor_id, terminator 0xff at end; (10) done — function becomes no-op until state is reset by caller. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined TickWorldBundleLoadStream(undefined4 param_1, undefined4 param_2, uint param_3);

/* 0x80014b70: Gem-pickup cutscene asset CD-streaming state machine — ticked from GemPickup_Update (Gamestate08). Streams dragon model + voice-clip + cutscene music in, plays the cutscene, then restores the world. State variable _DAT_8007705c (0..5). Each tick: ticks RestartStalledCdRead, gates on (_DAT_80076bb8==0 in-flight clear && CdSync==2 && _DAT_800774b4 bit 0x40 callback-ready). Dragon ID at _DAT_80077054 indexes per-dragon descriptor table &DAT_80076c20 (8-byte stride: LBA_offset + size). Cases: (0) StartCdReadAsync of dragon header sized _DAT_80077068 from per-dragon LBA + per-level offset (&DAT_8007a720)[g_nLevelIntroIdx*0x10] = base LBA; if available RAM < requested, truncate to 0x800-aligned + set _DAT_80077064=1 split-flag for SPU spillover path. (1) Copy 0x24 words of header to scratch &DAT_80077030; non-split path: relocate dragon model + actor-mesh via RelocateActorMeshHeader, save ptrs to _DAT_80076b70/74; split path: SPU-upload first chunk, slide remaining bytes down, kick read of remainder. (2) Wait for substate>=2 then multi-chunk SPU upload loop (0x8000 byte chunks) via _DAT_80077060 sub-counter — ReadSpuRam swap, WriteSpuRam, repeat until _DAT_80077038 bytes uploaded; on completion advance state. (3) Wait for substate>=6 (mid-cutscene), kick read for music-swap chunk. (4) WriteSpuRam to upload music data, then kick read of restore-world chunk sized _DAT_80076c14. (5) Restore world: RelocateMobActorTable, zero any mob slot at &DAT_80076378[i] whose ptr is below working-pool base _DAT_800785fc (2-pass: clear stale slots, then walk new mesh ptr list &DAT_80076c50/&DAT_80076d52 to repopulate via RelocateActorMeshHeader up to 0x40 entries). Final state increment after default branch makes _DAT_8007705c stop advancing. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined TickGemPickupAssetStream(undefined4 param_1, undefined4 param_2, uint param_3);

/* 0x80015370: Level-transition CD-streaming + spawn state machine — drives "switch to a new level" end-to-end. Ticked from Gamestate01_Update, Gamestate0C_Update, Gamestate0D_Update, Gamestate0F_Update, LevelIntro_Update. Shares state variable _DAT_80075864 with TickWorldBundleLoadStream (mutually exclusive driver). Final state = -1 (done). Gate at top: when state >= 2, gates on (CD in-flight clear && CdSync==2 && callback-ready bit 0x40); states 0/1 run unconditionally as the preserve/init entry. Level intro idx _DAT_80075964 indexes per-level descriptor table &DAT_8007a720 stride 0x10 = base LBA + chunk descriptors. Cases: (0) preserve-mob path — StopAllSoundExceptMask, if _DAT_8007576c<0 then save current section4 list (&DAT_80078a44[-3] back, slide down _DAT_800785e8-iVar10), else save mob blob at &DAT_80078640[_DAT_8007576c*4]; CopyWords + rebase section4 entries; CopyWords sample bank to (pool-end - sample_size) + RelocateLevelSampleBank; jump to state 2. (1) fresh init — StopAllSoundExceptMask, zero section4, advance state. (2) g_nCurrentLevelId → _DAT_8007596c, ApplyPerLevelGlobalsTable per-level hooks, recompute _DAT_80075964 = (g_nCurrentLevelId/10-1)*6 + g_nCurrentLevelId%10; if param_1!=0 kick read of level intro splash. (3) read 0x800 header into _g_pDrawBufB. (4) copy 0x1d0 words header to scratch &DAT_80076c00, kick 0x80000-byte first chunk read. (5) two LoadImage(TIM) calls iterating _DAT_8007576c 0..1 (two texture pages 512×256 stacked at +256 Y), then kick remaining-chunk read. (6) SPU upload kickoff (SetSpuTransferStartAddr 0x1010 + WriteSpuRam 0x7eff0). (7) wait for SPU transfer complete. (8) kick read of next chunk sized _DAT_80076c0c. (9) UnpackWorldDataChunks(_g_pDrawBufB, 0) — param 0 = full unpack; if _DAT_80078a40 set and not in gamestate 5/0xc: when _DAT_800758ac==0 snapshot fresh section4 _DAT_80077780..90 to _DAT_80078a40..50, else find matching entry in mob table walking _DAT_80078640..[count_DAT_800758bc] by tag _DAT_800758ac at +7; kick read of chunk size _DAT_80076c14. (10) RelocateMobActorTable if param_1!=0, sanitize mob ptrs below pool base _DAT_800785fc, walk &DAT_80076c50 actor-mesh count list calling RelocateActorMeshHeader up to 0x40 entries into &DAT_80076378 indexed by &DAT_80076d52, set _DAT_800785e4 = end-of-data, snapshot path-table sector LBA. (b) kick read of path-table chunk. (c) wait for camera spin-in to finish: gated by _DAT_800756d0 trigger; if normal-transition (_DAT_800758ac==0) wait until abs((DAT_80078a66 - DAT_80078c5e) & 0xff) < 0x10 (camera yaw matches target), else wait until abs((_DAT_80078b74 + g_nCameraYaw) & 0xfff) < 0x80 (tighter scripted alignment). (d) finalize — set music track + audio params from per-level tables (&DAT_8006ef9c, &DAT_80077420), FillWord 0x68 bytes of &DAT_80077888, call FUN_8001364c level-loader-meat to finish per-actor init, then branch on (_DAT_800756d0, _g_nAudioInitialized, _DAT_800758ac) to position Spyro via ChangeSpyroState (state 0xf if respawn) and set up camera (ComputeCameraToTargetAngle + ComputeCameraOrbitOffset + UpdateCameraEulerAngles + spring camera offsets) — case 0xd is THE per-level Spyro spawn + camera point: scripted-camera path (g_pCameraTargetParams != default) uses ActivateScriptedCameraView, else normal path advances _DAT_80078a60 by -0x1600 + sets per-Spyro-state camera mode from (&DAT_8006c588)[g_nSpyroState]; on completion sets _g_Gamestate=9 (or 0), state = -1 (done), clears 11 transition flags. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined TickLevelTransitionStream(int param_1, undefined4 param_2, uint param_3, undefined1 *param_4);

/* 0x800163e4: CD read stall-recovery hook called from inside CdReadSyncSectors / other M09 CD state-machine wait loops every spin. If a previous read was tagged (DAT_800756e0 != 0) and the stall counter DAT_8007588c crossed it, re-init the CDROM (CdInit), re-register the LAB_80016490 read callback, reset DAT_800774b4=0x40 / DAT_800776c4/c8=0, busy-wait for CdSync()==2, clear DAT_8007588c, then re-issue the original CdRead(sectors=DAT_80076b94, dst=DAT_80076b9c, mode=0x80). No-op when no stall is in flight. Used by all four CD wait paths (FUN_80014564 / FUN_80014b70 / FUN_80015370 level/gem-cutscene state machines + the inline CdReadSyncSectors wait loop). */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined RestartStalledCdRead(void);

/* 0x80016500: Synchronous blocking CD sector read. Two-phase: (1) busy-wait until any previous read is idle (DAT_80076bb8==0 && CdSync()==2 && DAT_800774b4 bit 0x40 set), servicing RestartStalledCdRead + TickActiveSoundVoices + libcard memcard state machine (FUN_8002bbe0) each spin so audio + memcard don't stall during the wait; (2) inline-issue the same CdControl(0xe,2x) + CdIntToPos + CdRead(count, dst, 0x80) body as StartCdReadAsync, then busy-wait again until completion (RestartStalledCdRead-only ticking — sound/memcard frozen during the actual read). Public sync-load API used by Initialize (boot), LoadAssetDirectoryFromCd, PauseMenu_Update, BeginCreditsSequence, LevelIntro_Update, CheckDeathPlane, LoadActorMeshTables. Parameters: param_1=base sector LBA, param_2=dst RAM ptr, param_3=byte count, param_4=byte offset (sectors via >>11), param_5=stall-recovery marker stashed at DAT_800756e0. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined CdReadSyncSectors(int param_1, undefined4 param_2, int param_3, int param_4, undefined4 param_5);

/* 0x80016698: Fires an async CD sector read without waiting. Sets CDROM speed to 2x via CdControl(0xe,&{0x80}), positions the head via CdIntToPos(LBA = param_1 + param_4>>11), then issues CdRead(nsectors = (param_3+0x7ff)>>11, dst = param_2, mode=0x80). Stashes dst at DAT_80076b9c, sector-count at DAT_80076b94, marker at DAT_800756e0 (used by RestartStalledCdRead for retry), and sets DAT_80076bb8=1 as the in-flight flag. Callers poll DAT_80076bb8 / CdSync themselves. Used by M09 CD streaming state machines (FUN_80014564/14b70/15370) + CheckDeathPlane (death-replay reload). */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined StartCdReadAsync(int param_1, undefined4 param_2, int param_3, int param_4, undefined4 param_5);

/* 0x80016784: Walks the per-depth OT slot table at DAT_80075820 backward from index `depth_max` down to 0, splicing each slot's primitive head into the previous slot's tail via 24-bit OT next-addr tags, and returns the head of the finalized chain. The chain is terminated at DAT_8006bbe0 (0xbbe0 written as the term tag). Called as `DrawOTag(LinkOTPrimitives(0x800))` from every Gamestate*_Draw. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined *LinkOTPrimitives(int param_1);

/* 0x800168a0: Appends primitive `param_1` to OT depth slot `param_2` (g_pOtDepthBinArrayBase[param_2*8]). Same head/tail-link logic as AddPrimToOT but takes an explicit slot index instead of using the active slot at g_pOtActiveDepthSlot. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined AddPrimToOTSlot(int param_1, int param_2);

/* 0x800168dc: Appends a primitive to the active OT depth slot (g_pOtActiveDepthSlot). If the slot already has a tail prim, patches its 24-bit next-addr tag to point at `prim`; else stores `prim` as the slot's head. PsyQ-style `addPrim` for this engine's per-depth OT slot table. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined AddPrimToOT(int param_1);

void FillWord(void *dst, uint value, int byte_count);

/* 0x80016930: Unrolled memset that writes a 32-bit value to byte_count bytes in 16-byte (4-word) chunks. byte_count must be a multiple of 16. SetupFrameOT uses it to zero the 0x1c00-byte OT buffer at DAT_8006fcf4. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined FillBlock16(undefined4 *param_1, undefined4 param_2, int param_3);

void CopyWords(void *dst, void *src, int byte_count);

/* 0x800169ac: 2-arg arctan returning 8-bit angle (0..0xff = 0..360deg). Smaller, lower-precision sibling of ArcTan2 — uses byte-LUT at s__________________________________8006d82c. */
int ArcTan2_8bit(int param_1, int param_2);

int ArcTan2(int y, int x, int high_precision);

int LookupSine(uint angle_12_4);

int LookupCosine(uint angle_12_4);

/* 0x80016d08: Wraps GTE LZCS/LZCR. Returns 0 if input is 0, otherwise returns (0x1f - leading_zeros), i.e. the bit position of the most-significant set bit. Used by lighting/depth normalization in DrawActors. */
int CountLeadingZeros(int param_1);

/* 0x80016d2c: Composes Euler rotation (packed yaw/pitch/roll in *param_1, low to high byte) onto the base 3x3 rotation matrix in param_3 (or identity if param_3 is NULL). Uses GTE rtv0 with the sine LUT at DAT_8006cc78 and cosine LUT at DAT_8006cbf8 to multiply axis-by-axis. Writes the composed matrix (5 packed words) to param_2. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined ApplyEulerRotation(uint *param_1, uint *param_2, uint *param_3);

/* 0x80017048: Rotates the 3-vector at param_2 by the 3x3 rotation matrix at param_1 using GTE rtir. The Y and Z components are negated on both input and output (PSX screen-up coord convention). Result written to param_3. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined RotateVectorByMatrix(undefined4 *param_1, undefined4 *param_2, undefined4 *param_3);

/* 0x800170c0: Rotate a 3-vector using the GTE's *currently-loaded* rotation matrix (no matrix load). Y/Z sign-flipped on input/output. Sibling of RotateVectorByMatrix at 0x80017048 which loads its own matrix. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined ApplyActiveGteRotation(undefined4 *param_1, undefined4 *param_2);

/* 0x80017110: Rotate a 3-vector by the camera rotation matrix at DAT_80076de4 (5 packed 32-bit slots = 9 GTE short matrix entries). Y/Z sign-flipped on input/output (PSX up-coord convention). Output written to param_2. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined WorldToCameraRotate(undefined4 *param_1, undefined4 *param_2);

uint VectorLength(int *vec, int include_z);

/* 0x8001729c: One Newton-Raphson refinement step for sqrt(x^2+y^2[+z^2]). Loads vec into IR1/IR2/IR3 (Z skipped when param_3==0), squares via gte_sqr0_b, uses GTE LZCS/LZCR to derive a half-bit-length shift, then returns refined estimate. Skips refinement when param_2^2 fits in <=12 bits. */
int RefineSqrtEstimate(int param_1, int param_2, int param_3);

/* 0x80017330: Set the magnitude of an int[3] vector to param_2 by normalizing then scaling. Uses same GTE-sqrt + g_anSqrtLUT path as IntegerSqrt. Zero-vector input clears to (0,0,0). */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined SetVector3Magnitude(int *param_1, int param_2, int param_3, int param_4);

/* 0x800175b8: In-place scale vector by ratio param_3/param_2 using GTE gpl0. Computes (param_3 &lt;&lt; 12)/param_2 then multiplies each component by it with 12-bit fixed-point. Heavily used by Spyro physics/movement. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined ScaleVector3ByRatio(VECTOR *param_1, int param_2, int param_3);

/* 0x80017614: In-place scale of int[3] vector by IR0 fixed-point factor (gte_gpl0 with MAC=0), then arithmetic right-shift each component by param_3. Used in physics for velocity damping. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined ScaleVector3Shifted(VECTOR *param_1, undefined4 param_2, uint param_3);

/* 0x800176a0: In-place left-shift of an int[3] vector by param_2 bits. Sibling of RShiftVector3. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined LShiftVector3(int *param_1, uint param_2);

/* 0x800176c8: Arithmetic right-shifts each of the 3 signed ints at *param_1 by (param_2 &amp; 0x1f). Used for fixed-point precision conversion in the actor draw path. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined RShiftVector3(int *param_1, uint param_2);

void ZeroVector(int *vec);

void CopyVector(int *dst, int *src);

void AddVector(int *dst, int *a, int *b);

void SubtractVector(int *dst, int *a, int *b);

/* 0x800177c0: Scale VECTOR by IR0 fixed-point factor (gte_gpl0 with MAC=0); store saturated IR1/IR2/IR3 packed via stlvnl0 into long*. Sibling of LerpVectorGTE but single-input + saturated short output. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined ScaleVector3Sat(long *param_1, VECTOR *param_2, undefined4 param_3);

/* 0x80017894: Linear interpolation between two VECTOR points using GTE gpl0. param_4 is the 0x1000-fixed blend ratio (0=param_3, 0x1000=param_2). Output written to param_1. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined LerpVectorGTE(int *param_1, int *param_2, VECTOR *param_3, int param_4);

/* 0x80017908: Absolute shortest delta between two 8-bit angles. Returns |(a-b) wrapped to [-0x80, 0x80)| in [0, 0x80]. */
uint AbsAngleDelta8(int param_1, int param_2);

/* 0x80017928: Absolute shortest delta between two 12-bit angles (0x1000 = 360deg). Returns |(a-b) wrapped to [-0x800, 0x800)| in [0, 0x800]. */
uint AbsAngleDelta12(int param_1, int param_2);

/* 0x80017948: Signed shortest delta between two 8-bit angles. Returns (a-b) wrapped to [-0x80, 0x80). */
uint SignedAngleDelta8(int param_1, int param_2);

/* 0x8001796c: Signed shortest delta between two 12-bit angles (0x1000 = 360deg). Returns (a-b) wrapped to [-0x800, 0x800). */
uint SignedAngleDelta12(int param_1, int param_2);

/* 0x80017990: Octagonal 2D distance approximation: max(|dx|,|dy|) + 3/8 * min(|dx|,|dy|). Faster than sqrt for collision broad-phase checks. */
int ApproxDist2D(int *param_1, int *param_2);

/* 0x800179f0: Step 8-bit angle param_2 toward target param_1 by param_3 step, with deadzone param_4. Returns target unchanged if within deadzone. */
char ApproachAngle8(char param_1, char param_2, char param_3, int param_4);

/* 0x80017a38: Integer square root via GTE LZCS-normalize + LUT (g_anSqrtLUT) + reciprocal-shift. Output is in 12.4 fixed-point (caller >>0xc to get integer)? Returns approx sqrt(param_1). */
uint IntegerSqrt(int param_1);

/* 0x80017aa4: Projects a world-space point to screen XY + depth via the GTE rotation matrix at DAT_80076dd0 / base at DAT_80076df8 — unscaled variant of ProjectWorldPointGTE (no input right-shift or output left-shift). Writes screen.x, screen.y, MAC3 to param_1[0..2]. All known callers live in the camera band. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined ProjectWorldPointGTEUnscaled(int *param_1, int *param_2);

/* 0x80017b48: Projects a world-space point to screen XY + depth via the GTE rotation matrix at DAT_80076dd0 and base point at DAT_80076df8. param_2 = source world point; param_3 = right-shift on input (precision selector); writes screen.x, screen.y, MAC3<<param_3 to param_1[0..2]. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined ProjectWorldPointGTE(int *param_1, int *param_2, uint param_3);

/* 0x80017c4c: Sign-extend a short[3] vector (SVECTOR) into an int[3] (VECTOR). PSX SDK-style helper. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined SVectorToVector(int *param_1, short *param_2);

/* 0x80017c68: Truncate an int[3] vector (VECTOR) into a short[3] (SVECTOR). PSX SDK-style helper. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined VectorToSVector(undefined2 *param_1, undefined4 *param_2);

/* 0x80017cb8: Unpacks a packed collision-triangle from the world's triangle table at *(DAT_800785d4+0x10) into a 9-int output buffer (3 vertices × 3 coords). Table entry size = 12 bytes (3 uint32 packed coord-triplets). Per axis (X, Y, Z): low 14 bits = vertex 0's coord (shifted left 4 to recover world units). Then v1 = v0 + signed-9-bit delta (X/Y) or unsigned-8-bit delta (Z); v2 same scheme with deltas at bits 23..31 (X/Y) or 24..31 (Z). All outputs scaled by 16. Output layout: [v0.X, v0.Y, v0.Z, v1.X, v1.Y, v1.Z, v2.X, v2.Y, v2.Z]. The asymmetric encoding (Z always-positive deltas) reflects that these are upward-facing collision triangles (floor + slope geometry). Callers: ProbeSpyroGroundContact + AverageSpyroPlatformMotion (M07 — collision query fetches a candidate triangle's vertices for ray test). */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined UnpackWorldCollisionTri(int param_1, int *param_2);

/* 0x80017e54: GTE depth-cue color computation. Interpolates the input RGB (param_1, packed 24-bit) toward a far color (param_2, packed as three 12-bit channels: R=bits 0-7, G=bits 4-15 masked, B=bits 12-23 masked) by IR0 factor (param_3, 0..0x1000). Returns the depth-cued RGB color in the low 32 bits, with the far-blue channel echoed in bits 32-39 (probably an artifact of the caller's unpack pattern at Gamestate0C_Draw where it stores all three output bytes into the backdrop color slots). Implementation: gte_ldRGB + gte_ldIR0 + gte_ldfcdir + gte_dpcs + gte_stRGB2. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
ulonglong ComputeDepthCuedColor(undefined4 param_1, uint param_2, undefined4 param_3);

/* 0x80017e98: Convert a buffer of RGB15 pixels (two per 32-bit word) to packed 8-bit luminance bytes. Luma = (4*R + G*(1/16+1/32) + B/1024) &gt;&gt; 3. param_2 = end-offset in bytes. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined ConvertRGB15ToLuma8(uint *param_1, int param_2);

/* 0x80017f24: RLE color-run bias applier. Each run header word encodes byte-offset (low 17 bits, *4-aligned) and count (bits 17..30); negative header = terminator. For each 32-bit RGBA packet, adds param_3 to each byte saturating at 0. Called once from Initialize, likely tinting boot CLUT/HUD palette. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined AddBiasToColorRuns(uint *param_1, int *param_2, int param_3);

/* 0x80017fe4: Emits a glyph-sprite chain for a string: per non-space byte allocates a 0x58-byte slot at DAT_80075710 (queue tail, grows down), maps '0'-'9' and 'A'-'Z' (+ a few specials: '/', '?', '%', '^', '+') to font tile indices, copies the cursor position from param_2 and advances *param_2 by param_3 between glyphs. Sibling of BuildTextSprites. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
void *BuildTextSpriteChain(byte *param_1, int *param_2, int param_3, undefined1 param_4);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
void *BuildTextSprites(byte *param_1, int *param_2, int *param_3, int param_4, undefined1 param_5);

/* 0x8001844c: Emits a 2-point gradient line (LineG2) into the OT. Each endpoint's RGB is tinted by the angular distance between its screen-relative angle (vs (256,120)) and a reference angle in DAT_800770f4 — radial-light effect for menu borders. Called by Gamestate02_03_06_Draw and DrawShadedMenuBox. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined EmitRadialShadedLine(int param_1, int param_2, int param_3, int param_4);

/* 0x80018534: Pushes a sprite16 onto the front of the sprite queue (DAT_80075710 bumps down 0x58 bytes). Gated by (param_2 & 0x1f) < 0x10 so the sprite blinks for half of every 32-frame cycle; param_3 < 2 selects tile U=0 vs U=0x80. Used as a selection marker in Gamestate02_03_06_Draw. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined EnqueueBlinkingMarkerSprite(int *param_1, uint param_2, int param_3);

/* 0x8001860c: Draws a menu rectangle: semi-transparent POLY_F4 fill at (param_1,param_3)..(param_2,param_4) plus four EmitRadialShadedLine borders. Called by SaveLoadMenu_Draw. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined DrawShadedMenuBox(int param_1, int param_2, int param_3, int param_4);

/* 0x80018728: Draws the dragon-rescue cutscene banner: "RESCUED" (g_szUiRescued) + the freed dragon's name (g_apDragonNames[g_pGemPickupSourceActor->id at +0x38]) as two right-aligned BuildTextSpriteChain labels (anchors x=176/x=336), per-glyph alpha pulsed by the sine LUT off g_nGemPickupSecondaryTimer. Called by Gamestate08_Draw. (ex-DrawWorldMapLevelLabel, corrected sweep 40.) */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined DrawDragonRescuedBanner(void);

/* 0x80018880: Refills the null-terminated sprite/HUD primitive request queue at DAT_800720f4 by carving the remainder of the primitive buffer (DAT_80075710 up to DAT_800756fc) into 0x58 (88-byte) chunks and appending each as a queue entry. Called near end of GamestateDraw before DrawSync. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined EnqueuePendingSpritePrims(void);

/* 0x80018908: Builds the "DEMO MODE" text overlay via BuildTextSprites, then phase-pulses each glyph's alpha using the sine LUT at DAT_8006cc78 indexed by DAT_800758c8 (animation phase). Hands the resulting 0x58-byte sprite records into the request queue via EnqueuePendingSpritePrims. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined DrawDemoModeOverlay(void);

/* 0x800189f0: Emits semi-transparent POLY_G4 ribbon segments between consecutive history points of each active "trail" (Spyro flight/glide/charge wake). DAT_80078658 holds per-trail point arrays; DAT_800772c8 holds per-trail point counts; DAT_80075684 is the active trail count. Perpendicular width is divided by point depth (piVar5[5]) and colored on a falloff curve from head to tail. Resets DAT_80075684 to 0 on exit. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined DrawMotionTrailRibbons(void);

/* 0x80018f30: Draws two stacked black POLY_F4 cinematic bars (top y=0..h, bottom y=240-h..240) where h = DAT_800756c0 animates 0..0x16 driven by g_nGameplayBlocked. The "letterbox close" / "open" animation seen during cutscenes and gameplay-blocked transitions. Submits both bars via AddPrimToOT. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined DrawCinematicLetterbox(void);

/* 0x800190d4: Submits a semi-transparent fullscreen POLY_F4 (rect (0,8)-(512,232)) of color (r,g,b) into the active OT depth slot. Allocates from the primitive buffer at g_pPrimBufferWriteCursor; chains via AddPrimToOT. Used for fade/tint overlays (g_nGenericCountdown brightness fade, gem-pickup flash, gem-cutscene fade). */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined DrawFullscreenTint(int param_1, undefined1 param_2, undefined1 param_3, undefined1 param_4);

/* 0x8001919c: Emits a textured POLY_FT4 quad: rect (x,y,w,h)=param_1[0..3] shorts, UV+CLUT=param_2[0..1], RGB color=param_3[0..2] or default 0x80,0x80,0x80. Calls AddPrimToOT and advances DAT_800757b0 by 0x28 bytes. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined EmitTexturedQuad(short *param_1, undefined4 *param_2, undefined4 *param_3);

/* 0x80019300: Pre-audio "loading screen" sprite builder. When g_nAudioInitialized == 0, appends sprite entries into the DAT_800720f4 request queue: optional "%d %d" file-progress text (when DAT_80077fa8 == 4), three banner-sprite groups gated by DAT_80077fa9 / fa, fb / fac, and an "additional" sprite at DAT_800783b4. After the queue is built, the trailing block runs unconditionally to animate the load-bar icons in DAT_8007846c / DAT_8007840c using the sine LUT at DAT_8006cc78 indexed by DAT_800770f4 (icon counts in DAT_80077fe0 / DAT_80077fd4). */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined EnqueueLoadingScreenSprites(void);

/* 0x80019698: Mid-frame scene composer — sequences the actor/sprite/effect passes of one frame: BuildActorDrawList, FillWord(&g_pOtDepthBinHead0,0,0x900) OT clear, EmitActorDrawList, BuildSecondaryActorDrawList + EmitSecondaryActorPrimitives, RasterizeSpritePrimQueue, DrawEntityDirectionalSpikes; unless g_nSpyroDrawSuppressed: RasterizePairedActor (Spyro) + DrawSpyroDropShadow; if g_bSpyroHornCollisionActive: DrawSpyroHornStrikeTrails. Returns DrawWorldParticleEmitters() chained out. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined8 ComposeFrameScene(void);

/* 0x8001973c: Renders the level-entry/exit overlay used by Gamestate01/09 (level transition). Builds the primary banner ("RETURNING HOME...", "ENTERING %s...", or "CONFRONTING %s..." for boss levels — level 4 of each world and level 0x3F) from g_apLevelNameStrings indexed by world/level. From phase 0x40 (post-level results) also emits "TREASURE FOUND" / "TOTAL TREASURE" with the count animated via g_szFmtPercentD: found = g_anLevelGemsCollected[g_nPrevLevelIntroIndex] - g_nLevelGemsAtEntry, interpolated over the gem fly-in (duration 2*(g_nGemTallyIconCount+1) clamped 0x40), plus one gem icon per alive g_pGemTallyParticleRecords record with sprite U from g_abGemTallyIconUvTable. All slide/fade animation is driven by g_nLevelTransitionPhase; panel hold ends at g_nGemTallyHoldEndPhase. Clears g_apSpritePrimQueue[0], then EnqueuePendingSpritePrims + RasterizeSpritePrimQueue to flush directly. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined DrawLevelTransitionOverlay(undefined4 param_1, undefined4 param_2, uint param_3, undefined4 param_4);

/* 0x8001a050: Draws Gamestate 1 (boot/early splash) and 9 (sibling): clears OT, draws a rotating 3D model (via FUN_8004eba8 with matrix built from DAT_80076e1c/e1e/e20 rotation angles), copies background RGB from DAT_80078a50 into draw-env clear color, then VSync/frame submit. DAT_80075910 is an animation phase counter that decrements by 2 each frame; when 0, uses identity matrix. Active model render only when DAT_80078a40 != 0. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined Gamestate01_09_Draw(void);

/* 0x8001a40c: Draw for gamestates 2 (pause menu), 3 (inventory sub-menu), 6 (overlay-rendered dialog). g_nPauseMenuTransitionFrames==0 (entry frame): renders one full gameplay scene (BuildRenderEntityLists/ComposeFrameScene/RasterizeEmitList/DrawActors), then builds the frozen backdrop — StoreImage's the frame, ConvertRGB15ToLuma8 packs 4 strips to 8bpp luma in g_pRenderScratchRegionBase, reloads them at VRAM x=0x200 and loads g_anMenuBackdropGrayClut to (0x200,0xe0) (clut id 0x3820). Later frames: draws 4 textured quads of the gray backdrop + menu UI. State 2: PAUSED box (substate 0 = CONTINUE/OPTIONS/INVENTORY/QUIT-or-EXIT-LEVEL, 1 = options incl. DualShock-gated VIBRATION, 2 = QUIT GAME? YES/NO); cursor glyphs wave via g_anSineLut. State 3: inventory — header gem/dragon/egg tallies + per-level rows of the g_nSubMenuCursor world (only rows with g_abLevelVisitedFlag set), scroll arrows gated by the +/-6 virtual bases of g_abLevelVisitedFlag. State 6: backdrop + overlay-enqueued sprites only. Ends with sprite-queue rasterize, 2-vblank pacing (g_nVsyncFramePaceAnchor), increments g_nPauseMenuTransitionFrames. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined Gamestate02_03_06_Draw(void);

/* 0x8001c694: Draws gamestate 0xA (pause -> world-map transition, set by BeginPauseToMenuTransition). g_nPauseMenuTransitionFrames==0 phase: re-darkens the frozen gray backdrop by re-loading g_anMenuBackdropGrayClut SHIFTED — rect {x=0x200+g_nPauseMenuIdleFrames, y=0xe0, w=0x20-idleFrames} maps ramp[0..w] onto CLUT entries idle..31, so the bright end dims as the 0..32 idle counter runs — then draws the 4 backdrop quads (clut id 0x3820) into OT slot 0x3ff. ==1 phase: fog cross-fade — g_nFadeProgress=(0x20-idle)*0x80 through ComputeDepthCuedColor into the back DRAWENV r0/g0/b0 + EmitStaticActorMeshListFogged. Both phases: OT clear, RasterizePairedActor (Spyro), DrawOTag, 2-vblank pacing via g_nVsyncFramePaceAnchor. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined Gamestate0A_Draw(void);

/* 0x8001ca38: Draw routine for g_Gamestate == 4 or 5 (in-level / game-over). Two phases gated by g_nGameplayDrawMode (DAT_80075940): mode 0: normal in-level frame. On frame 0 calls the full game render chain (FUN_800521c0/19698/573c8/50bd0/2b9cc); subsequent frames flash a fade color via FUN_800190d4. mode 1: game-over sequence. After frame 0x3c, builds the "GAMEOVER" letter sprites (s_GAMEOVER_8006f304 / DAT_8006f2b4 layout table), and after frame 0x1a5 also renders "PRESS START". g_nGameplayDrawFrame (DAT_8007593c) is the per-mode frame counter that drives both fade timing and the GAMEOVER reveal animation. Tail is the standard PutDispEnv/PutDrawEnv/DrawOTag frame submit. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined RespawnOrGameOver_Draw(void);

/* 0x8001cfdc: Draws Gamestate 8: gem-cutscene / gem-pickup overlay. Has 8 substates (0..7) driven by g_nGemCutsceneSubstate: 0: full render chain (normal gameplay frame, fade-only effect). 1-4: composite render with partial OT buffers and dragon-statue sprites. 5-7: cutscene-only render with timed model placement. Uses g_nGemCutsceneTimer for animation timing. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined Gamestate08_Draw(void);

/* 0x8001d718: Draws Gamestate 0xB: SAVE / LOAD MENU. DAT_80078d00 == 1 enables UI render (else skip straight to gameplay underlay). DAT_80078d0c is the menu substate: 0: main menu (SAVE GAME / REPLAY DRAGON / CONTINUE) 1: 'NO SAVE FILE' (insert memory card) 2: 'SAVING...' 3: 'NO MEMORY CARD' 4: 'NO SAVE FILE' 5: 'SAVE ERROR' 6: 'SAVE FAILED' (RETRY/ABORT) 7: 'GAME SAVED' DAT_80078d08 is the menu cursor index. DAT_80078d18 selects SLOT 1 vs SLOT 2. DAT_80078d10 is an x-coordinate offset for menu placement. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined SaveLoadMenu_Draw(void);

/* 0x8001e24c: Draw routine for g_Gamestate == 0xC (level-transition state). Renders the "ENTERING %s WORLD" / "ENTERING %s" title-card overlay using BuildTextSprites, then submits the frame. Active only when the 0xC substate g_nGamestate0cSubstate (DAT_800777e8) is 4 or 5; otherwise dispatches to the function pointer at DAT_800758d8. World/level names are looked up by g_nCurrentLevelId (DAT_800758b4): worldIdx = id / 10 -> PTR_s_DRAGON_X_8006f7d0[worldIdx] levelIdx = id - worldIdx*4 -> PTR_s_PEACE_KEEPERS_8006f7d8[levelIdx] Tail (DrawSync/VSync/PutDispEnv/PutDrawEnv/DrawOTag) is the standard frame-submit sequence shared by every Gamestate*_Draw routine. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined Gamestate0C_Draw(void);

/* 0x8001e6b8: Draws Gamestate 0xD when DAT_80078d78 == 3: 'story intro' splash that fades in/out one of three messages (selected by DAT_80078d94): 0: 'IN THE WORLD OF DRAGONS...' 1: 'THE ADVENTURE BEGINS...' / 'THE ADVENTURE CONTINUES...' (DAT_80078e78 picks variant) 2: 'ENTERING DEMO MODE...' DAT_80078d80 is the per-message frame counter (used for fade alpha; animation starts when the value crosses the message-specific threshold). */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined StoryIntro_Draw(void);

/* 0x8001e9c8: Draws Gamestate 0xE: gem cutscene (gem displayed in 3D with countdown fade). Sets clear color from DAT_80078a50, calls FUN_800258f0(-1) to register the gem model, then FUN_8004eba8(-1,...) with identity matrices to render. Uses g_nGenericCountdown for the closing fade-to-black. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined GemCutscene_Draw(void);

/* 0x8001eb80: Draws Gamestate 0xF when DAT_80075704 >= 99: post-credits 'RETURNING HOME...' message with animated text wave. Clears bg, builds text sprites for the message, then animates per-character brightness by stepping a 4-byte stride into DAT_8006cc78 (sin/intensity LUT) indexed by DAT_800756f8 (animation phase counter). */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined ReturningHome_Draw(void);

/* 0x8001ed5c: Per-frame draw dispatcher (partner of GamestateUpdate). Flips g_pActiveFrameDrawEnv between the two frame DBs, reloads g_pPrimBufferWriteCursor/g_pOtDepthBinArrayBase/g_pOtActiveDepthSlot from the DB tail (+0x70/74/78), clears g_nPrimBufferOverflowFlag, then for gamestate 0 runs the full scene pipeline (BuildRenderEntityLists -> ComposeFrameScene -> SetupFrameOT -> DrawActors -> RasterizeEmitList + overlays), paces to >=2 vblanks via g_nVsyncFramePaceAnchor, PutDispEnv/PutDrawEnv, DrawOTag. Other states branch to per-state Draw functions; state 7 calls g_pfnGamestate7DrawHook. Writes g_dwWorldFogColor into both DRAWENVs' r0/g0/b0 each gameplay frame. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined GamestateDraw(void);

/* 0x8001f158: First half of the actor render pipeline. Walks the actor reference array at DAT_8006fcf4, frustum/depth-culls each record using the camera position (DAT_80076df8/dfc/e00) and bounding radius from the actor record, then writes per-visible-actor display records (14 ints each) to the draw list at DAT_800712f4. Each record stores actor pointer, mesh ptr, texture-page ptr, world-to-screen rotation submatrix (lazily wobbled via the optional roll/pitch/yaw fields in DAT_80076dd0..e0), and clip-space position. Terminated by 0. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined BuildActorDrawList(void);

/* 0x8001f798: Second half of the actor render pipeline. Walks BuildActorDrawList's output at DAT_800712f4 and, for each visible actor, runs the GTE per-vertex transform + lighting pass (rtps/intpl/nclip), emits POLY_F3/POLY_G3/POLY_FT4/POLY_GT4 primitives into the OT depth bins at DAT_8006fcf4, and writes per-actor scratch (depth-cued colors at DAT_80070df4). Reads per-frame fog params at DAT_80078a50 and ambient/background colors via gte_ldRFC/gte_ldGFC/gte_ldBFC. Submits via per-bin insertion (not AddPrimToOT) for correct depth sort. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined EmitActorDrawList(void);

/* 0x800208fc: Sibling of BuildActorDrawList. Walks the actor pointer table at DAT_80071ef4 (terminated by NULL) — a different source than BuildActorDrawList's DAT_8006fcf4 depth-bin list — performs frustum culling against bounds in DAT_80076df8/dfc/e00, then composes each actor's per-axis Euler rotation (packed in *(actor + 0x44)) onto the camera matrix DAT_80076dd0..de0 via GTE rtv0 + sine/cosine LUTs. Appends 0x38-byte entries (transform + matrix + back-pointer) to the visible-actor list at DAT_800712f4. Writes the terminator 0 at the end. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined BuildSecondaryActorDrawList(void);

/* 0x80020f34: Sibling of EmitActorDrawList. Walks the visible-actor list at DAT_800712f4 (filled by BuildActorDrawList + BuildSecondaryActorDrawList). For each entry, loads the per-actor 3x3 matrix and translation into the GTE, sets background depth via gte_ldbkdir for per-vertex lighting, and emits transformed mesh primitives to the scratch buffer at DAT_800726f4 / the active prim buffer at DAT_800757b0. Restores DAT_800757b0 on completion. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined EmitSecondaryActorPrimitives(void);

/* 0x80022a2c: Walks the sprite/HUD request queue at DAT_800720f4 (refilled by EnqueuePendingSpritePrims) and rasterizes each request. For each actor record: applies per-axis billboard rotations from `actor.flags` (gte_rtv0 for X/Y/Z roll/pitch/yaw bytes), runs the per-vertex GTE transform with optional Z-bias depth cueing, emits POLY_F3/POLY_F4/POLY_G4 primitives into the OT depth bins, and updates the per-bin head/tail pointers (DAT_8006fcf4 / DAT_8006fcf8) directly for depth-sorted insertion. Inserts a TPAGE/DRAWMODE command before the first run of primitives in a frame. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined RasterizeSpritePrimQueue(void);

/* 0x80023ac4: Rasterizes a pair of actor records selected via DAT_80078a70..7b index tables (e.g. Spyro + a coupled prop / shadow companion). Loads mesh and texture-page data for both records from DAT_80076378's actor table, optionally intpl-blends per-vertex normals between the two actors when DAT_80078a7c/7d/7e flags are set, runs the GTE transform/lighting pass with shared camera matrix (DAT_80076dd0..e0) and color refs (DAT_80078a64/68/6c), and emits POLY_GT4 / POLY_G4 / sprite primitives into the OT depth bins. Handles a Gouraud color interpolation table at DAT_80070df4 when distance/depth fog applies. Largest single rasterizer in the binary; investigate further before depending on field interpretations. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined RasterizePairedActor(void);

/* 0x800258f0: World chunk renderer. Loads the camera GTE rotation matrix from DAT_80076de4..df4, then iterates a chunk pointer table: if param_1 &gt;= 0 it walks the per-LOD entry at DAT_800785b0 + param_1*4 (terminated by byte 0xff); if param_1 &lt; 0 it walks the full visible-chunk list at DAT_800785a8 (length DAT_800785ac). For each chunk, projects its anchor through gte_rtir_b, performs frustum/depth tests, and on visibility emits primitives into the OT depth bins via the scratch buffer at DAT_800771c8. Also called by GemCutscene_Draw. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined RenderWorldChunks(int param_1);

/* 0x8002a6fc: Per-frame world chunk-animation driver — advances all 6 world chunk-anim tables by param_1 ticks. Called once per game frame from GamestateUpdate. Each table has the layout: count at DAT_80078560/8/70/8/80/8 + ptr-array at DAT_80078564/c/74/c/84/c (last pair = DAT_800785a0/a4). Per-entry header bytes: [+1]=state-flags (bit 2=halt, bit 0=arm), [+2]=current step index, [+3]=delay counter, [+4]=target index. Each tick: skip if halted, else delay -= param_1 → if expired advance state via the RLE/state table at +8 (4-byte step entries) or +0xc (8-byte step entries) following the (bit&1?next_b:next_a) successor links; arm new delay from step byte and write its payload to the world target. The 6 sub-tables drive: (1) full-mesh keyframe copy (0xa8-byte vertex blob at DAT_800785c4 + 0x10-byte header at DAT_800785c0); (2) UV scroll/cycle that writes 6/12/8/8/8 byte UVs at fixed offsets in iVar3/iVar7 producing a rotating-byte pattern; (3..6) single-byte writes at offsets +0x18..+0x1b of section0-indexed targets (color cycle); (7) packed-21-bit-per-axis vertex morph at DAT_800785a4 that decodes the source triplet then writes the dest triangle's packed-XY anchors (with optional GTE matrix interpolation when secondary keyframe selected by step byte). Skips advance if not yet due (just decrements delay counter). */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined TickWorldChunkAnimations(int param_1);

/* 0x8002b4ac: Sibling of TickWorldChunkAnimations that snaps every world chunk-anim table to its current keyframe state without advancing time (no delay decrement, no RLE walk). Same 6 tables: copies the current-keyframe full-mesh blob (0xa8 + 0x10), applies the current UV rotation pattern, writes the current step-index byte to each color-cycle target byte (+0x18..+0x1b), and rebuilds the packed-XY triangle anchors at DAT_800785d4+0x10 from the source triplet. Used by the level-loader (FUN_8001364c) to seed all world animations to their initial keyframe before gameplay begins. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined ResetWorldChunkAnimations(void);

/* 0x8002b9cc: Frame OT/depth setup. Memcopies an empty OT into DAT_8006fcf4 (size 0x1c00), then picks the draw-distance constant DAT_800785d0 by context: In-level (DAT_80076e24 &lt; DAT_800785b4): 0x28000 (far draw distance, OT slot = current LOD). Cutscene story-intro / gem-cutscene (GS_STORY_INTRO or GS_GEM_CUTSCENE): 0x1c000 (close). Otherwise: 0x14000 (default UI / shorter range). Then calls FUN_800258f0 to publish the chosen OT range. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined SetupFrameOT(void);

/* 0x8002bb20: Returns the "X%" completion percentage shown in the inventory header (Gamestate 3). Formula: (g_nWorldTreasureCollected*50 + g_nWorldDragonsRescued*6000 + g_nWorldEggsRecovered*10000) / 12000 — i.e. 12000 gems = 50%, 80 dragons = 40%, 12 eggs = 10%, Spyro 1's exact 100% split. When treasure exceeds 12000 (Gnasty's Loot's extra 2000), switches to 100 + (treasure-12000)/100, giving the famous 120% cap. */
int ComputeInventoryProgressPercent(void);

/* 0x8002bbe0: Per-frame CD-DA / XA music stream state machine + volume fade ramp. NOT memcard despite earlier triage. Ticked every game frame from GamestateUpdate / PauseMenu_Update / Gamestate0D_Update / GemCutscene_Update / CheckDeathPlane, and additionally from inside CdReadSyncSectors's wait loop so music keeps advancing during long sync CD reads. (1) Polls CdSync(1, &DAT_800776bc); on completion of the in-flight cmd at DAT_800776c4 (mid-range 8..9 cmds need status-bit 0x200, 0x1b=ReadS uses a different mask), clears the cmd code and stamps DAT_800774b4 status. If a pending music cmd is queued at DAT_800776c8, dispatches HandleMusicCommand(track=DAT_800774b0, cmd=DAT_800776c8) and clears the pending slot. (2) If music-stream-active (DAT_800774b4 bit 0x10): on CdSync==2 idle, if read head DAT_800776b8 has passed the current track end (DAT_800774bc[track*8]), maybe advance to a random track from the per-world triple at &DAT_8006ef9c[DAT_80075964*0xc/4] (track guard via DAT_800776cc cooldown vs DAT_800758c8 timer) then CdControlBlocking(0xd=setfilter, filter=track&7) + CdControl(0x1b=ReadS, LBA), else (still inside track) issues CdControlForce(0x11=getlocL) and copies the absolute time at &DAT_800776c1 back into DAT_800776b8. On CdSync==5 hold, re-issues 0x1b ReadS at the track-start LBA. (3) Volume fade ramp: animates current vol DAT_80075f18 toward target DAT_800761d8 by DAT_80076230 step/frame; on reaching target, issues a finalizing CdControl (e.g. cmd 8/9 for unmute/mute via DAT_800776c4 path) and resets the request. Final volume byte stamped into DAT_80075f08=0xc0 buffer and pushed via SetSpuCommonAttr. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined TickCdMusicStream(void);

/* 0x8002bfe0: Gem-cutscene per-frame driver — interpolates camera transform + per-actor poses from the scripted cutscene path table at _DAT_80075680. Reads _DAT_80075680[0] = current sub-frame index, [3] = actor count, [4] = camera keyframe array base (24-byte entries: pos[12]+euler[12]). Writes camera pos to DAT_80076df8/dfc/e00 and Euler to g_nCameraEulerRoll/Pitch/Yaw. Per-actor (i=0..count-1): looks up actor's mob-type byte at *(*(mob_table[id]+0x38)+0xc) to choose a per-mob keyframe-rate divisor — 0x10 → /8 (8fps phase, low 3 bits ×8), 0x15 → /6 (custom), 0x20 → /4, default → /2 (60fps). Stamps actor's per-frame anim-state (4 bytes at record+0x3c..+0x3f for current/next keyframe indices, +0x40 = inter-frame phase byte) and linearly-interpolates XYZ from packed 10-bit-per-component vertex pairs at *(_DAT_80075680+0x14+id*4) into actor record+0xc/+0x10/+0x14. Sole caller GemCutscene_Update. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined AdvanceGemCutsceneFrame(void);

/* 0x8002c420: Enters GS_PAUSE_MENU: g_nGamestate=2, zeros pause-menu cursor/substate/idle frames, plays the menu-open sfx (sample id from g_pLevelSampleBankHeader+0x2c). param nonzero = entered from gameplay/sub-menu (StopAllSoundExceptMask(0) first, resets transition frames); param 0 = re-entry path that forces all 5 HUD counters visible with roll phase 0xc. Flight levels skip the HUD work. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined EnterPauseMenu(int param_1);

/* 0x8002c534: Exits pause menu back to gameplay: g_Gamestate=0, restores pause-snapshot framebuffer from _DAT_800785f0-0x1c200 via LoadImage, repaints HUD palette indices to {3,…,0xd}, releases memcard slot. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined ExitPauseMenuToGame(int param_1);

/* 0x8002c618: Begins the pause→world-map curtain animation: g_Gamestate=10 (GS_MENU_TRANSITION), zeros pause idle frames, kicks ChangeSpyroState(NULL,…) to freeze the player. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined BeginPauseToMenuTransition(undefined4 param_1, undefined4 *param_2, int param_3, undefined1 *param_4);

/* 0x8002c664: Returns to the current world hub from a sub-level: g_Gamestate=1 (logo/splash), g_nGamePaused=1, snaps g_nCurrentLevelId down to world id (id/10*10). Called from SpyroStateDelay_Update at homecoming-portal exit. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined BeginWorldHubReturn(void);

/* 0x8002c714: Enters GS_INVENTORY (g_Gamestate=3); seeds g_nSubMenuCursor to g_nCurrentLevelId/6 so the cursor lands on the active world row; zeros scroll state and clears the pad-history ring. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined EnterInventoryMenu(int param_1);

/* 0x8002c7bc: Exits the sub-menu back to title (g_Gamestate=0); restores the title snapshot framebuffer from _DAT_800785f0-0x1c200, zeros all five HUD palette indices, releases memcard slot 8. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined ExitSubMenuToTitle(void);

/* 0x8002c85c: Player death dispatch: if g_nExtraLives (DAT_8007582c) > 0 decrement and go to g_Gamestate=4 (GS_IN_LEVEL, respawn); else g_Gamestate=5 (GS_GAME_OVER). Resets g_nGameplayDrawMode/Frame to 0 so the frame counter restarts cleanly. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined TriggerRespawnOrGameOver(void);

/* 0x8002c8a4: Sets g_Gamestate=0 (gameplay) and restores HUD palette indices to defaults; ends the respawn (state 4) and game-over restart (state 5) paths of RespawnOrGameOver_Update — its only two callers. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined EndRespawnToGameplay(void);

/* 0x8002c91c: Empty body (single `jr ra`). Placeholder for a removed substate handler in Gamestate06_Update — likely a stub left over from a cut feature (e.g. an animated logo/title sub-state). Kept as a labeled no-op so the call-site dispatch table stays at a stable offset. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined Gamestate06SubstateStub_Noop(void);

/* 0x8002c924: Begins the dragon-rescue statue cutscene overlay (g_nGamestate=8 = GS_GEM_PICKUP — family rename to "dragon rescue" pending, see M14 notes). Seeds the cutscene substate/timer globals, increments BOTH dragon counters in lockstep (g_anLevelDragonsRescued[g_nLevelIntroIndex] per-level + g_nWorldDragonsRescued world total, with the HUD cache primed from the latter), blocks gameplay (g_nGameplayBlocked=1, g_nGamePaused=1), flags the source actor's render record at +0x3c, anchors the aura star above the statue, and fires the rescue chime sfx. Special cases: Artisans home (level 10) upgrades pickup type 1->2 once any sibling-level g_abLevelGemPercent entry is nonzero; Gnasty's Loot (level 0x3c) rewrites the actor params for the key-locked treasure variant. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined BeginGemPickupOverlay(undefined4 *param_1);

/* 0x8002cb6c: Ends the gem-pickup overlay back to gameplay: g_Gamestate=0, g_nGameplayBlocked=0; restores the world camera matrix from the DAT_80076e60 stash, advances camera angle by g_dwCameraAngleDelta, calls ChangeSpyroState(1,…) to unfreeze the player. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined EndGemPickupToWorld(undefined4 param_1, undefined4 param_2, undefined4 param_3, undefined1 *param_4);

/* 0x8002d02c: Ends the save-menu (GS_SAVE_MENU) back to gameplay: g_nGamestate=0, resets Spyro (ResetSpyroEntity) + camera mode 0, snapshots the live camera pose INTO g_anCameraDynamicTargetParams (CopyWords from &g_nCameraYaw, 0x18 BYTES = 6 ints), biases slot[0] yaw by g_nCameraTargetYawBiasFromMode, and points g_pCameraTargetParams at it so the world-return camera eases from the menu pose instead of a canned table. Re-inits HUD counters, restores music track. Mirrors EndGemPickupToWorld. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined EndSaveMenuToWorld(undefined4 param_1, undefined4 param_2, undefined4 param_3, undefined1 *param_4);

/* 0x8002d170: Begins the "IN THE WORLD OF DRAGONS…" story intro: g_Gamestate=13 (GS_STORY_INTRO). FillWord zeros 0x5c bytes starting at g_nGamestate0dMode, resets sfx/music volume to 10, primes the title-CD anchor at DAT_80078dc4 to the CD buffer end-2000, and kicks the libpress audio start at FUN_8006275c(0x159). */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined BeginStoryIntro(void);

/* 0x8002d228: Begins the credits sequence: g_Gamestate=15 (GS_CREDITS), g_nGamestate0fMode=-1. Clears the screen to black, calls ChangeSpyroState(1,…) to freeze the player, loads the credits image via CdReadAndCopyToRam. param_1 nonzero (post-game variant) also runs FUN_8005b7d8 (audio reset) and marks DAT_800757ac=10 (post-game flag). */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined BeginCreditsSequence(int param_1);

/* 0x8002d338: Begins the 3D gem-cutscene model render (g_Gamestate=14 = GS_GEM_CUTSCENE). Builds a 0x10-byte intro packet at &DAT_800778f0 keyed off DAT_8007566c (cutscene id), with sound bank id from &DAT_8006ee6c[id], and sets sfx/music vols to max (0x3fff). */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined BeginGemCutscene(void);

/* 0x8002d440: Dispatches a special-cutscene request based on DAT_8007566c: 1 → clears screen + g_Gamestate=13 (story intro with substate 3 / message 1, calls FUN_8001277c to reload the new-game state, jumps to world 10); 2 → BeginCreditsSequence(0) (mid-game credits); 3 → BeginCreditsSequence(1) + sets the post-game marker DAT_800757ac=10. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined DispatchSpecialCutscene(void);

/* 0x8002d554: Zeros the 16-slot pad-history ring at DAT_80076bc0 and resets the write index DAT_80075928 to 0. Called by cheat-code reset (after a successful match) and by EnterInventoryMenu so cheat sequences don't carry across the menu open. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined ClearPadHistoryRing(void);

/* 0x8002d580: Level-select (warp) mode input, active while g_nLevelSelectModeActive: face/shoulder buttons pick the sub-level digit (0x20->0, 0x40->1, 0x80->2, 0x10->3, 0x2000->4, 0x4000->5, 0x8000->6); UP (0x1000) auto-advances g_nCurrentLevelId to the next valid id (wraps sub>5 to next world, world>6 back to 10) and arms g_nLevelSelectWorldIndex. A sub-press while a world is armed commits: g_nCurrentLevelId = world*10+sub, resets Spyro (RestartSpyroAnimWithState 0xf), g_nGamestate=1 + g_nCdStreamState=1 (level load), camera mode 0x12, clears g_nLevelSelectModeActive, records g_nLevelSelectSubIndex. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined HandleLevelSelectInput(undefined4 param_1, undefined4 *param_2, int param_3, undefined1 *param_4);

/* 0x8002d810: Cheat-code dispatch (slot index from CheckCheatCodeMatch). Slot 2 = skip-credits: stashes g_nActiveLevelId in g_nCreditsReturnLevelId then BeginCreditsSequence(0). Slot 3 = unlock: fills g_abLevelVisitedFlag[0..31] with 1 (32 levels, not the egg-less tail) + g_abSaveOptionsBlock[0..5] with 2. Slot 4 = g_nExtraLives=99. Slots 0/1/5-7 have no in-EXE effect (their sequences exist in g_anCheatCodeSequences — likely consumed by overlay code, possibly the g_nLevelSelectModeActive raiser). Slots 3/4 play the confirmation jingle (sample id at g_pLevelSampleBankHeader+0). */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined ApplyCheatCodeEffect(int param_1);

/* 0x8002d8ec: Cheat-code matcher: appends g_dwPadPressed to the 16-slot g_anPadHistoryRing (index g_nPadHistoryIndex); for each of the 8 sequences in g_anCheatCodeSequences (stride 0x40 = 16 ints, 0-terminated) compares the last len presses of the ring; on match calls ApplyCheatCodeEffect(slot) + ClearPadHistoryRing. Only runs while g_nCdStreamState < 0 (no stream in flight). Called from SubMenu_Update (inventory screen). */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined CheckCheatCodeMatch(void);

/* 0x8002da74: Update half of the level-transition gem tally (sole caller Gamestate01_Update; was misread as a GS_LOGO_SPLASH fire ring). At phase 0 computes g_nGemTallyHoldEndPhase and clears the alive flags of all 64 records at g_pGemTallyParticleRecords (reused g_apSecondaryEntityList storage). Advances g_nLevelTransitionPhase by g_nFrameStep; from phase 0x80 spawns one gem-icon particle per tally unit (i-th at phase 0x80+2i, i < g_nGemTallyIconCount, max 32) with randomized arc params, then flies each through three sine arcs (age 0..0x30 rise, 0x30..0x60 hop, 0x60..0x93 fall into the total counter) and retires it — pickup sfx every 8 phase steps on spawn, chime every 3rd particle on landing. Past phase 0x1a0 clears g_nLevelTransitionTallyActive. DrawLevelTransitionOverlay renders one gem sprite per alive record. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined AnimateGemTallyParticles(void);

/* 0x8002df9c: [inferred] g_Gamestate==1 handler. Brief: calls FUN_8004a7ec, FUN_80037bd4, FUN_8002da74; conditionally calls FUN_80015370 when DAT_80075864<0xb or DAT_800756b0==0. Role unclear — kept generic name pending more context. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined Gamestate01_Update(undefined4 param_1, undefined4 param_2, uint param_3, int *param_4);

/* 0x8002e000: Gamestate 9 handler (set by TickLevelTransitionStream after a level transition): runs a normal gameplay tick (TickSpyroGameplayFrame + UpdateCameraFrame) while the scripted arrival camera settles. Normalizes g_nCameraEulerPitch to signed 12-bit; returns to gamestate 0 when pitch < -0x200 OR g_pCameraTargetParams has latched g_anCameraMode12Params (the mode-0x12 arrival block). */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined CameraSettle_Update(undefined4 *param_1, undefined4 *param_2, int param_3, int *param_4);

/* 0x8002e084: [inferred] g_Gamestate==10 handler. Counter g_nPauseMenuIdleFrames (reused) increments. At frame 16 (when DAT_800758b8==0) calls ChangeSpyroState(0xf,...). At frame 32 resets counter, increments DAT_800758b8; when it reaches 2 calls FUN_8002c664. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined SpyroStateDelay_Update(undefined4 param_1, undefined4 *param_2, int param_3, int *param_4);

/* 0x8002e12c: g_Gamestate==2 handler. Pause menu input loop: reads g_dwPadButtons (bits 0x10/0x40/0x1000/0x2000/0x4000/0x8000), moves g_nPauseMenuCursor, plays SFX from DAT_800761d4+0x2d..0x2e on every move/select. Sub-mode in g_nPauseMenuSubstate (0=top, 1=options, 2=quit). Cases adjust SFX/music/vibration volumes and quit-to-title. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined PauseMenu_Update(void);

/* 0x8002eb2c: Gamestate 3 (inventory sub-menu) update. Ticks HUD bob/cycle clocks + RotateLightVectorXZ; after 4 transition frames: if g_nLevelSelectModeActive routes everything to HandleLevelSelectInput; else runs CheckCheatCodeMatch, then horizontal world scrolling — L1/R1-style presses (0x8000/0x2000) start a +/-0x40-per-frame slide of g_nSubMenuScrollOffset toward +/-0x1c0 where g_nSubMenuCursor steps and the offset flips sign (gated on the neighbor world's home-level g_abLevelVisitedFlag via the +/-6 virtual base); 0x810 buttons re-enter the pause menu (EnterPauseMenu(0)), 0x140 exits via ExitSubMenuToTitle. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined SubMenu_Update(void);

/* 0x8002edf0: Gamestate 4+5 handler (ex-LevelIntro_Update misnomer), mirror of its setter TriggerRespawnOrGameOver. State 4 (lives left): wait 0x10 frames, EndRespawnToGameplay + LoadAndStartLevelFromCd reload. State 5 (game over): CD-loads the game-over scene, resets g_nExtraLives=4 / sparx, latches g_nGameOverLevelIntroIndex, FillWord-clears the 0x68-byte level-persistence block, then flies Spyro in a spiral (frames 0xb4..0x174) over the GAMEOVER letters until Start (pad 0x800) restarts via TickLevelTransitionStream. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined RespawnOrGameOver_Update(undefined4 param_1, undefined4 *param_2, uint param_3, undefined1 *param_4);

/* 0x8002f3c4: Gamestate 6 handler — intentionally empty (calls the no-op substate stub). NO in-EXE writer ever sets g_nGamestate=6 (full setter map audited sweep 45: 1=HandleLevelSelectInput/BeginWorldHubReturn/portal, 2=EnterPauseMenu, 3=EnterInventoryMenu, 4/5=TriggerRespawnOrGameOver, 7=PauseMenu_Update overlay handoff, 8=BeginGemPickupOverlay, 9=TickLevelTransitionStream, 0xA=BeginPauseToMenuTransition, 0xD=BeginStoryIntro/DispatchSpecialCutscene, 0xE=CheckDeathPlane, 0xF=BeginCreditsSequence) — state 6 is entered only by overlay code poking g_nGamestate by absolute address. Its Draw path (Gamestate02_03_06_Draw) renders just the grayscale frozen-scene backdrop + whatever sprites the overlay enqueued: an overlay-rendered menu/dialog state (balloonist dialogue is the prime candidate; confirm when overlay disassembly lands). */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined Gamestate06_Update(void);

/* 0x8002f3e4: g_Gamestate==8 (GS_GEM_PICKUP) handler. 8-state submachine via g_nGemPickupSubstate (0..7) and g_nGemPickupTimer drives the gem-collect / dragon-rescue overlay: camera orbits via ArcTan2 + LookupSine/Cosine, spawns icons DAT_800770b8/bc, plays sfx bank DAT_800761d4+0x31..0x35. Substate 7 ends via EndGemPickupToWorld back to GS_DEFAULT. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined GemPickup_Update(undefined4 param_1, int *param_2, uint param_3, int *param_4);

/* 0x800314b4: [inferred] g_Gamestate==0xB handler. Not yet decompiled in detail. Generic name retained. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined Gamestate0B_Update(undefined4 param_1, undefined4 param_2, undefined4 param_3, undefined1 *param_4);

/* 0x800324d8: [inferred] g_Gamestate==0xC handler. Not yet decompiled in detail. Generic name retained. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined Gamestate0C_Update(undefined4 param_1, undefined4 param_2, uint param_3, undefined1 *param_4);

/* 0x80032b08: g_Gamestate==0xD primary handler — the story-intro flow ("IN THE WORLD OF DRAGONS…"). Reached when g_nGamestate0dMode==3; other modes dispatch into Gamestate0D_Alt (overlay slot at 0x8007abac). Drives a 3-state submachine via g_nStoryIntroSubstate (0=enter/CD-load init, 1=stream tier-N CD chunks until ready, 2=camera fly-by + per-glyph text emit, ends by either BeginGemCutscene or fading to level-load). g_nStoryIntroMessage selects the variant (0=intro, 1=mid-game interlude, 2=death-retry story). */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined Gamestate0D_Update(undefined4 param_1, undefined4 param_2, uint param_3, undefined1 *param_4);

/* 0x800331ac: g_Gamestate==0xE (GS_GEM_CUTSCENE) handler. Paired with GemCutscene_Draw (8001e9c8) which renders the 3D gem model. On entry (timer==0, cutscene id &gt; 1) it issues a synchronous CD-load loop and re-seeds via BeginGemCutscene; per tick it advances *_DAT_80075680 (timer) toward threshold _DAT_80075680[2]*2, fades g_nGenericCountdown at both ends, then calls DispatchSpecialCutscene to hand off to BeginStoryIntro / BeginCreditsSequence when finished. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined GemCutscene_Update(void);

/* 0x800333dc: g_Gamestate==0xF (GS_CREDITS / returning-home) primary handler. Reached when g_nGamestate0fMode &gt;= 99. mode 99 = init: picks return-to-hub level (60 normal / 10 post-game when DAT_80075860==14000), zeros g_nReturningHomePhase, transitions mode to 100. mode 100 = tick: bumps phase, drives FUN_80015370 (level transition state machine). Modes &lt;99 (e.g. -1 from BeginCreditsSequence) dispatch into Gamestate0F_Alt — an overlay slot at 0x8007aa50 (post-.bss RAM) that the credits CD-stream populates at runtime. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined Gamestate0F_Update(undefined4 param_1, undefined4 param_2, uint param_3, undefined1 *param_4);

/* 0x800334d4: Death-plane watcher. Called only from GamestateUpdate state-0 path when g_nDeathState != 0. Increments g_nDeathFadeFrame each frame; at 16 fades to black, reloads palette via FUN_80016500, resets gameplay globals, sets g_Gamestate=0xE, advances g_nDeathTryIndex (&3). Returns 1 if death sequence fired (caller bails out of further state-0 work). */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined4 CheckDeathPlane(void);

/* 0x8003385c: Per-frame main-loop dispatcher (called from main @ 0x80012204). 16-way switch on g_Gamestate: 0 = active gameplay (heavy path: input, physics, camera, early/late hooks g_pfnGamestate0EarlyHook/LateHook, pause/inventory entry); 1 = Gamestate01_Update; 2 = PauseMenu_Update; 3 = SubMenu_Update; 4,5 = RespawnOrGameOver_Update (4 = death respawn with lives left, 5 = game over); 6 = Gamestate06_Update; 7 = (*g_pfnGamestate7Handler)() overlay hook; 8 = GemPickup_Update (dragon rescue); 9 = CameraSettle_Update; 0xA = SpyroStateDelay_Update; 0xB..0xD per-state updates (0xD mode-switched on g_nGamestate0dMode); 0xE = GemCutscene_Update; 0xF = Gamestate0F_Update/Alt switched on g_nGamestate0fMode (>=99 = returning-home/credits). Death-plane watcher CheckDeathPlane can flip to state 0xE. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined GamestateUpdate(undefined4 *param_1, int *param_2, uint param_3, int *param_4);

/* 0x80033c50: Builds the 3x3 camera view matrix from g_nCameraEulerPitch/Yaw/Roll (X * Y * Z order). Writes the full matrix to DAT_80076de4 and a Y-row-scaled-by-0x140>>9 copy to DAT_80076dd0 (the projection-ready variant). Stack matrix is reused as both accumulator and scratch. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined BuildCameraViewMatrix(void);

/* 0x80033e40: Line-of-sight segment test. Marches from param_1 toward param_2 in 0x400-length steps (segment length / 0x400 iterations) and calls FUN_8004ae38 (world collision query) at each step. Returns 1 if every step is clear, 0 on the first hit. Shared by UpdateGameplayCamera for camera occlusion tests; M07 will likely reuse for projectile and entity LOS. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined4 CheckLineOfSightSegment(int *param_1, int *param_2, undefined4 param_3, int *param_4);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined4 ComputeCameraToTargetAngle(int *param_1);

/* 0x80034198: Loads the current camera-mode's 6-int parameter block from g_pCameraTargetParams into the g_nCameraTarget* set (Yaw, Pitch, Distance, Roll, PitchBias, YawBias). The yaw is biased by -g_nCameraTargetYawBiasFromMode &amp; 0xfff so the params can be stored in world-space yaw. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined LoadCameraTargetAnglesFromMode(void);

/* 0x80034204: Polar-to-Cartesian for the camera orbit. Writes (x = D*Cos(P)*Cos(Y), y = -D*Cos(P)*Sin(Y), z = D*Sin(P)) into *param_1 using g_nCameraSpringYaw/Pitch/Distance. The result is the offset from g_pCameraAnchorPos to the eye position. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined ComputeCameraOrbitOffset(int *param_1);

/* 0x800342f8: Final blend of the spring biases into the Euler angles consumed by BuildCameraViewMatrix. EulerPitch = Pitch + SpringPitchBias; EulerRoll = SpringRoll; EulerYaw = (0x800 - SpringYawBias) - Yaw. All wrapped &amp; 0xfff. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined UpdateCameraEulerAngles(void);

/* 0x80034358: Hard-reset of the camera spring state to the current target. Clears tracking accumulators (DAT_80076e78..e98 and DAT_8007590c, DAT_800756dc), copies the g_nCameraTarget* set into g_nCameraSpring*, derives a fresh eye position via ComputeCameraOrbitOffset + g_pCameraAnchorPos, then primes the smoothed-position copy and runs one ComputeCameraToTargetAngle + UpdateCameraEulerAngles pass. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined ResetCameraStateToTarget(void);

/* 0x80034480: Per-frame gameplay camera update. Marches the smoothed camera position toward the desired eye in 0x100-sized substeps; at each substep runs CollideSphereWithWorldAndActors and slides off the surface using the contact at DAT_80076b80. On LOS-clear (g_nCameraLosClear=1) decrements g_nCameraCollisionRetries; on occlusion (=0): after 0x1e stuck frames first re-seats via the 5 mode-param blocks at &DAT_8006cab4 (g_pCameraTargetParams + ResetCameraStateToTarget), then probes g_anCameraEscapeOffsetTable (5 yaw/pitch/dist offset records at 1x, 2x, 4x scale) for a collision-free seat with two-way line of sight. Calls ComputeCameraToTargetAngle each frame to refresh g_nCameraYaw/Pitch/Distance, then runs two CheckLineOfSightSegment probes between the eye and the look-at point g_anSpyroWorldPos. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined UpdateGameplayCamera(void);

/* 0x80034c84: Teleports the smoothed camera position to the current eye (DAT_80076e04 -> DAT_80076df8), zeros the spring tracking state, raises g_nCameraSnapped + g_nCameraLosClear, and rebases the camera-to-target angles via ComputeCameraToTargetAngle. Called when a discontinuous camera cut is needed. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined SnapCameraToTarget(void);

/* 0x80034ce8: Per-frame 6-axis camera spring step. param_1 = tuning-set COLUMN (0..5) into g_anCameraSpringDriveGains (K0) / g_anCameraSpringDampingGains (K1) / g_anCameraSpringSnapThresholds: callers pass min(4, (g_nCameraStuckFrames+1)>>1) (collision-stuck stiffness escalation), 5 for scripted modes (mode 9 script-stick; mode 0xa with g_dwSpyroRequestMask & 0x1000), 0 from UpdateLedgeGrabCamera and the mode-6 settle loop. For each axis (Yaw, Pitch, Distance, Roll, PitchBias, YawBias): g_anCameraSpringDeltaState[axis] += (K0*delta - K1*accum) >> 8, snaps to 0/±0x100 below threshold (g_nCameraSnapped), then adds (accum >> 8) to the matching g_nCameraSpring* angle. Yaw uses g_nCameraManualYawInput as drive override (R1/L1 rotate); mode 6 (latched gem-grab) zeros all drives. Clamps: ±0x800 in headbash state (0xf/0xb) and passive-camera reengage; ±0x2000 with option bit 0x40 in modes 9/3. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined AdvanceCameraSpringStep(int param_1);

/* 0x800357a4: Per-frame camera-mode arbiter. Holds/clears the sentinel modes against g_dwSpyroRequestMask bits (0x200 mode 0xa transition, 0x400 mode 0xb gem-pickup path, 0x8000 mode 7 scripted target, 0x10000 mode 0xc scripted follow) and otherwise falls back to g_abSpyroStateCameraMode[g_nSpyroState]. Mode 9 (free-look, option 0x10 passive): exits to mode 0x80000010 with g_nCameraPassiveModeHoldTimer = 0x2d when L1 (held bit 0x10 in g_dwPadHeld) releases in Spyro state 0/0xd. Modes 0x10/0x11 persist while g_nCameraPassiveModeHoldTimer &gt; 0 and the state-table mode is 0/4. Passive-idle recenter: when mode 0 with passive camera and Spyro near the screen anchor (projected |x−0x100| ≤ 0x40, |y−0x78| ≤ 0x28, depth ≤ 0x1400 via ProjectWorldPointGTEUnscaled), re-enters mode 0x10; a generic mode change with passive idle instead enters mode 0x80000011 with timer 8 + g_nCameraSpringReengageCountdown 0x5a. On mode-0xb entry seeds the path index (0 forward / count−1 reverse) in g_pGemPickupCamPathData per g_nGemPickupCamPathReverseFlag. On commit re-snaps the spring state to the recomputed g_nCameraYaw/Pitch/Distance (modes 2/0xe too); entering mode 9 zeros g_anCameraScriptStickIntegrator and clears g_nCameraCollisionRetries if LOS is clear. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined SelectCameraMode(void);

/* 0x80035f58: Reads R1/L1 camera-rotate input bits at _DAT_80077380 (bits 0/1) and writes ±0x400 to g_nCameraManualYawInput — the yaw-drive override consumed by AdvanceCameraSpringStep. Suppressed when (_DAT_80078c4c & 0x1000) is set or when g_nCameraStuckFrames != 0. Called by UpdateCameraSpringForMode for modes that allow manual camera rotation; the rotation-blocked modes (0x80000009 free-look, 0x80000012 ledge, 0x8000000b gem-grab, 0x8000000e, mode 3) zero g_nCameraManualYawInput directly instead. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined UpdateCameraManualYawInput(void);

/* 0x80035fb4: Per-frame camera spring driver. Dispatches off g_nCameraCurrentMode to pick the target-params block (g_anCameraParamDefault / Mode2Front/Reverse / Mode3 / Mode4Front/Reverse / Mode5LeftBias/RightBias / Mode9ScriptStick / ModeC_ScriptedFollow / ScriptedTarget) and the anchor (g_anSpyroWorldPos for most modes; g_anCameraScriptedTargetPos from g_pScriptedCameraActor for mode 0x80000007; latched copy g_anCameraLatchedAnchorPos for mode 6). Computes the spring tuning column iVar9 = min(4, (g_nCameraStuckFrames+1)>>1), overridden to 5 for modes 9 / 0xa-with-request-0x1000, then runs AdvanceCameraSpringStep(column) x g_nFrameStep, ComputeCameraOrbitOffset, UpdateGameplayCamera + UpdateCameraEulerAngles. MODE 6 (latched gem-grab): springs only advance while sum(|g_anCameraSpringDeltaState[0..5]|) > 0x20 — the settle threshold (the old 'slti 0x21' question) — then SnapCameraToTarget; once settled the camera freezes. Mode 9 integrates the right stick / D-pad into g_anCameraScriptStickIntegrator (via g_pPadSubstepState or g_dwPadHeld) onto g_nCameraTargetYaw/Pitch and publishes counter-request g_nCameraScriptStickRequestX/Y. Passive-camera (option 0x10 / look-around) idle path: while g_nPadAllInputIdleFlag with LOS clear in mode 0, counts down g_nCameraSpringReengageCountdown (0x5a) before re-engaging the auto spring; modes 0x10/0x11 decrement g_nCameraPassiveModeHoldTimer. Look-ahead: target yaw-bias = angle(player) − angle(player + motionvec * g_bCameraLookaheadMotionScale >> 5|6) in modes 1/2/5. Ends by re-running ComputeCameraToTargetAngle to re-sync the spring state with the smoothed eye position. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined UpdateCameraSpringForMode(void);

/* 0x8003740c: Camera updater for mode 0x8000000b (gem-pickup spline path). Walks g_anCameraPos along the overlay-provided path at g_pGemPickupCamPathData: count byte [0], current index byte [1], waypoint vec3s at +8 + i*0x10. Within 0x200 of the current waypoint the index advances (g_nGemPickupCamPathReverseFlag == 0) or retreats. Speed: while an onward waypoint exists, dot(camera→player excess beyond 0x400, segment tangent normalized to 0x1000) &lt;&lt;7 &gt;&gt;12 (capped 0x80); at the path ends, distance capped by g_nGemPickupCamPathMaxStep. Step = ScaleVector3ByRatio(toWaypoint, dist, speed * g_nFrameStep); finishes with ComputeCameraToTargetAngle + UpdateCameraEulerAngles to keep the angles in sync. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined UpdateGemPickupPathCamera(void);

/* 0x80037714: Sets the active scripted-camera record (_DAT_80076ebc), snaps the smoothed eye position DAT_80076df8 to the record's stored eye at +0x18, zeros velocity at DAT_80078b4c, sets DAT_80078b54=-0x2300 (default Z), loads Roll/Pitch/Yaw from record offsets +0x24/+0x28/+0x2c into g_nCameraEulerRoll/Pitch/Yaw, then forces both camera mode slots to 0x8000000e. Called only by UpdateScriptedCameraView when selecting a new vantage. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined ActivateScriptedCameraView(int param_1);

/* 0x800377a8: Camera updater for mode 0x8000000e (scripted vantage triplet). When the level uses script-driven camera (g_nLevelMode &lt; 0), picks the next mode from &DAT_8006c588 unless the current camera state matches the override list (modes 6/0x10/0x20/0x21/0x22/0xf/0x1d). When the active scripted-camera record (_DAT_80076ebc) is exhausted — either the player has moved further than the record's max distance (field 0xd) OR the player has left the camera-screen window via ProjectWorldPointGTEUnscaled (Y &gt; 0xf0) — picks one of 3 ROM records at &DAT_8006eb24 (stride 0x38), avoiding immediate self-repeat, and calls ActivateScriptedCameraView. Refreshes camera euler angles when the record's type field at +0x30 == 1. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined UpdateScriptedCameraView(void);

/* 0x80037a20: Camera updater for mode 0x80000012 (ledge-grab freeze). Anchors on the player (&DAT_80078a58) with yaw bias from level yaw _DAT_80078b74. Picks the target-params block based on whether yaw is above/below the level yaw (DAT_8006ca3c vs DAT_8006ca6c) or, when _DAT_800758ac is set, by submode threshold (DAT_8006ca54 vs DAT_8006c964). Zeros the yaw torque, runs the spring step (axis 0) per frame-step, recomputes the eye position via ComputeCameraOrbitOffset + AddVector, then SnapCameraToTarget + UpdateCameraEulerAngles + ComputeCameraToTargetAngle. Copies the auto-aim outputs into the spring set so subsequent frames smoothly leave the freeze. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined UpdateLedgeGrabCamera(void);

/* 0x80037bd4: Top-level per-frame camera updater. Called from gameplay state Updates (CameraSettle/Gamestate01/SpyroStateDelay/GemPickup + the main GamestateUpdate). Sets _DAT_8007592c=(camera-mode-state==0x1b) and picks the next mode from the per-state byte table at &DAT_8006c588[DAT_80078ad0]. Calls SelectCameraMode to commit g_nCameraCurrentMode, then dispatches: 0x8000000e → UpdateScriptedCameraView, 0x80000012 → UpdateLedgeGrabCamera, 0x8000000b → UpdateGemPickupPathCamera, else → UpdateCameraSpringForMode. Tail handles camera shake (DAT_8007590c countdown) and the per-level-mode camera distance/LOS check (DAT_80075864 negative branch). */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined UpdateCameraFrame(void);

/* 0x80038dc0: Approaches an entity's byte yaw (+0x46) toward ArcTan2(g_nSpyroPosX/Y - entity.x/y). Camera-mode boost: _DAT_800756c4 == 3 → step *= 1.5; == 4 → step *= 2. Returns 1 once within param_3 deadzone (param_4 != 0 still applies one ApproachAngle8 step at lock-on); returns 0 while still rotating. Caller: Gamestate0B_Update (gem cutscene framing). */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined4 TrackEntityYawTowardSpyro(int param_1, uint param_2, int param_3, int param_4);

/* 0x8003a720: Zero/default initializer for an actor's render-state block (offsets +0x18..+0x57 from record base). Sets two PsyQ POLY_F3 prim-type tags (0x20) at +0x41 and +0x4b, full-bright RGB intensities (0xff) at +0x43/+0x4a/+0x52/+0x53, fade level 0x7f at +0x54, default counters 0x10 at +0x50 + 4 at +0x47 + 1 at +0x3f, and clears byte yaw/pitch/roll at +0x44..+0x46 plus all state words at +0x18/+0x1c/+0x38/+0x4c. Callers: FUN_80014564 (level loader, M09) + GemPickup_Update. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined InitActorRenderDefaults(int param_1);

/* 0x8003c6e4: Per-frame kinematic tick for a gem-pickup burst entity. Reads a velocity/rotation sub-record at *param_1: [0/1/2] = vx/vy/vz, [3] = z floor threshold, byte +0x11/12 = rotation rates, byte +0x13 = TTL counter. Per tick: pos += vel (vec3 at +0xc/+0x10/+0x14); vz -= 6 (clamped at -0x80, gravity); byte angles at +0x44/+0x45/+0x46 += rotation rates; TTL counter decremented; every other frame spawns 1 sparkle via (*_DAT_800758e4)(1, 1, &pos, &localBuf). On TTL == 0 OR pos.z &lt;= z-floor: spawns the 0x46-particle final burst (cmd 3) and calls FUN_80052568 to despawn the entity. Caller: GemPickup_Update. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined TickGemPickupBurstEntity(int *param_1);

/* 0x8003cb24: Adds param_1 to the sub-frame timer g_bSpyroSubFrameTimer. On overflow past 0xf, advances g_bSpyroAnimFrame by 1 within current anim, wrapping back to g_abSpyroAnimDescTable[anim*4] (loop start) when it reaches [anim*4+1] (loop end). Pure incremental step — does not change state. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined AdvanceSpyroAnimFrame(char param_1);

/* 0x8003cbb8: Frame stepper with state-transition commit. Like AdvanceSpyroAnimFrame but when the current anim hits its total frame count (g_abSpyroAnimDescTable[anim*4+2]), commits the pending state change and loads the new state's default anim g_abSpyroStateAnimIndexMap[g_nSpyroState]. Restart style comes from g_abSpyroStateTransitionAnimMatrix[newState + prevState*0x2d]: entry 10 = restart at the anim's loop-start frame (g_abSpyroAnimDescTable[anim*4]), else partial restart at frame 1 with sub-frame timer 4. Returns 1 if the transition committed, 0 otherwise. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined4 StepSpyroAnimAndCommitTransition(char param_1);

/* 0x8003cce4: Applies a pending Spyro state transition. Reads transition-action byte at &DAT_8006bc84[g_nSpyroState + g_nSpyroPrevState*0x2d] (case 1..0xb) and dispatches to one of 9 anim-restart strategies. Sets DAT_80078aac (anim sub-state code), DAT_80078a71/77 (current anim/frame), DAT_80078a7c (timer). Also handles case-4 break-out to a fresh anim when transitioning from state 6 with DAT_80078adc < 0x18. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined ApplySpyroStateTransition(void);

/* 0x8003d194: Per-frame Spyro animation state-machine tick. Routes on g_nSpyroAnimMode (0..10): case 0 = advance frame (no pending transition) or ApplySpyroStateTransition; cases 1/2/6 = advance sub-frame timer by 2; cases 3/10 = stepped transition — only when g_abSpyroStateTransitionAnimMatrix[g_nSpyroState + g_nSpyroPrevState*0x2d] is 3/4/10, calls StepSpyroAnimAndCommitTransition at the previous anim's play rate (plays the landing SFX when prev anim was 0x1a); case 4 = wait for sub-frame overflow then enter mode 3; cases 5/8 = advance with a specific increment then drop back to mode 0. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined TickSpyroAnimStateMachine(void);

/* 0x8003d3b8: Reads the current pad (via _DAT_800757e0) and converts directional input into a movement target. Analog stick (offsets +0x16 +0x17 centered at 0x7f) maps to ArcTan2 → target yaw DAT_80078b24 + magnitude-scaled speed DAT_80078b20. D-pad (button bits in +4 nibble 0xf000) maps via the 16-entry yaw table at &DAT_8006c5d0 (stride 2). Both paths combine with g_nCameraEulerYaw so movement is camera-relative. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined ComputeSpyroMoveTargetFromPad(int param_1);

/* 0x8003d52c: Applies sin/cos integration of body-frame angular velocity (param_1) to update world-frame Euler angles. Reads current pitch (DAT_80078b7c) and yaw (DAT_80078b78) via the LUTs, then increments yaw (DAT_80078b78), roll (DAT_80078b74), and pitch (DAT_80078b7c) by the cos/sin-projected components. Also updates the byte snapshots DAT_80078a64/65/66 (12-bit angles &gt;&gt; 4). The cos(pitch) denominator divisor models a pitch-coupled yaw scaling. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined IntegrateSpyroBodyEuler(int param_1);

/* 0x8003d6d0: Steers Spyro's body yaw (DAT_80078b74) toward the move target yaw (DAT_80078b24) using a lean accumulator (DAT_80078ba0, range -6..+6). Picks turn rate from the 2D lookup table at &DAT_8006c5fc + param_1 * 0x1a + lean * 2 (param_1 selects speed bucket). Resets lean when target is overshot or the desired delta falls inside the table-indexed band. Always finishes by calling IntegrateSpyroBodyEuler with the chosen angular delta and reports the new |delta| via AbsAngleDelta12. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined TurnSpyroTowardMoveTargetWithLean(int param_1);

/* 0x8003d840: When the analog stick is past the inner deadzone (button bit and not centered at 0x7f7f) but its magnitude is &lt; 0x60, smooths Spyro's body yaw (DAT_80078b74) partway toward the move target yaw (DAT_80078b24) proportional to stick magnitude. Large delta (&gt; 0x100) sets the snap-flag DAT_80078ad4 = 1. Always follows with TurnSpyroTowardMoveTargetWithLean(0). */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined ApplyPadInputToSpyroBodyYaw(void);

/* 0x8003d92c: Accelerate/decelerate current speed (DAT_80078b70) toward the target speed (DAT_80078b20) using two separate ramps: +param_1 per frame when below target, -param_2 per frame when at-or-above. Clamps to the target on overshoot. The split rates let walking states use a softer brake than throttle. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined AdvanceSpyroSpeedTowardTarget(int param_1, int param_2);

/* 0x8003d978: Compose Spyro's linear velocity vector (DAT_80078b4c..b54) by rotating (speed,0,0) through the body Euler matrix. Builds the matrix into stack via ApplyEulerRotation from the byte snapshots of pitch/roll/yaw (DAT_80078b7c/74/78 &gt;&gt; 4), then RotateVectorByMatrix expands the speed (DAT_80078b70) along the body forward axis. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined BuildSpyroVelocityFromBodyEuler(void);

/* 0x8003da08: Free-fall body attitude damper. Spring-damps pitch (DAT_80078b7c) and yaw (DAT_80078b78) back toward 0 via the angular accumulators DAT_80078cb0 / DAT_80078cb4 with PD coefficients (4/16 - 16/64). No target-velocity tracking — used when Spyro is too high above ground (DAT_80078b08 ≥ 0x17) for UpdateSpyroFlightAttitudeNearGround. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined DampenSpyroFreeFallAttitude(void);

/* 0x8003dae4: Per-frame attitude controller for glide/flight when Spyro is close to ground (DAT_80078b08 &lt; 0x17). Spring-damps body pitch (DAT_80078b7c) and yaw (DAT_80078b78) toward velocity-relative angles derived from the local ground frame and body yaw, using the angular accumulators DAT_80078cb0/cb4. When both byte snapshots DAT_80078a64/65 are tilted past 0xc0+0x20, layers an additional position offset on DAT_80078a58 (Spyro pos) for the ground-hug effect. Falls through to DampenSpyroFreeFallAttitude when above the 0x17 altitude threshold. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined UpdateSpyroFlightAttitudeNearGround(void);

/* 0x8003de44: Spring-aligns Spyro's body pitch (DAT_80078b7c) and yaw (DAT_80078b78) to the current velocity (DAT_80078b4c..b54). Computes target pitch as -ArcTan2(|XY|, Z) and target yaw as ArcTan2(vx,vy)+0x8e, drives DAT_80078cb0/cb4 angular accumulators with PD coefficients (4/16 - 16/64) and integrates the result. Used by free-flight states where the body should "face the way it's moving". */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined AlignSpyroBodyToVelocity(void);

/* 0x8003dfa4: Hard-zero of Spyro's linear motion. Clears the knockback accumulator DAT_80078b64..b6c, the frame-delta length DAT_80078b80, the live velocity DAT_80078b4c..b54, and the speed scalar DAT_80078b70. Called by jump/glide/dive transitions (FUN_80040f68/41270/41670) just before they overwrite velocity with their own launch. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined ResetSpyroLinearMotion(void);

/* 0x8003dfec: Rotates Spyro's body yaw (DAT_80078a66 byte / DAT_80078b74 word) by ±param_1 toward the knockback direction ArcTan2(DAT_80078b64, DAT_80078b68). The 8-bit angle delta must lie outside the ±param_2 dead-band; if it also escapes ±param_1, the body snaps directly to the target rather than stepping. Used by FUN_80043fe4 (on-ground update) to recover orientation after impacts. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined RotateSpyroBodyTowardKnockbackDir(int param_1, int param_2);

/* 0x8003e0b4: Decomposes gravity along the slope tangent of the current surface. Computes the component of gravity (DAT_80078ba4) projected onto the ground plane defined by normal DAT_80078afc and writes the down-slope acceleration into DAT_80078b28 (with DAT_80078b30 = gravity magnitude). Called by ChangeSpyroState cases 0xb (charge), 0x13, 0x2c to give Spyro a slope-slide push along the ground tangent. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined ApplyGravityAlongSpyroSlopeTangent(void);

/* 0x8003e1ac: Sets DAT_80078b34 to gravity-magnitude pulling Spyro along the negated ground normal. Copies DAT_80078afc (surface normal) into DAT_80078b34, scales length to |DAT_80078ba4| (gravity), then negates. Combines with ApplyGravityAlongSpyroSlopeTangent to fully decompose gravity onto the slope frame. Called widely from ChangeSpyroState (cases 1/2/3/7/0xe/0x15/0x16/0x1a/0x1c/0x1e/0x1f/0x2c) for slope-aware launches. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined ApplyGravityAlongSpyroSlopeNormal(undefined4 param_1, undefined4 param_2, int param_3, int param_4);

/* 0x8003e218: Cliff-detection probe: fires a LOS (FUN_8004ae38) from a point ahead-and-down of Spyro (body-frame offset 0x1c4, 0, -0x164) toward a point further-down (GTE-rotated 0, 0, -0x1a4) relative to DAT_80078a58. If the probe hits ground and the slope angle is steep (&gt; 0x16 in 8-bit angle units), latches DAT_80078b0c = 1 (ledge-detected flag) and snapshots the hit normal into DAT_80078b10..b18. Used by ground-state to trigger cliff-aware behavior. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined ProbeSpyroLedgeForward(void);

/* 0x8003e318: Per-frame Spyro grounding probe (skipped when DAT_80078c4c bit 0x4000 is set). Fires LOS from a body-frame point above Spyro to a point below; on hit, captures the surface normal into DAT_80078afc..b04, computes the slope angle into DAT_80078b08, stores the world-chunk id into DAT_80078ccc, and loads chunk vertices into DAT_80078cd0+ via FUN_80017cb8. Slope &lt; 0x21 marks footing (DAT_80078ba8 = walk/run flag, DAT_80078af4 = 0 contact frame counter); steeper triggers the fallback inner downward probe (vertical line to z=-0x3c). Updates DAT_80078b1c if a secondary forward-down LOS fails (cliff-edge hint). */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined ProbeSpyroGroundContact(void);

/* 0x8003e628: When standing on a known world chunk (DAT_80078ccc &gt;= 0), averages the per-vertex displacement of the chunk's first 3 vertices vs. their cached values at DAT_80078cd0+ (loaded by ProbeSpyroGroundContact). Stores the mean delta into DAT_80078b84 — Spyro's "moving-platform" inherited velocity, applied by the per-frame physics integrator. Significant motion (length &gt; 0x20) sets DAT_80076ec0 = 1 to wake camera tracking. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined AverageSpyroPlatformMotion(void);

/* 0x8003e758: Sub-frame footing recovery: fires LOS (FUN_8004ae38) from a body-frame point (X+0x80) to a point (Z-0x164, Z-0x80) and, on a hit with positive height delta, subtracts the world-collision delta from the hit position (relative to DAT_80076b80 GTE reference) and applies the small displacement (per-component cleared if abs &lt; 8) to DAT_80078a58 (Spyro pos). Clears the off-ground counter DAT_80078af4 on success, otherwise increments it. Acts as the per-frame "snap-to-floor" step. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined StepSpyroOntoLocalFloor(void);

/* 0x8003e90c: Cliff-edge / wall-clear nudge: fires 4 LOS probes (FUN_8004ae38) around Spyro using delta vectors at &DAT_8006ccb8/cbf8 stride 0x40, builds a 4-bit hit mask, then looks up a clear-direction index in the 16-entry table at &DAT_8006c714. If index ≥ 0, applies the corresponding (X,Z) nudge from &DAT_8006cc78/cbf8 (>>4 scaled) to velocity DAT_80078b4c/b50. Called by ChangeSpyroState cases 6 (cliff-fall) and 0x10 (break-out) when DAT_80078b08 &lt; 0x20 — pushes Spyro horizontally clear of geometry. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined NudgeSpyroFromWallProbes(void);

/* 0x8003ea68: Spyro state-change entry point: per-new-state initialization switch (param_1 = new state ID 0..0x2c). Exit-side preamble: leaving certain states stops/plays exit SFX from g_pLevelSampleBankHeader bytes. Per-state setup writes the movement core (g_nSpyroStateFlags, g_nSpyroGravityStep/EnableFlag, velocity/carry vectors, g_nSpyroFloorReferenceZ) — notably: ground states 1/2/3/0x15 compute g_nSpyroMoveTargetMag from g_anSpyroSlopeSpeedTargets interpolated by ground slope; state 0xb (charge) scans the actor list for the nearest chargeable target in a 0x200-yaw cone; state 0 and the idle-fidget states (0x12, 0x24..0x2b) seed g_nSpyroIdleAnimTimeout. Tail commits g_nSpyroState/g_nSpyroPrevStateTimer, zeroes g_nSpyroStateTimer, and loads g_nSpyroAnimPlayRate from g_abSpyroAnimDescTable via g_abSpyroStateAnimIndexMap. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined ChangeSpyroState(undefined4 *param_1, undefined4 *param_2, int param_3, undefined1 *param_4);

/* 0x8003fdc8: Wrapper: calls ChangeSpyroState(param_1..4), then hard-resets the anim state machine — sets DAT_80078a71 (current anim) to &DAT_8006c470[g_nSpyroState] (the state's default anim), frame=1, timer=0, sub-state DAT_80078aac=0, and collapses DAT_80078ab4 (prev-state ctx) to g_nSpyroState. Used by callers that need a clean anim restart vs ChangeSpyroState's preserved blending. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined RestartSpyroAnimWithState(undefined4 *param_1, undefined4 *param_2, int param_3, undefined1 *param_4);

/* 0x8003fe40: Per-frame Spyro physics integrator + collision march. Consumes the request bitmask DAT_80078c4c (bit0/1/2 set linear velocity from absolute targets DAT_80078c50..58; bit3/4/5 set angles 0xc..0xe from DAT_80078c5c..5e; bit6 selects shifted-value mode; bit8 forces ChangeSpyroState to DAT_80078c70; bit0x200..0x10000 set g_nCameraNextMode/anchor/target-params; bit0x1000 = enemy-locked turning toward DAT_80078c7c+0x46). Divides velocity by g_nFrameStep to per-frame delta, then substep-marches in 0x154-unit segments calling FUN_8004e3c8 (collision detect) + FUN_8004be4c (slide) up to 6 retries per substep. Maintains DAT_80078ae4 prev-position + DAT_80078b80 distance traveled. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined4 AdvanceSpyroPhysics(void);

/* 0x80040f68: Spyro pad-input → state-change dispatcher. Reads latched pad-transition mask DAT_80078a84 AND'd with mask param_1 (0xfff9/0xfbf9/0x8400). Bits: 0x10=jump, 0x20=headbutt(7), 0x40=swim/launch(0x1b), 0x80=alt-attack(0x1c), 0x100=glide-entry(0xe/0x16), 0x400=swim-enter(0x1d), 0x800=flame-hold(state 0x11), 0x2000=charge-hold(DAT_80078ca8), 0x8000=kick-velocity(state 0xc). Returns 1 if a state change occurred. Called first by every per-state Update in UpdateSpyroStateBehavior to bail when input triggered a transition. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined4 DispatchSpyroPadStateChanges(uint param_1, undefined4 *param_2, int param_3);

/* 0x80041270: Charge-specific pad→state dispatcher (called from state 0xb=charge and 0x14=flame). Narrower mask, only bits 0x6/0x10/0x20/0x40/0x80/0x100/0x400/0x800/0x2000/0x8000. When two-button combo (uVar2 == 2) AND direction matches target via ArcTan2(target_pos - DAT_80078a58)→body yaw within ±0x1ff, kicks state 0xc. Otherwise routes jump/glide/headbutt/flame/swim transitions same as DispatchSpyroPadStateChanges but with charge-state quirks (no early-out, all transitions run; sets DAT_80078ca8 charge-hold flag). */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined4 DispatchSpyroChargeStateChanges(undefined4 param_1, undefined4 *param_2, undefined4 param_3, undefined1 *param_4);

/* 0x80041558: Idle-animation cycler: advances g_nSpyroIdleAnimSeqCursor (wrap 9) through g_anSpyroIdleAnimStateSeq looking for the next idle-fidget state whose anim record has a nonzero mesh callback (state -> g_abSpyroStateAnimIndexMap -> g_apActorMeshTable[+0x38]). Calls ChangeSpyroState to that state ID. If a full lap finds none, sets g_nSpyroIdleAnimTimeout = 10000 (re-idle later). Triggered from state 0 (Idle) of UpdateSpyroStateBehavior when the state timer exceeds g_nSpyroIdleAnimTimeout. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined CycleSpyroIdleAnim(void);

/* 0x80041670: Per-frame Spyro state Update dispatcher: massive switch(g_nSpyroState) 0..0x2c. Every case starts with DispatchSpyroPadStateChanges() (bails if input changed state), then runs state-specific decision tree picking the next state ID puVar5, then ChangeSpyroState(puVar5). Drives walk(0)/walk-slow(1)/run(2)/skid(3)/skid-stop(4)/fall(5)/hard-fall(6)/respawn-wait(7)/glide(0xe)/swim(0x1d)/jump-cluster(0xf/0x17/0x20-22)/charge(0xb)/flame(0x14)/death(0x1e-0x1f) etc. Called by TickSpyroGameplayFrame (the main gameplay tick). */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined UpdateSpyroStateBehavior(undefined4 param_1, undefined4 *param_2, int param_3, undefined1 *param_4);

/* 0x80043fe4: Per-substep Spyro motion integrator. Called _g_nFrameStep times per frame from AdvanceSpyroFrameMotion / TickSpyroFrameForceJump / TickSpyroFrameNoInput; param_1 = substep index. Sets _DAT_800757e0 = per-substep pad-input record (&DAT_800773bc + idx*0x18). switch(g_nSpyroState) drives per-state motion routines: walk/run cluster → ComputeSpyroMoveTargetFromPad + ApplyPadInputToSpyroBodyYaw + TurnSpyroTowardMoveTargetWithLean + AdvanceSpyroSpeedTowardTarget + BuildSpyroVelocityFromBodyEuler; flame/glide/jump → IntegrateSpyroBodyEuler + BuildSpyroVelocityFromBodyEuler; charge inner switch on DAT_80078ad4 (charge-substate). Accumulates DAT_80078b4c (velocity this substep) into DAT_80078b58 (frame motion accumulator). Updates byte Euler snapshots at DAT_80078a64/65/66. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined IntegrateSpyroMotionForSubstep(int param_1, undefined4 *param_2, int param_3, int *param_4);

/* 0x80047b60: Per-substep Spyro physics dispatcher — runs the correct ground-probe/gravity/collision pipeline for the current state. switch(g_nSpyroState) routes to a combination of: AverageSpyroPlatformMotion (moving-platform inheritance for grounded states 0/0xc/0xd/0x12/0x23-0x2b/1-3/0x15/4/8), AdvanceSpyroPhysics (collision march, near-universal), ProbeSpyroGroundContact (ground/normal/slope read), ApplyGravityAlongSpyroSlopeNormal+Tangent (slope-frame gravity decomposition), StepSpyroOntoLocalFloor (snap-to-ground for states 0/0xb/0x1d/8), ProbeSpyroLedgeForward (cliff-edge stop), CollideSphereWithWorldAndActors (fall-into-pit detect for states 0x20-0x22). State-specific bookkeeping: walk/run slope-friction _DAT_80078b9c (states 1-3/0x15), respawn-blink alpha DAT_80078a80..83 (state 7), fall-height tracking _DAT_80078bb0 + _DAT_800758c0 fall-damage compare (states 0xf/0x17/0x20-22/6/0x10), swim breath g_nSpyroBreathTimer (state 0x1d, with drown detection), charge-substate kickup splash (state 0x2c → particle-fx callback _DAT_800758e4). Called per-substep from AdvanceSpyroFrameMotion (the first loop, before AdvanceSpyroSubstepState). */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined DispatchSpyroPhysicsByState(undefined4 param_1, undefined4 param_2, int param_3, int *param_4);

/* 0x8004888c: Per-substep Spyro state advance (attitude + state-timer). switch(g_nSpyroState) dispatches the attitude controller for this substep: UpdateSpyroFlightAttitudeNearGround for most ground/glide states, DampenSpyroFreeFallAttitude for state 6 (hard-fall), AlignSpyroBodyToVelocity for states 0x14/0x18 (flame/charge variants). Also state-specific updates to DAT_80078ab0 (anim play-rate scaled from current speed _DAT_80078b70) for states 1/5/0x13/0x15, and the state 0x14 charge-snap into camera mode 0x80000000 when _DAT_80075914 & 0x10. Always ends with g_nSpyroStateTimer++. Called per-substep from AdvanceSpyroFrameMotion (the second loop, after DispatchSpyroPhysicsByState). */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined AdvanceSpyroSubstepState(void);

/* 0x80048b9c: Per-frame Spyro physics + footstep audio driver. Zeros DAT_80078b58 motion accumulator, loops _g_nFrameStep times calling IntegrateSpyroMotionForSubstep(substep_idx), then FUN_80047b60 (footing-state — includes AdvanceSpyroPhysics collision march), then loops _g_nFrameStep times calling FUN_8004888c (per-substep finalize). Computes _DAT_80078af8 = local ground height via FUN_8004d5ec, and triggers footstep SFX via FUN_80056f64 gated on _DAT_80075718 step counter + _DAT_80078bc4 surface-type. Skips footstep audio if DAT_80078c4c bit 0x4000 set. Called only by TickSpyroGameplayFrame. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined AdvanceSpyroFrameMotion(undefined4 param_1, undefined4 *param_2, int param_3, int *param_4);

/* 0x80048d10: Per-tick Spyro attack-effects driver — two independent subsystems sharing the function. (1) Flame breath: while g_nSpyroFlameBreathTimer > 0 spawns random-offset particle (1,0x1e) behind Spyro via g_pfnLevelOverlayParticleSpawn, sets g_abSpyroFxParticleRgba trail (0x90,0x20,0x10) + sinusoidal alpha pulse; timer decrements by g_nFrameStep. (2) Horn-strike sweep: when g_bSpyroHornCollisionActive, arms trail slots through the g_abSpyroHornStrikeSlotMap permutation, recomputes g_anSpyroHornStrikeAnchorPos (static or body-frame per g_bSpyroHornStrikeAnchorMode, matrix copied to g_anSpyroHornStrikeAnchorMtx), then per slot walks the arc point at its age: DispatchActorContactAtSphere(radius 0xa0) + CastRayWorldAndActors from g_anSpyroHornStrikeSlotPrevPos; on hit freezes the slot (state 2, clip age saved) + spawns a 3-particle burst + kick SFX from world track 4; on arc-end without hit spawns the g_anSpyroHornStrikeEndBurstDirs burst. param_1 = frame delta. Called from TickSpyroGameplayFrame and Gamestate0B_Update. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined TickSpyroAttackEffects(int param_1);

/* 0x800495d8: Anim-layer-1 sub-frame advance: DAT_80078a7d += param_1; on overflow >= 0x10 wraps and advances DAT_80078a79 (frame) by 1, snapshots DAT_80078a73 → DAT_80078a72 and DAT_80078a79 → DAT_80078a78. Caps frame at &DAT_8006c4a2[DAT_80078a73 * 4] (per-anim length). Layer-1 byte set is the horn-strike anim overlay (vs layer-0 g_bSpyro* primary body anim). */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined AdvanceSpyroAnimLayer1Frame(char param_1);

/* 0x80049660: Per-tick anim-layer-1 state machine — tracks horn-strike anim overlay. _DAT_80078ab8 = layer mode (0=play, 1=mid-blend +2/tick, 2=hard-switch +4/tick). When mode=0 and _DAT_80078bf0 (horn-strike state) matches _DAT_80078ac0 (last seen): if bf0=0 snapshots primary g_bSpyro* into layer-1 byte set (a72/73/78/79/7d), else AdvanceSpyroAnimLayer1Frame(_DAT_80078abc rate). On state mismatch reads &DAT_80075264[bf0+ac0*2] transition byte (1=blend, 2=hard) and starts that mode. Modes 1/2 finish when DAT_80078a7d > 0xf → AdvanceSpyroAnimLayer1Frame(0) + mode=0. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined TickSpyroAnimLayer1(void);

/* 0x80049880: Per-tick PD spring of byte-Euler snapshot. Drives DAT_80078a68/69/6a (smoothed byte Euler) toward targets DAT_80078bfc/c00/c04 via accumulators DAT_80078c14/18/1c using shortest-path 12-bit angle delta (uVar2 - prev & 0xfff, > 0x800 wrapping). Spring law: accum += (delta<<7 - accum<<4) >> 6 ; prev += accum >> 6. Called every tick by TickSpyroHornStrikeAttack. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined SmoothSpyroByteEulerSpring(void);

/* 0x800499c0: Per-tick horn-strike (headbutt, pad button 0x20) attack state machine. Driven by _DAT_80078bf0 (state 0=idle / 1=swing-active). If state's default-anim has &DAT_8006c558[anim] != 0 (anim suppresses attack): force-cancel + ZeroVector(DAT_80078bfc target Euler). State 0 + pad 0x20 + DAT_80078760 idle → start swing: bf0=1, DAT_80078ab8=0, DAT_80078abc=&DAT_8006c4a3[DAT_80075269*4] anim rate, DAT_80078760=1 horn-collision-active, DAT_80078761=0 horn-segment-count, DAT_80078763=random{0,1} swing direction, _DAT_80078764=(flame timer active), zeros &DAT_800786e8 horn-segment ring. State 1 timer DAT_80078bf8: phase 0xc..0x1d (every 4 frames, sky lit) spawns 2 horn-tip particle pairs at &DAT_8006e238/244 offsets; pad held + timer > 0x2c → restart swing into combo mode (DAT_80078a7d=4, DAT_80078a79=0); timer > 0x2f → end. Always ends with SmoothSpyroByteEulerSpring(). */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined TickSpyroHornStrikeAttack(void);

/* 0x80049dfc: Anim-layer-2 sub-frame advance: DAT_80078a7e += param_1; on overflow >= 0x10 wraps and advances DAT_80078a7b (frame), snapshots DAT_80078a75 → DAT_80078a74 and DAT_80078a7b → DAT_80078a7a. Wraps to &DAT_8006c4a0[anim*4] (start frame) when frame hits &DAT_8006c4a1[anim*4] (end+1). Layer-2 byte set at DAT_80078a74/75/7a/7b/7e. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined AdvanceSpyroAnimLayer2Frame(char param_1);

/* 0x80049e8c: Per-tick anim-layer-2 state machine (analogous to TickSpyroAnimLayer1 but for the second overlay). Only ticks when _DAT_80078ac4 == 0 (layer-2 enabled) AND _DAT_80078c40 == _DAT_80078acc (state stable). If _DAT_80078c40 == 0 snapshots primary g_bSpyro* into layer-2 byte set (a74/75/7a/7b/7e); else AdvanceSpyroAnimLayer2Frame(_DAT_80078ac8 rate). _DAT_80078c40 is forcibly zeroed by TickSpyroAnimLayer2GateTimer when primary anims block layer 2. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined TickSpyroAnimLayer2(void);

/* 0x80049f3c: Layer-2 gate timer. If &DAT_8006c558[g_bSpyroAnimPrev] == 0 AND &DAT_8006c558[g_bSpyroAnimCurrent] == 0 (primary anims permit layer-2 overlay): _DAT_80078c44++ (frame counter of permissive duration). Else: _DAT_80078c40 = 0 (force-cancel layer-2 active anim). &DAT_8006c558 is the per-anim layer-2-blockage flag table — non-zero means the primary anim takes over the bodypart. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined TickSpyroAnimLayer2GateTimer(void);

/* 0x80049fac: Per-tick Spyro shadow-ring sampler. Computes the shadow center (local (0,0,-0x164) through g_anSpyroBodyMtx + g_anSpyroWorldPos) into g_anSpyroShadowPos with Z = g_nSpyroGroundHeightZ. Refreshes g_bSpyroOtBinBias (5 while < 0x7f), g_nSpyroShadowOtBinBias (3, or 5 when Spyro > 0x364 above ground) and g_nSpyroShadowHiddenFlag (ground <= g_anLevelDeathPlaneZTable[g_nLevelIntroIndex] or trigger event active). Then advances g_nSpyroShadowRingIndex (&7) and samples ONE probe vertex per tick: g_anSpyroShadowProbeOffsetTable entry through the body matrix, +0x200 Z headroom, FindGroundHeightBelow(pt, 0x400); ground within 0x100 of expected writes g_abSpyroShadowRingHeights[idx] = (shadowZ - ground)*0xc2>>9, else sets g_abSpyroShadowRingNoGroundFlags[idx]. param_1 == 0 skips probing and clears both slots. Output consumed by DrawSpyroDropShadow. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined SampleSpyroShadowRingHeights(int param_1);

/* 0x8004a200: Main per-frame Spyro tick — gameplay entry. Called from GamestateUpdate, CameraSettle_Update, GemPickup_Update. Order: (1) handle DAT_80078c4c bits 0x2000 (pause input) + 0x100 (suppress state-machine), (2) AdvanceSpyroFrameMotion (physics + footing), (3) UpdateSpyroStateBehavior (state transitions) unless suppressed, (4) per-substep TickSpyroAnimStateMachine + 4 actor ticks FUN_800499c0/9660/9f3c/9e8c, (5) build body rotation matrices DAT_80078a8c + DAT_80078c20, (6) FUN_80049fac/8004d8d10 actor-pose finalize, (7) clears DAT_80078a84 pad latch, (8) death/respawn arbiter: out-of-bounds → ChangeSpyroState 6/cam scripted-view → TriggerRespawnOrGameOver, (9) per-frame footstep SFX from DAT_80078a76/78 anim frames. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined TickSpyroGameplayFrame(undefined4 *param_1, undefined4 *param_2, int param_3, int *param_4);

/* 0x8004a7ec: Spyro frame tick — forces state 0xf (jump). Called from Gamestate01_Update (post-level-load intro). Syncs DAT_800776d8 ↔ g_dwPadButtons, forces ChangeSpyroState(0xf) if not already there, sets DAT_80078ad4=8 (disable charge), then per-substep IntegrateSpyroMotionForSubstep → RShift+Add to position DAT_80078a58 → FUN_8004888c → TickSpyroAnimStateMachine + 4 actor ticks. Builds Euler rotation matrices. Skips footing probe and ground-snap collision (no FUN_80047b60/d5ec/56f64 calls) — Spyro just flies wherever scripting puts him. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined TickSpyroFrameForceJump(undefined4 param_1, undefined4 param_2, int param_3, int *param_4);

/* 0x8004aa0c: Spyro frame tick — no player input, no state-machine. Called from SpyroStateDelay_Update. Same structure as TickSpyroFrameForceJump but does NOT force a state (only sets DAT_80078ad4=8 if already jumping). Per-substep IntegrateSpyroMotionForSubstep + position-step + FUN_8004888c + TickSpyroAnimStateMachine + actor ticks. Used during scripted post-state delays where Spyro coasts visually without responding to pad or transitions. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined TickSpyroFrameNoInput(undefined4 param_1, undefined4 param_2, int param_3, int *param_4);

/* 0x8004ac24: Reset Spyro entity state. State 7 → clear high byte of DAT_80078a80. States 0xb/0xf/0x20/0x2c → FUN_800562a4(audio reset). param_1==0 = soft reset (clear input/charge flags DAT_80078a80/84/88/ca4/ca8/cac/bc0 + rebuild DAT_80078b7c/78/74 from byte Euler snapshots). param_1!=0 = hard reset: save pos+euler+_g_nLevelReadyFlag, FillWord(DAT_80078a58, 0, 0x2a4) wiping the whole Spyro state block, restore saved fields, ChangeSpyroState(0). Always sets _DAT_80078bc4=0xff (sentinel surface-type) and _DAT_80078768=0. Called from BeginCreditsSequence/BeginPauseToMenuTransition/EndGemPickupToWorld/EndSaveMenuToWorld/LevelIntro_Update/Gamestate0D_Update/HandleLevelSelectInput/GemPickup_Update + level loader FUN_8001364c. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined ResetSpyroEntity(int param_1, undefined4 param_2, undefined4 param_3, undefined1 *param_4);

/* 0x8004ae38: Ray cast against world geometry + dynamic actor meshes. Args: ray-start (vec3), ray-end (vec3), unused, scratch buffer. Walks the 4-cell actor grid at _DAT_80075778+0x1000 (testing each actor's local-frame mesh via the per-actor-mesh table at &DAT_80076378[idx]+0x38) and the world-chunk index at _DAT_800785d4. On hit writes: _DAT_80076b80..b88 = hit point, _DAT_80077368..70 = hit normal (or surface vector), _DAT_800757e4 = hit actor ptr (NULL = world hit), _DAT_80075808 = hit triangle index (-1 = actor hit). Returns 1 on hit, 0 otherwise. Shared by every M05 line probe (LedgeForward / GroundContact / OntoLocalFloor / WallProbes) and M06 CheckLineOfSightSegment. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined8 CastRayWorldAndActors(uint *param_1, uint *param_2, undefined4 param_3, int *param_4);

/* 0x8004be4c: Sphere-vs-world+actors collision with slide-out resolution. Args: sphere center, radius, max-slide-distance, scratch. Same 4-cell actor grid + world-chunk-index iteration as CastRayWorldAndActors but tests sphere against each triangle: AABB reject, signed dot-product front-face test, then ray-triangle barycentric for closest point. On contact, computes penetration via g_anSqrtLUT iterative refinement and writes pushed-out sphere center to _DAT_80076b80..b88 + contact point to _DAT_80077858..60 + normal to _DAT_80077368..70 + hit actor/triangle. Returns 1 on hit. Callers: AdvanceSpyroPhysics (per-substep collision march, up to 6 retries), DispatchSpyroPhysicsByState (state 0x20-0x22 fall-into-pit detect), UpdateGameplayCamera (camera-vs-world collision). */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined4 CollideSphereWithWorldAndActors(uint *param_1, uint param_2, uint param_3, int *param_4);

/* 0x8004d5ec: Finds the highest world+actor triangle below a point within search distance. Args: position (vec3), max-search-down. Iterates 4-cell actor grid + world chunk index, testing each candidate triangle for XY-containment and reading its Y at the projected point. Tracks highest hit so far in DAT_1f800020. On result writes _DAT_80076b80..b88 (hit point), _DAT_80077368..70 (normal), _DAT_800757e4 (actor or NULL=world), _DAT_80075808 (triangle), _DAT_80075718 (per-triangle surface-type byte). Return value (via DAT_80076b88 + v0) is the ground Y the caller uses for foot height. Callers: AdvanceSpyroFrameMotion (per-frame foot probe via _DAT_80078af8), DispatchSpyroPhysicsByState (swim/state-0xf air probes), IntegrateSpyroMotionForSubstep, Gamestate0B_Update, level-loader FUN_8001364c. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined FindGroundHeightBelow(uint *param_1, int param_2);

/* 0x8004df24: FindWorldFloorBelowPoint — lightweight world-only ground-height probe at a point. Differs from FindGroundHeightBelow (8004d5ec): - World chunks only (no actor grid scan). - First-hit early-exit: scans Z-chunks downward one at a time from the input Z chunk; stops as soon as any world triangle satisfying (X,Y)-containment + Y &lt; input.Y is found. - Writes only to _DAT_80075844 (per-tri surface-type byte); does NOT touch the shared hit-point / hit-normal globals. - Assumes ground Y > 0 — sentinel iVar19==0 doubles as "no hit yet". Returns floor Y (in world units, &lt;&lt; 4 of grid units), or 0 + _DAT_80075844 = -1 on no-hit. Callers: - UpdateCameraFrame (camera-vs-floor — what's the world geometry directly under the camera target). - GemPickup_Update (snap gem-display pose to local floor). World data layout: same as the other M07 probes — chunk header at _DAT_800785d4: +0x8 = X-axis ushort index, +0xc = Y/Z combined ushort index, +0x10 = triangle records (stride 0xc, 14-bit grid-quantized vertices), +0x14 = per-triangle surface-type byte array. */
int FindWorldFloorBelowPoint(uint *param_1);

/* 0x8004e3c8: Actor-only sphere overlap dispatcher (no world). Args: sphere center, radius, ?, ?, exclude-actor-ptr. Walks the 4-cell actor grid at _DAT_80075778+0x1000 (no world chunk iteration). For each actor whose bounding sphere overlaps, dispatches via jumptable &UNK_8004e5b8 indexed by the actor's type byte at puVar1+2 — runs the per-actor-type contact handler (damage application, pickup trigger, push/bounce, etc). Callers: AdvanceSpyroPhysics (per-substep actor-interaction sweep) and FUN_80048d10 (post-tick actor pose finalize). */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined4 DispatchActorContactAtSphere(uint *param_1, int param_2, undefined4 param_3, undefined4 param_4, int param_5);

/* 0x8004eba8: Renders the per-level static-actor mesh list. Stage 1: Z-cull each mesh chunk via gte_rtps_b, building visible-chunk pointer list at &DAT_8006fcf4. Stage 2: project triangle verts through GTE, build 5-bit frustum outcodes, emit POLY_F3 (0x84) / POLY_F4 (0x86) flat prims into the OT bump buffer at _DAT_800757b0. param_1 = actor mesh index into _DAT_80078a4c table (or -1 to walk the flat list at _DAT_80078a44..+_DAT_80078a40*4). param_2/param_3 = camera rot/translate matrices. Called once at the end of DrawActors, and by Gamestate01_09/04_05/GemCutscene_Draw. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined8 EmitStaticActorMeshList(int param_1, undefined4 *param_2, undefined4 *param_3);

/* 0x8004f000: Fog-cued sibling of EmitStaticActorMeshList. Same two-stage pattern but uses pre-loaded global view matrix (&DAT_80076de4..df4) + model matrix (&DAT_80076dd0..de0), and applies per-vertex depth cueing via gte_dpcs_b using _DAT_800757d4 fog RGB and _g_nFadeProgress. Cued colors are written to scratch buffer &DAT_800724f4 and fed back into the F3/F4 emit loop. Callers: Gamestate0A_Draw (world hub) + Gamestate0C_Draw (post-level treasure summary). */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined8 EmitStaticActorMeshListFogged(void);

/* 0x8004f4bc: Per-actor textured mesh emitter for close-range dynamic actors. Stage 1: Z-cull each mesh chunk via gte_rtps_b. Stage 2: project each triangle, build 5-bit outcode against the custom scissor rect at _DAT_8007aa00..0c (set by DrawActors per-actor from screen-space vert bounds), dispatch to ClipAndEmitActorTriangle. Final drain of the deferred-prim stack at _DAT_80075798/&DAT_800704f4. param_1 = actor mesh struct (size+vertList). param_2/param_3 = view/model matrices. Caller: DrawActors close-no-fog path (depth < 0x3001). */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined2 *EmitDynamicActorMeshScissored(int *param_1, undefined4 *param_2, undefined4 *param_3);

/* 0x8004f7e8: Per-triangle clip-and-emit helper for EmitDynamicActorMeshScissored. Builds a 6-bit visibility code from 3 gte_nclip signed-area tests (one per edge) + the upstream 5-bit frustum outcode, then dispatches the 32-entry UNK_8004f890 jumptable to pick a clip case. Fast path: when in_t2 prim head is empty, writes the triangle directly into the OT bump buffer instead of clipping. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined ClipAndEmitActorTriangle(void);

/* 0x8004fdac: GTE depth-cue color helper for a clipped polygon edge. Loads vertex RGB + fog direction (param_4 fog color) into GTE, runs gte_gpl12 (general-purpose linear interp along edge) followed by gte_dpcs_b (depth-cue single-color) to produce the blended RGB at the clip point. Returns the depth-cued color packed with POLY_F2 prim code (0x30000000). No direct callers: invoked exclusively via the UNK_8004f890 / UNK_80050028 clip jumptables from ClipAndEmit{Actor,FoggedActor}Triangle. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined8 BlendDepthCuedEdgeColor(int param_1, int param_2, undefined4 param_3, uint param_4);

/* 0x8004fea0: Fog-cued sibling of EmitDynamicActorMeshScissored. Drives per-triangle ClipAndEmitFoggedActorTriangle over the prim batch set up by the caller. Saves register state (s0-s8, ra, sp, k0) into the GTE backup area at _DAT_80077dd8..e04 so the clip jumptable can read it without reload. Saves/restores _DAT_800757b0 OT bump pointer. Reads its triangle list entirely from registers populated by the DrawActors per-actor projection loop — no explicit params. Caller: DrawActors fade-zone path (0x3000 < depth < 0x4000). */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined EmitDynamicActorMeshFogged(void);

/* 0x8004ff90: Per-triangle clip-and-emit helper for EmitDynamicActorMeshFogged. Mirrors ClipAndEmitActorTriangle: 3 gte_nclip tests build a 6-bit visibility code that dispatches the UNK_80050028 fog clip jumptable (32 cases). Fast path: when in_t2 is empty, emits a 5-dword POLY_F4 record into the caller's prim buffer (unaff_s2) and links it via OT head/tail (unaff_s3) without going through the clip jumptable. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined ClipAndEmitFoggedActorTriangle(void);

/* 0x80050240: Fog-shaded sibling of EmitDynamicActorMeshScissored. Mirrors its Z-cull pre-pass over the submesh list (param_1 = {count, *list}, param_2 = world rotation matrix) into &DAT_8006fcf4 survivor list. Stage 2 (uses param_3 = pose matrix) adds a per-vertex gte_dpcs_b color pass over RGB[] (loaded from _DAT_800757d4 / _g_nFadeProgress, writing back into scratch at &DAT_800724f4) before the gte_rtps_b projection loop; emits screen outcodes against the same scissor at _DAT_8007aa00..0c and dispatches each triangle to ClipAndEmitFogShadedActorTriangle (jumptable UNK_800506b4). Saves GTE backup at _DAT_80077dd8..e04 and advances _DAT_800757b0 OT bump pointer. Caller: DrawActors fade-band outline-jittered pass (depth 0x3001..0x3fff) — the per-vertex-dpcs sibling of EmitDynamicActorMeshScissored. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined2 *EmitDynamicActorMeshFogShaded(int *param_1, undefined4 *param_2, undefined4 *param_3);

/* 0x8005060c: Per-triangle clip-and-emit helper for EmitDynamicActorMeshFogShaded. Mirrors ClipAndEmit{,Fogged}ActorTriangle: 3× gte_nclip on the three SXY's loaded by the caller → 6-bit outcode → dispatches through 32-entry jumptable at UNK_800506b4. Fast direct-emit path (5-dword POLY_F* head/tail) when the prim head is empty; otherwise writes the 7-dword deferred slot into the &DAT_800704f4 stack at _DAT_80075798 for the caller's drain loop. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined ClipAndEmitFogShadedActorTriangle(void);

/* 0x80050bd0: Dynamic-actor draw. Walks DAT_80078640[0..DAT_800758bc] (the live-actor pointer array). For each actor: 1. Builds the actor-relative rotation matrix from camera euler angles (DAT_80076e1c/e1e/e20 plus per-frame swing DAT_800758fc / DAT_80075858). 2. Computes depth-cue color via ComputeDepthCuedColor(actor.material, ..., depth). 3. Projects the actor vertices (FUN_80017b48) into screen-space buffer DAT_80078dd8. 4. Submits each visible vertex to the actor primitive list at DAT_80077ea0 (one slot per edge: x/y/dx/dy/cross-product); these feed FUN_8004f4bc / FUN_80050240 which emit textured / depth-faded actor polys into the OT. The DAT_80076e24 vs DAT_800785b4 check at the end selects whether the player-controlled actor uses its own OT slot (-1) or the level-shared one. Trap calls are integer-divide guards (Ghidra-emitted; not real game logic). */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined8 DrawActors(void);

/* 0x800521c0: Visible-entity-list builder. Walks the entity array starting at DAT_80075828 (stride 0x58, visibility byte at +0x48, link byte at +0xa9, next-entity flags in word at +0xf8) and sorts each visible entity into one of three OT/render buckets at DAT_8006fcf4 / DAT_80071ef4 / DAT_800720f4 depending on its category flags. Hides entities whose mask byte at &amp;DAT_800771c8 + idx is zero. Terminates each list with a 0 entry. Called once at the top of every gameplay-frame render chain. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined BuildRenderEntityLists(void);

/* 0x800522c0: Gem-cutscene actor anim tick. Walks NULL-terminated actor-pointer list at *param_1, advances per-actor anim subframe at +0x40 by (DAT_1f800000._1_1_ >> param_2), wraps to next animation frame when subframe overflows 0x40 (consulting the per-anim length at puVar15[0]), and on key-frame markers (+0x27 sound-trigger byte) queues PlaySoundEffect requests into a small queue at &DAT_8006fcf4 + scratch. After the actor walk, drains the queue calling PlaySoundEffect(soundId, actorPtr, 8, actorPtr+0x54) for each queued trigger. Sole caller Gamestate0B_Update (gem cutscene substate); drives the cutscene actor anim playback + lip-sync-style sound emission. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined TickGemCutsceneActorAnims(uint *param_1, uint param_2);

/* 0x800524c4: Allocates the next free actor record from the primary (88-byte stride) + aux (0x18 stride) pools. Reads current free-list head _DAT_8007573c, increments _DAT_800756a4 live count, clears the slot's allocated marker (byte +0x48 = 0), then walks forward 88 bytes at a time skipping mid-pool free markers (0xfe) until it hits either a -1 end sentinel (new free list end at +0x28 = 0xff) or the next free slot. Repeats the same dance for the aux 0x18-byte pool at _DAT_80075930. Writes the aux pointer back to primary[0] (the bridge). Result: caller reads the *previous* _DAT_8007573c value as the freshly-allocated primary slot pointer. Only caller currently: GemPickup_Update. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined AllocActorRecordSlot(void);

/* 0x80052568: Frees an actor record: unlinks from spatial-grid cell list at _DAT_80075778[param_1[0xd]], decrements _DAT_800756a4 live count, marks the primary 88-byte slot freed (byte +0x48 = 0xfd if live-tail / 0xfe if mid-pool / 0xff if new free-list end), then frees the chained 0x18-byte aux record at *param_1, and advances free-list heads _DAT_8007573c (primary) / _DAT_80075930 (aux) to whichever slot is now the lowest free. The 0xfe/0xff/0xfd markers form a run-length-encoded free bitmap walked by AllocActorRecordSlot. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined DespawnActorRecord(int *param_1);

/* 0x800526a8: Per-actor finalize after pose change: looks up anim frame at &DAT_80076378[type_idx]+0x38+state*4+frame*8+0x24, copies prim-type byte to +0x41, copies bound-vector to +0x8 (offset selected by bits 19..20 of anim flags). If mob_table[+0x14]!=0 (collidable), quantizes pos.x/y at +0xc/+0x10 to a 14-bit grid cell index (32-cell-wide grid, bit 0x400 = z-sign), stores it at +0x34 (short), and prepends the actor onto the per-cell linked list at _DAT_80075778[cell]. Finally rebuilds a 5-short rotation matrix at +0x20..+0x30 by multiplying X-pitch * Y-yaw * Z-roll using the cos/sin LUTs at &DAT_8006cc78 / &DAT_8006cbf8 — the three byte Eulers live in the high 3 bytes of word +0x44 and each is a 2-byte LUT entry (8-bit angle × 2 = 0..0x1fe). Skips any matrix mult whose angle byte == 0. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined InsertActorIntoSpatialGrid(int param_1);

/* 0x800529cc: Hides an actor: clears prim-type tag bytes at +0x41/+0x4b (preventing render submission), clears the first dword of the bound-vec at +0x8, and writes 0xffff to the spatial-grid cell index at +0x34 (the "not in grid" sentinel). Caller-driven inverse of InsertActorIntoSpatialGrid; does NOT walk the per-cell linked list (the caller is responsible for unlinking, or the actor was never linked). Used by HUD init (UpdateHudCounters' setup pass over the 12 icon slots) and by the M09 level loader to reset stale slots. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined HideActorRenderRecord(int param_1);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined AccumulateAnimRootMotionDelta(int param_1, int *param_2, int param_3, int param_4);

/* 0x800533d0: Encodes the cached probe vector at _DAT_80077368/6c/70 (X/Y/Z, the collision-system scratch vector written by FindGroundHeightBelow / CastRayWorldAndActors / sphere-probe paths) plus a 14-bit scalar at _DAT_80076b88 into a packed direction code at param_1+0x1c. Only re-encodes when bit 31 of the existing field is set (caller flips bit 31 as a "needs recompute" sentinel). Magnitude path: gte_sqr0 on (X,Z), sqrt-LUT lookup at g_anSqrtLUT → 2D XZ-plane magnitude. Then two arctan2 quadrant-decompositions: (XZmag vs Y) → pitch byte angle, (Z vs X) → yaw byte angle (each = quadrant + 5-bit frac, packed to 6 bits via >>2). Output layout: bit 31 set + pitch<<22 + yaw<<16 + (_DAT_80076b88>>2) in low 14 bits. Callers: FUN_8001364c (level loader, spawn-time encoding), Gamestate0B_Update (gem cutscene anim re-encode), GemPickup_Update (per-frame burst direction). */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined EncodeCachedVecToActorDirCode(int param_1);

/* 0x800536a4: Resets the per-substep pad-input distribution table (head + 4 records of 0x18 bytes each). Head: param_1[0..2]=0, [6]=[7]=1, [5]=0x7f7f7f7f (neutral analog). Per-record (stride 0x18 = 6 dwords): zeroes [0x12/0x13/0x14], preserves [0x11] = param_1[3] (calibration link), writes [0x16] = 0x7f7f7f7f (neutral analog). Called by SnapshotPadInputState (end-of-frame) and the level-loader FUN_8001364c. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined ResetPadFrameSubstepTable(undefined4 *param_1);

/* 0x80053708: Snapshots the live pad input state (0xa4 words, head + 4-record per-substep table) from src=param_1 into dst=param_2 and then re-inits param_1 via ResetPadFrameSubstepTable so the next frame starts fresh. Toggles parity byte DAT_80075944 (frame double-buffer index 0/1). Called by per-frame ticks: TickSpyroFrameForceJump, TickSpyroFrameNoInput, TickSpyroGameplayFrame, UpdateCameraFrame. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined SnapshotPadInputState(undefined4 *param_1, void *param_2);

/* 0x8005375c: Writes the default uncalibrated 4-axis stick bounds at param_1[0x20..0x2b]: each axis gets +0x20=0x7f (neutral), +0x24=0 (low), +0x28=0xff (high) for axes 0..3. Used as the fallback when FinalizePadCalibration detects the controller's reported range is too narrow to trust. Callers: FinalizePadCalibration (fallback path), PollPadAndDistributeInput (init), InitPadSystem. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined ResetPadStickCalibrationDefaults(int param_1);

/* 0x80053790: Finalizes per-axis analog calibration after the controller reports its observed lo/mid/hi values. For each of 4 axes: param_1[+0x20+i] = recorded neutral, +0x24+i = lo bound, +0x28+i = hi bound. If hi-lo span < 0x30 (controller didn't move enough yet) OR lo-mid distance < 0x30: falls through to ResetPadStickCalibrationDefaults (sets the no-calibration defaults +0x20=0x7f, +0x24=0, +0x28=0xff for all 4) and returns 0. Otherwise: computes lo_mid breakpoint at +0x2c = neutral + '0' (0x30), hi_mid breakpoint at +0x30 = neutral - 0x30, and scaling factors +0x34 = 0x4000 / (hi - lo_mid), +0x3c = 0x3f80 / (lo_mid - lo), then clears the snapshot head (param_1[0..2]=0, param_1[6]=param_1[7]=1, and the 4-byte field at +0x14 of head = 0x7f7f7f7f neutral). Also clears the 4-record per-substep table: each record at +6 dwords stride has [0x12/0x13/0x14] = 0 and +0x58 (= +0x16 of record from caller view) = 0x7f7f7f7f. Returns 1 on success. Callers: PollPadAndDistributeInput, InitPadSystem. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined4 FinalizePadCalibration(undefined4 *param_1);

/* 0x80053940: Maps raw 8-bit analog stick values into the calibrated 0..0xff range for 4 axes (param_1[0..3]). For each axis: if raw < calibration[+0x2c] (mid-low): output = 0x7f + ((raw - low_mid) * scale_low[+0x34]) >> 7, clamped to ≤0xff. If raw > calibration[+0x30] (mid-high): output = 0x7f - ((raw - high_mid) * scale_hi[+0x3c]) >> 7, clamped to ≥0. Else: output = 0x7f (neutral). The two scale factors (+0x34 / +0x3c) and the two mid breakpoints (+0x2c / +0x30) per axis are filled in by FinalizePadCalibration from the controller's reported hi/lo bounds. Callers: SamplePadForDeathReplayFrame, PollPadAndDistributeInput. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined MapPadAxisToCalibratedRange(byte *param_1, int param_2);

/* 0x800539fc: Per-frame pad sample used during the death state (Spyro is dying or dead, gameplay frozen mid-replay). When g_nDeathState == 2 (death-active): reads raw pad (DAT_800786a2/a3 button shorts + DAT_800786a4 analog stick word), calls MapPadAxisToCalibratedRange to remap analog, packs (~buttons & 0xffff) | (analog<<16) into uVar5, advances death-replay ring write pointer _DAT_8007585c by 4 bytes. Other states: reads the ring back and reproduces the pre-recorded input for replay. Always writes the result into g_dwPadButtons / _DAT_8007737c (pressed/released edges vs _DAT_80077380 previous) and broadcasts to all 4 substep records at &DAT_800773bc..d0 (i*0x18). Also sets _DAT_80077384 = 3 (signaling 'dualshock present' for the replay) and _g_nFrameTicks = 2 (forces all substep records to share this frame's sample). Sole caller: CheckDeathPlane. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined SamplePadForDeathReplayFrame(void);

/* 0x80053c68: Per-VBlank pad poll callback (registered by InitPadSystem via InstallVSyncCallback). Increments g_nVblankTickCount (+ g_nCdStallWatchdogTicks while g_nCdStallMarker set), bails if g_nDeathState != 0 (death-replay path in SamplePadForDeathReplayFrame). Identifies pad type from g_abPadRawReport (status/id bytes) → g_nPadType {0=disconnected, 1=digital, 2=analog 0x41, 3=DualShock 0x73 / Saturn 0x53 remapped}; on disconnect sets g_nPadSetMainModePending, on state 6 sends PadSetActAlign(g_abPadActAlignTable) → g_nPadActAlignedFlag; PadInfoMode(0,2,0) → g_nPadIsDualshockFlag. Vibration: builds g_abPadActCommand from g_nHitRumbleTimer (motor1 0x78) / g_nVibrationLevel (motor0) / g_nPulseRumbleTimer+Amount, decrements all timers, sends via PadSetAct — all cleared unless g_nOptionVibrationEnabled && DualShock. Input: inverts button shorts, remaps Saturn buttons, MapPadAxisToCalibratedRange → g_dwPadAnalogCalibrated, synthesizes virtual D-pad bits 0x1000-0x8000 from the left stick (g_nPadStickActiveFlag), edge-detects vs g_dwPadHeld, resets (substep 0) or OR-accumulates g_dwPadPressed / g_dwPadReleased, maintains the sticky idle flags g_nPadAllInputIdleFlag / g_nPadDirectionalIdleFlag, and distributes {type, held, pressed, released, stickActive, analog} into g_anPadSubstepRing[g_nFrameTicks*6] (max 4 substeps). Increments g_nFrameTicks. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined PollPadAndDistributeInput(void);

/* 0x800542e4: Sets a multi-digit decimal counter into HUD icon slots. Splits param_3 (value) into decimal digits, writes them into param_2 slots starting at slot index param_1 (each slot is a 0x58-byte record at &DAT_80077fec[slot*0x58]). param_4 = init/commit flag: when 0, writes only the pending digit byte at +0x35 (used mid-roll-animation); when non-zero, writes the committed glyph UV at +0x22 (= 0x104 + digit, the digit-glyph UV in the HUD tilemap) and forces alpha 0xff at +0x3c. Leading zeros render as glyph 0 unless param_4 != 0 in which case unused slots get alpha 0. Called by UpdateHudCounters' state-2 path and by the HUD init. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined SetHudCounterDigits(int param_1, int param_2, int param_3, int param_4);

/* 0x80054400: Per-slot roll-animation offset writer for the big HUD icon records. For param_2 slots starting at param_1, writes (base + param_3) into the +0x14 field of each 0x58-byte icon record at &DAT_80077fec, where base = (&DAT_8006e690)[slot*0xc] (= +4 of the per-slot rest-position vec3 at &DAT_8006e68c). Sole caller UpdateHudCounters passes ±sine-LUT entries to drive the roll-in/roll-out animation on digit changes. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined OffsetHudIconRollAxis(int param_1, int param_2, int param_3);

/* 0x8005445c: Per-slot roll-animation offset writer for the small HUD digit sprite descriptors. For param_2 slots starting at param_1, writes (base + param_3) into the +0x2 (V) field of each 8-byte sprite record at &DAT_8007840c, where base = (&DAT_8006e71e)[slot*4] (= +0x2 of the per-slot rest UV in &DAT_8006e71c). Sole caller UpdateHudCounters; mirror of OffsetHudIconRollAxis but for the digit-sprite vertex table rather than the big icon records. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined OffsetHudDigitRollAxis(int param_1, int param_2, short param_3);

/* 0x800544a8: Commits the per-slot digit-roll animation state byte at +0x32 of each 0x58-byte HUD icon record. For param_2 slots starting at param_1: if the slot is already mid-animation (+0x32 != 0) OR its pending glyph at +0x35 differs from its committed glyph at +0x22 (- 0x104 base), writes param_3 as the new state. When param_3 == 0xc0 (end-of-roll commit), also latches +0x22 = pending glyph + 0x104 and forces alpha 0xff. Called by UpdateHudCounters' state-2 path to ratchet the roll animation forward. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined CommitHudCounterAnimState(int param_1, int param_2, int param_3);

/* 0x80054600: HUD init for entering gameplay-with-HUD state. Zeros + re-seeds the 12 big-icon records in g_abHudIconActorRecords (HideActorRenderRecord per slot, rest vec3 from g_anHudIconRestPos, default render bytes), copies the 12 rest rects from g_anHudDigitRestRects into g_anHudSpriteRects, sets all five g_abHudCounterDisplayState machines to 1 with phases/timers zeroed, seeds the cached counts (g_nHudLevelGemCachedCount / g_nHudDragonCachedCount / g_nHudLivesCachedCount / g_nHudWorldEggCachedCount / g_nHudLevelEggCachedCount / g_nHudButterflyCachedCount) from the live values, and draws the initial digits via SetHudCounterDigits x3. param_1 != 0 additionally clears g_nHudWorldEggRollRequest and lays out the sparx-food butterflies from g_abHudButterflyRingXY. Callers: EndGemPickupToWorld, EndSaveMenuToWorld, EnterPauseMenu, BuildLoadedLevelRuntime. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined InitHudCounters(int param_1);

/* 0x80054988: Per-frame HUD update. Ticks the five digit-counter state machines in g_abHudCounterDisplayState (0=level gems vs g_anLevelGemsCollected[g_nLevelIntroIndex], 1=dragons rescued, 2=extra lives + sparx food, 3=world-egg tally vs g_nWorldEggsRecovered — forced visible while g_nHudWorldEggRollRequest is set (overlay-written balloonist flag), 4=current-level eggs vs g_nLevelEggCount). Each machine: state 1 roll-in (g_anHudRollInStepTable), state 2 hold + count-up toward the live value (g_nHud*HoldTimer pacing, sparkle bursts via g_pfnLevelOverlayParticleSpawn), state 3 roll-out (g_anHudRollOutStepTable); counter 0 adds state 4 = level-complete highlight when cached == g_anLevelTotalGemsTable[level]. Animates icon bob/tint bytes inside g_abHudIconActorRecords from g_anSineLut, lays out one butterfly sprite per food unit into g_anHudSpriteRects, and rolls g_nSparxFoodCount over into +1 extra life at 20. Callers: GamestateUpdate, PauseMenu_Update, EnterPauseMenu. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined UpdateHudCounters(void);

/* 0x8005595c: Top-level music/sound state init called from Initialize after SpuInit. Calls FUN_8005ba60 (sound channel reset), seeds master volume + CD-DA mix words at DAT_80075f08..DAT_80076240, then FUN_8005cc58 / FUN_8005c7ac / FUN_8005c7d4 / FUN_8005cb7c (sound-bank registration), FillWord(DAT_80075f30, 0, 0x2a0) zeros the channel-state block. Body internals deferred to M10. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined InitSoundEngine(void);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined4 PlaySoundEffect(uint param_1, int param_2, uint param_3, undefined1 *param_4);

/* 0x80056200: Stops one SPU voice by index. param_1 = voice index (low 7 bits, 0..23). Sets bit param_1 of _DAT_8007623c (pending hardware-keyoff mask), then if param_2 is 2 or 4: resets the table row at &DAT_80075f30 + (idx*0x1c) — writes 0x7f to the busy-marker via the writeback pointer at +0x18, zeros owner-key +0x00 + writeback ptr +0x18 + interp delta +0x14, sets flags ushort +0x0e=0x40 (idle), sets byte +0x0d=0xff (alignment). The 24-entry × 0x1c table at &DAT_80075f30 holds per-voice runtime state. Sole caller: GemPickup_Update. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined StopSoundVoiceByIndex(uint param_1, int param_2);

/* 0x800562a4: Stops all SPU voices whose owner-key at +0x00 matches param_1. Walks all 24 voice rows at &DAT_80075f30 (stride 0x1c); for each match: kills the voice (resets row to idle as in StopSoundVoiceByIndex) when param_2==1, or when param_2==2 AND the looping flag (bit 0x100 of flags +0x0e) is set. OR's the kill bitmask into _DAT_8007623c at end. Callers: ChangeSpyroState (stops all Spyro-owned voices on state change), ResetSpyroEntity (full-stop on respawn/level-load). */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined StopSoundVoicesByOwner(int param_1, int param_2);

/* 0x8005637c: Per-frame SPU voice mixer tick — walks all 24 voice rows at &DAT_80075f30 (stride 0x1c). For each active voice (flags bit 0x1, mask 0x40 idle clear): queries hardware state via FUN_8005c9d0 (SpuGetKeyStatus equiv) and dispatches on flags 0x1c mode group. Mode 8 (3D positional): subtracts source pos from camera at _DAT_80076df8, computes distance via VectorLength, ArcTan2 vs camera yaw g_nCameraEulerYaw, calls FUN_80056c84 for 3D pan, attenuates with 1/r² falloff over the per-voice range at owner+0x55 (defaults 0x4000 when 0), reuses cached vol/pan via SpuGetVoiceVolume when out-of-range. Mode 4: defaults vol to 0x2000. Mode 0x10: holds last vol/pan. Always multiplies final by master vol _DAT_8007622c (>>0xc), advances per-voice interp counters +0x14/+0x18, issues SPU update via FUN_8005c7ac (SpuSetVoiceVolume). On hardware status indicating natural-end (FUN_8005c9d0 returns 1 or 3 + flag bit 0x2 set): kills the row and ORs bit into _DAT_8007623c. Callers: FUN_80016500 (CdReadAndCopyToRam), GamestateUpdate. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined4 TickActiveSoundVoices(void);

/* 0x800567f4: Music streaming command dispatcher (CD-DA / XA via libcd). param_2 = command: 1 = play track param_1 (uses sector at DAT_800774b8 + param_1*8); 2 or 4 = stop streaming (sets state machine via _DAT_800776c8 to fade/pause); 8 = resume streaming from saved sector _DAT_800776b8. Play path issues CdlSetmode(0x0e, 0xc8) + CdlSetfilter(0x0d) + CdlReadS(0x1b) (XA-stream read). Short-circuits when mute bit (_DAT_800774b4 & 0x80) is set; falls through to "stop" path if music volume is 0 or 0x40 state bit not set. Used by game-state transitions (level/cutscene boundaries) and StopAllSoundExceptMask. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined HandleMusicCommand(int param_1, int param_2);

/* 0x80056b28: Mass voice-stop helper used for state transitions. Walks all 24 SPU voice rows at &DAT_80075f30: for any active voice (flags +0x0e bits 0x83 set — bit 0x1=active or bit 0x2=playing) NOT in param_1 mask, stamps owner's per-mode busy-flag byte (offset +0x54 for mode-8 / +0x2a0 for mode-4) with 0x7f=released, ORs voice idx bit into _DAT_8007623c (pending hardware keyoff mask consumed by SPU update), zeros voice row (owner ptr / interp delta / writeback ptr), sets flags=0x40 (idle). After the walk, zeros _DAT_80076238 and calls HandleMusicCommand(0, 4) to stop CD music. Common callers (param_1=0 → "kill everything"): pause-menu enter, level-cutscene begin, death plane, credits sequence. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined StopAllSoundExceptMask(uint param_1);

/* 0x80056c84: 3D positional sound pan + distance falloff. Inputs: param_1=&out[L,R] shorts, param_2=squared-distance vol scalar, param_3=pan byte (0x80=center; wraps full-L/full-R via the 0x40/0xbf compare), param_4=range (divisor; returns early if 0), param_5=listener-relative baseline [L,R]. Computes falloff fraction (param_2<<8)/param_4, derives per-channel gains, applies to baseline, clamps each to [0,0x3fff]. If mono-mix flag _DAT_80076240 set, averages L=R. Called by PlaySoundEffect (one-shot trigger pan) + TickActiveSoundVoices (per-frame mode-8 voices). */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined ComputeVoicePanAndFalloff(short *param_1, int param_2, byte param_3, uint param_4, short *param_5);

/* 0x80056dc4: Looks up the SPU voice currently playing for a given (owner-key, marker-byte) pair. Walks all 24 voice rows at &DAT_80075f30 matching (param_1 == row+0x00 = owner-key) AND (param_2 == row+0x0d = marker). Returns: 0 = no match (sound not playing for this owner/marker); 1 = found, one-shot (flags +0x0e bit 0x100 clear); 2 = found, looping (flags +0x0e bit 0x100 set). Sole caller: PlaySoundEffect (used to skip re-triggering an already-playing sound). */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined4 GetSoundVoiceStatusByOwner(int param_1, uint param_2);

/* 0x80056e3c: Reconciles software voice state with hardware. Probes the SPU via GetAllSpuVoiceKeyStatus into a 24-byte local buffer; for any voice whose hardware status code is 1 (SPU_ON) but whose software flags +0x0e have neither bit 0x1 (active) nor bit 0x40 (idle) set (i.e., software thinks the row is stale), marks the row idle (flags=0x40) and ORs the bit into _DAT_8007623c (pending hardware-keyoff mask consumed by SetSpuKeyMask). Sole caller: TickSpuPerFrame (runs once every 64 frames via g_dwGamestateFrames & 0x3f). */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined SyncSoundVoicesWithHardware(void);

/* 0x80056ed4: Per-frame SPU update tick. Every 64 frames (g_dwGamestateFrames & 0x3f == 0) runs SyncSoundVoicesWithHardware to reconcile drift. Then drains the two pending hardware-key masks: if _DAT_80076238 (pending keyon mask) != 0, calls SetSpuKeyMask(1, mask) and clears it; if _DAT_8007623c (pending keyoff mask) != 0, calls SetSpuKeyMask(0, mask) and clears it. Callers: GamestateUpdate + every per-state Update that may set/clear voices (PauseMenu_Update, GemCutscene_Update, etc.). */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined TickSpuPerFrame(void);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined DispatchSpyroTriggerEvent(int param_1, uint param_2);

/* 0x800573c8: Emit-list rasterizer (hand-asm GTE core; saves s-regs to scratchpad 1f800000, gp anchor 80075264). Walks the per-level particle/billboard emit list at g_pEmitListBase: linear 0x20-byte records, capacity 0x2000 (256 records), list empty when a record's opcode byte is 0xff (loader seeds this; GemPickup_Update resets it during the rescue cutscene; records are appended by overlay code via g_pfnLevelOverlayParticleSpawn). RECORD FORMAT: +0 short header {lo byte = index into g_apMobEntryTable (texture source), hi byte = opcode; hi<0 = skip record, 0xff = end}; +3 byte = written back each frame (1 = prim emitted, 0 = culled) so game logic can poll visibility; +4/+8 packed world pos (x,y,z halfwords, transformed camera-relative via g_anCameraPos>>2 and g_anWorldToCameraRotMtx); for opcodes 2/5/6+: +8 bits 16-23 = brightness (IR0), bits 23+ = spin phase into g_anSineLut; +0xc = packed RGB/mode color word; +0x10 ushort {lo = 8-byte sub-record index into the mob's table (+4/+8 = texpage/UV words), hi = OT depth-bin bias subtracted from sz>>5}; +0x14 = texword for flat opcodes. OPCODES: 0 = 1px line dot (0x03-len prim), 1/4 = flat 2-vert prim (0x04-len; 4 = absolute-pos variant via gte_ldtr), 2 = lit spinning billboard quad, 3 = alt-lit billboard, 5 = depth-faded spinning billboard, 6+ = default spinning billboard (all 0x09-len POLY_FT4, 0x28 bytes). Links prims into g_pOtDepthBinArrayBase bins; bails with g_nPrimBufferOverflowFlag=1 at g_pPrimBufferLimit-0x400. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined RasterizeEmitList(void);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined DrawWorldSparkleParticles(void);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined8 DrawWorldRotatingBillboards(int param_1);

/* 0x80058864: Gem-pickup aura emitter — 8-pointed semitransparent star burst billboard around the gem position. Loads view matrix at _DAT_80076dd0..de0, computes camera-relative anchor (_DAT_8007624c/dfc/e00 minus camera _DAT_80076df8/250/254), then loads billboard rotation matrix at _DAT_80076258..68 (filled by ApplyEulerRotation from byte-angles at _DAT_80076270 by caller). Projects center vertex + 8 inner-ring verts (radius scale = _DAT_8007626c >> 0xc, angles from sin/cos LUTs at &DAT_8006cbf8/&DAT_8006cc78 stride 0x40 = 45°) + 8 outer-ring verts (radius 4x larger via >> 0xa, angles offset by 22.5°). Inner ring is closed (vertex[0] = vertex[N]). Emits drawmode prim (0xe1000620 untextured + 0x2000000 semi-trans), then loops 8 times emitting 2 POLY_F3 per slice: outer-spike triangle (inner[i], inner[i+1], outer[i]) + inner-fan triangle (inner[i], inner[i+1], center) for 16 triangles total. Color byte applied to all 6 color slots from DAT_80076273. Each prim inserts into OT via _DAT_8007581c head-ptr. Sole caller: Gamestate08_Draw, conditional on _DAT_80076248 trigger flag. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined DrawGemPickupAuraStar(void);

/* 0x80058b68: Resets the world particle/billboard tables on level load. Zeroes the 16-entry x 0x24-byte emitter array at &DAT_80078800 (used by DrawWorldParticleEmitters/FUN_800580f4) and the 8-entry x 0x18-byte billboard array at &DAT_80077114 (used by FUN_800584c4). Sole caller FUN_8001364c (level loader meat). */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined ResetWorldParticleTables(void);

/* 0x80058ba8: Per-frame driver for the world particle/billboard emitter pair. Calls FUN_800580f4 (sparkle/particle emitter — iterates &DAT_80078800 16-entry table, each entry = 9 ints, emits GTE-projected POLY_FT3+POLY_GT3 chains scaled by distance) then FUN_800584c4(_g_nFrameStep) (16-entry billboard rotator at &DAT_800770f0..0x800771b0, advances per-entry phase by _g_nFrameStep, emits POLY_GT4 with cos/sin tables from DAT_8006cbf8/cc78). Called by ComposeFrameScene. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined8 DrawWorldParticleEmitters(void);

/* 0x80058bd8: Precomputes the 3 camera-space basis vectors used by the billboard sprite emitter. Calls WorldToCameraRotate on the X-axis (0x1000,0,0), Y-axis (0,0x1000,0), and inverted Z-axis (0,0,-0x1000), packs each result via VectorToSVector to the 3 SVECTOR slots at &DAT_800770e0/e6/ec. Sole caller GamestateUpdate; runs once per frame after the camera matrix is committed so the billboard emitter can splat sprites with consistent screen-facing orientation. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined ComputeBillboardBasisVectors(void);

/* 0x80058c7c: Seeds the 6-component light-direction vector pair at _DAT_800770c8..dc with the fixed default {-0x9d5, -0x99a, -0xb20, -0x9d5, -0x99a, -0xb20} (twice the same vector — likely two light slots sharing the world default). Called by level-init paths before RotateLightVectorXZ tweaks the vector per-frame. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined InitLightVectorConstants(void);

/* 0x80058cc0: Spins the primary light direction vector around the world Y axis. Advances byte angle accumulator _DAT_800770f4 by param_1 (wraps mod 256), looks up cos at DAT_8006cc78[angle*2] scaled by 0x7bf and sin at DAT_8006cbf8[angle*2] scaled by 0x18c9 (the two scale factors give an asymmetric ellipse, not a circle), pins Y component at -0x400 (flat overhead). Writes XYZ to _DAT_800770c8/cc/d0. Used to animate sun/moon direction over time. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined RotateLightVectorXZ(int param_1);

/* 0x80058d64: Draws the horn-strike swoosh trails (formerly misnamed DrawWorldFenceBillboards — a survey guess). For each armed trail slot (g_abSpyroHornStrikeSlotAge, walked 8..1) it follows the slot's precomputed sweep arc (g_anSpyroHornStrikeArcPathsShort/Long) around g_anSpyroHornStrikeAnchorPos, oriented by g_anSpyroHornStrikeRenderMtx on top of the world-to-camera matrix. End-cap stage: projects 3 ring verts at the trail head and emits up to 4 backface-culled POLY_F3 prims colored from g_anSpyroHornStrikeCapColors (Alt palette when g_nSpyroHornStrikeVariant != 0, i.e. flame-breath-boosted swing). Ribbon stage: POLY_GT3 strip from the per-age head (g_anSpyroHornStrikeAgeConfigShort/Long) back to the clip point (g_abSpyroHornStrikeSlotEndAge), fading by 0xa0a0a then 0x50505 with a 2x-elongated teardrop head. OT insert at sz3>>7 via g_pOtDepthBinArrayBase. Sole caller: ComposeFrameScene. The _DAT_80077dd8..e04 stores are register-spill scratch (Ghidra artifact). */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined8 DrawSpyroHornStrikeTrails(void);

/* 0x8005956c: Save-block checksum compute. Sums the unsigned byte values of the first 0x58c bytes of the save buffer (everything except the 4-byte checksum field itself at +0x58c). Used both by BuildSaveGameBuffer to seal the buffer and by Gamestate0B_Update to verify a loaded buffer matches its stored checksum. */
int ComputeSaveGameChecksum(byte *param_1);

/* 0x80059864: Memcard save-block serializer. Packs current game state into a 0x600-byte buffer (memcard block size): +0x00 level ID + sfx/music vols + vibration flag; +0x04 g_dwActiveCameraOptions; +0x08 mono flag, +0x0b extra lives; +0x10 g_anWorldPersistentFlags (0x14 bytes); +0x24 5x5 per-world table from g_abWorldPerLevelByteTable; +0x40 g_abLevelVisitedFlag (0x24 bytes); +0x64 g_abLevelGemPercent (0x24 bytes); +0x88 g_anLevelDragonsRescued as bytes (max 5/level); +0xac g_anLevelGemsCollected as shorts (up to 2000 needs 16 bits); +0xf4 g_anLevelEggsRecovered as bytes; +0x106 g_abSaveOptionsBlock (6 bytes); +0x10c g_anLevelKillBitmapTable (0x480 bytes, per-level entity-state blob); +0x58c checksum from ComputeSaveGameChecksum. Sole caller Gamestate0B_Update (save-write substate). */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined BuildSaveGameBuffer(byte *param_1);

/* 0x80059a48: Spyro drop-shadow renderer. Emits a 16-segment subtractive-blend polygon ring (texpage 0xe1000640, blend mode 2 = B-F, darkens framebuffer) anchored at g_anSpyroShadowPos, rotated to Spyro's yaw (g_abSpyroPersistentEuler[2] through g_anSpyroShadowCircleTable 16-gon). Silhouette extents come from per-anim-keyframe bytes (mesh +0x38 anim table, +0x24 frame entry) interpolated by g_bSpyroSubFrameTimer in GTE scratchpad; vertex Z lift from g_abSpyroShadowRingHeights; if any g_abSpyroShadowRingNoGroundFlags byte is set the silhouette shrinks x3/4. Per-segment OT bin = depth>>9 - g_nSpyroShadowOtBinBias (skip if negative). Early-out while g_nSpyroShadowHiddenFlag >= 1 (over water/abyss or trigger event). Was misnamed DrawSunFlareBillboard until the 36th M14 sweep. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined8 DrawSpyroDropShadow(void);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined DrawEntityDirectionalSpikes(void);

/* 0x8005a470: Per-level globals fixup table. Switches on _DAT_8007596c (current level ID, ~50 cases: home worlds 0/2/3/4/7/9 + boss levels + treasure rooms + special-state levels 0x0f/0x19/0x23/0x2d/0x37 which seed gamestate-7 mode-handler set), assigns level-specific data pointers (_g_pDrawBufA, _g_pfnGamestate0EarlyHook, _g_pfnGamestate0LateHook, _DAT_800758cc, _DAT_800758e4, plus optional gem-table pointer set at _DAT_800757a0/758a8/8007574c/800758d8) into the active-level slots — these point into the level-specific overlay loaded by the level transition state machine. Sets _g_pDrawBufB = _g_pDrawBufA as a final unconditional pair. Sole caller FUN_80015370 (level transition SM); runs once per level entry after the level overlay has been DMA'd into PSX RAM. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined ApplyPerLevelGlobalsTable(void);

/* 0x8005b6f8: Carves the per-frame render scratch out of the top of the work area: g_pEmitListBuffer = top-0x2000, OT depth-bin base = top-0x6000 (g_pFrameOtDepthBinBase0/1 + published g_pRenderScratchOtBase, 0x4000 zeroed), OT merged-chain slot = top-0x6008 (g_pFrameOtMergedChainSlot0/1, 8 bytes zeroed), then two prim-buffer halves of 0x1c000 (param=0, in-game) or 0x13000 (cutscene) growing down: g_pFramePrimBufferBase1 then g_pFramePrimBufferBase0. Publishes the bounds at g_pRenderScratchRegionBase/PrimBase1/PrimTop/OtBase. Callers: Gamestate0D_Update, GemCutscene_Update, LoadActorMeshTables. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined InitActorMeshScratchRegions(int param_1);

/* 0x8005b7d8: Loads the actor mesh table from CD into the top of the boot-load region. CdReadAndCopyToRam pulls a directory (size DAT_8007a714, sector DAT_8007a710) into a buffer below 0x80200000-g_nBootBufferOffset; iterates up to 64 entries; for each: FUN_800133e0 (anim/mesh header relocator - M09) and stores the resolved pointer at DAT_80076378 + slot*4. Finishes with FUN_8005b6f8(0) to post-init the actor system. Body internals deferred to M09. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined LoadActorMeshTables(void);

/* 0x8005b8e0: Possible SNMAIN.OBJ/__SN_ENTRY_POINT */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined start(undefined4 param_1, undefined4 param_2, uint param_3, int *param_4);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined __main(void);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined __do_global_dtors(void);

/* 0x8005ba60: PSY-Q SpuInit equivalent (S_I.OBJ/SpuInit signature match). Trivial 1-line wrapper: calls InitSpuSubsystem(0) (full-init path that re-seeds voice-table shadow, opens VBlank event, zeros DMA tracking state). No other callers under this address — game routes SPU subsystem init through this single entry. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined InitSpu(void);

/* 0x8005ba80: SPU subsystem init worker — the public InitSpu wraps with (0). Sequence: (1) FUN_8005ddc8 vtable callback (pre-init hook through PTR_PTR_800749ac+0xc), (2) InitSpuHardware(param_1) — hardware reset + per-voice register seed, (3) when param_1==0: fills all 24 entries of g_anSpuVoiceNoteShadow with 0xc000 (default note shadow), (4) InstallSpuDmaEventHandler — opens VBlank event 0xf0000009 for SPU DMA completion, (5) zeros DMA-tracking state (DAT_800730f4/f8/104/108/10a/10c/110), (6) saves DAT_800735bc -> DAT_800730fc (transfer-control default), (7) WriteSpuRegister(0xd1, DAT_800735bc, 0) — direct SPU register write at slot 0xd1, (8) zeros more SPU tracking state (DAT_800735b0/b4/b8, DAT_800730f0/ec/118/114). param_1 selects full reset (0) vs minimal re-init (!=0; skips voice-shadow seed). Sole caller: InitSpu. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined InitSpuSubsystem(int param_1);

/* 0x8005bb78: Installs the VBlank-driven SPU DMA-completion event handler exactly once (guarded by SPU-state-struct+0x40 install flag). Wraps EnterCriticalSection / ExitCriticalSection (syscall(0)) around OpenEvent(0xf0000009, 0x20, 0x2000, NULL) (vector=root-counter VBlank, class=interrupt-handler-mode, mode=do-not-call-callback) + EnableEvent. Saves the event handle into DAT_800730e8 for later cleanup. Also runs FUN_8005c788 (further DMA-engine init via FUN_8005de28). Sole caller: InitSpuSubsystem. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined InstallSpuDmaEventHandler(void);

/* 0x8005bbf4: PSY-Q SpuInit-equivalent SPU hardware reset. Clears main/reverb vol, sets transfer mode=4, key-offs all 24 voices, waits SPUSTAT lower 11 bits to settle. When param_1==0 (full-init path): clears FM/Noise/reverb-config regs, calls WriteSpuRamPio(shadow+0x14, 0x10) to seed reverb work area, writes default voice register set (VolL=0/VolR=0/Pitch=0x3fff/StartAddr=0x200/ADSR1=0/ADSR2=0) to all 24 voices, then key-on-all-24 followed by key-off-all-24 to flush key state. Finally sets SPUCNT (+0x1aa) = 0xc000 (SPU enable + IRQ unmask). param_1 selects full reset (0) vs minimal re-init (!=0). Sole caller: FUN_8005ba80 (InitSpu wrapper). */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined4 InitSpuHardware(int param_1);

/* 0x8005be88: PSY-Q SpuWrite-style PIO upload to SPU sound RAM via the data register at SPU_BASE+0x1a8. Stamps the SPU transfer-address register (+0x1a6) from the shadow at the top. Chunks param_2 bytes into batches of up to 0x40 bytes, writing 2 bytes per store, then sets SPUCNT transfer-mode bits (+0x1aa & 0xffcf | 0x10), and waits for SPUSTAT (+0x1ae) bit 0x400 (write-FIFO ready) to clear. Loops until param_2 reaches 0; on entry with param_2==0, finalizes by clearing the SPUCNT transfer-mode bits and waiting SPUSTAT lower 11 bits to return to the saved value. Used by both the SPU init path (seeding the reverb work area) and the PIO branch of the DMA/PIO dispatcher FUN_8005c450. Logs "SPU:T/O [...]" on timeout. Callers: InitSpuHardware, FUN_8005c450. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined WriteSpuRamPio(undefined2 *param_1, uint param_2);

/* 0x8005c1c0: PSY-Q-style multi-mode SPU DMA transfer controller. param_1 = subcommand: 0 = start READ transfer (SPU→CPU; waits for SPU transfer-addr register at +0x1a6 to match shadow, then sets SPUCNT (+0x1aa) transfer-mode bits = 0x30 = DMA-read, sets pending-read flag DAT_800735a4=1 so subcmd 3 will issue CHCR=0x1000200); 1 = start WRITE transfer (CPU→SPU; same wait, sets SPUCNT transfer-mode bits = 0x20 = DMA-write, DAT_800735a4=0 → CHCR=0x1000201); 2 = set transfer-address shadow + immediately stamp encoded form to SPU +0x1a6 register; 3 = wait for transfer-mode bits to settle, fire pre-kick state-bit callback (SetSpuDmaReadStateBits if read / SetSpuDmaWriteStateBits if write), then kick DMA controller via DPCR-shadow at SPU-state+0x3c (byte addr), +0x40 (chunk count = (param_3>>6 round-up) | 0x10), +0x44 (control word 0x1000200 read / 0x1000201 write). Returns 0xfffffffe on timeout, 0 otherwise. Used by both ReadSpuRamDma and the DMA path of WriteSpuRamInternal. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined4 ControlSpuDmaTransfer(int param_1, uint param_2, uint param_3);

/* 0x8005c450: SPU RAM write dispatcher (CPU→SPU) — chooses DMA or PIO based on g_dwSpuTransferMode flag at SPU-state-struct+0x4e. DMA path (flag==0): builds encoded SPU address from current transfer-addr shadow shifted left by g_nSpuAddrShift, then ControlSpuDmaTransfer(2, addr, _) sets destination + ControlSpuDmaTransfer(1, addr, _) puts the channel into DMA-write (CPU→SPU) mode + ControlSpuDmaTransfer(3, buf, nbytes) kicks the actual DMA controller. PIO path (flag!=0): WriteSpuRamPio(buf, nbytes) directly. Sole caller: WriteSpuRam (public size-clamping entry). */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
uint WriteSpuRamInternal(undefined2 *param_1, uint param_2, uint param_3);

/* 0x8005c4d8: DMA SPU→CPU read implementation (no PIO fallback). Re-stamps the current transfer-addr shadow (encoded back to a byte address via the SPU address shift) into the DMA controller via ControlSpuDmaTransfer subcmd 2 (set addr), switches direction to "read SPU→CPU" via subcmd 0 (SPUCNT |= 0x30 / pending-read flag DAT_800735a4=1 → CHCR=0x1000200), then subcmd 3 kicks the DMA controller with (param_1=CPU buffer addr, param_2=byte count). Returns param_2. Sole caller: ReadSpuRam (public size-clamping entry). */
uint ReadSpuRamDma(uint param_1, uint param_2, uint param_3);

/* 0x8005c540: Write one short into an arbitrary SPU register slot. Address = (param_1*2) + SPU register base ptr at SPU-state-struct+24. When param_3==0 writes param_2 raw; otherwise writes (param_2 >> g_nSpuAddrShift) — used to encode byte-addresses into SPU register units. Used by InitSpuSubsystem to seed the SPU control word at slot 0xd1 with DAT_800735bc. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined WriteSpuRegister(int param_1, uint param_2, int param_3);

/* 0x8005c588: Encodes a byte-address into SPU register units and writes it to one voice's address slot. Aligns up by DAT_80073580 (8) if DAT_80073578 alignment-enable is set; shifts down by DAT_8007357c (=3, since SPU memory is addressed in 8-byte blocks); writes the result short to PTR_VOICE_00_LEFT_RIGHT_80073554 + param_1*2 when param_1>=0. Sentinels: param_1==-1 returns the encoded value (no write), param_1==-2 returns the aligned byte address unchanged. Used by ApplySpuVoiceAttrRange for sample-start (slot voice*8|3) and loop-start (slot voice*8|7) writes. */
uint WriteSpuVoiceAddrRegister(int param_1, uint param_2);

/* 0x8005c6c8: Pre-DMA-kick CHCR state-bit prep for SPU writes (CPU -> SPU). Clears CHCR upper-byte lower nibble (bits 24-27 — busy + reserved) and sets bit 29 as a software sentinel. Called by ControlSpuDmaTransfer subcmd 3 immediately before the real CHCR write (0x1000201) so the channel is in a known state when the new transfer is triggered. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined SetSpuDmaWriteStateBits(void);

/* 0x8005c6f4: Pre-DMA-kick CHCR state-bit prep for SPU reads (SPU -> CPU). Same shape as SetSpuDmaWriteStateBits but ORs in 0x22000000 (sentinel bit 29 + bit 25 distinguishes the read path). Called by ControlSpuDmaTransfer subcmd 3 immediately before the real CHCR write (0x1000200). */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined SetSpuDmaReadStateBits(void);

/* 0x8005c720: SPU register-write fence — empty 0x3c-iter busy-wait loop that gives the SPU time to absorb prior register writes before the next access. Called as a "flush" after each batch of voice-register writes (ApplySpuVoiceAttrRange, WriteSpuVoiceAddrRegister, SetSpuVoiceVolumeLR). PSY-Q-internal style hardware barrier. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined DelaySpuRegisterWrite(void);

/* 0x8005c788: Installs the SPU DMA channel-4 completion callback. Trivial wrapper: forwards caller's a0 as a1 (the callback function pointer), hardcodes a0=4 (SPU DMA channel), and calls DmaCallback. Sole caller: InstallSpuDmaEventHandler — pairs the OpenEvent VBlank handler with the underlying DMA-completion hook. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined InstallSpuDmaCallback(void);

/* 0x8005c7ac: Apply SPU voice attr struct to all selected voices and flush. Thin wrapper for FUN_8005cfec(attr, 0, 0x17, 0): iterates voice indices 0..23, writes attr fields (volume/pitch/ADSR/sample-addr/mode) into the voice register shadow at PTR_VOICE_00_LEFT_RIGHT_80073554 for each voice whose bit is set in attr[0] (voice select mask), then issues hardware flush via FUN_8005c720. Equivalent to PSY-Q SpuSetVoiceAttr. Callers: InitSoundEngine, PlaySoundEffect, TickActiveSoundVoices. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined ApplySpuVoiceAttr(uint *param_1);

/* 0x8005c7d4: PSY-Q SpuSetKey-equivalent voice key-on / key-off applier. param_1 = 0 (keyoff) or 1 (keyon); param_2 = 24-bit voice mask (low 16 + high 8). Dispatches by SPU-state callback-mode flag at +0x10 bit 0: SYNC path writes SPU registers +0x188/+0x18a (KON lo/hi) or +0x18c/+0x18e (KOFF lo/hi) directly and updates the live key-mask DAT_800730ec; DEFERRED path (called from event-callback context) queues into shadow registers _DAT_800777a8/aa (KON) or _DAT_800777ac/ae (KOFF), accumulates into DAT_80073114, and sets pending-flush bit DAT_80073118 |= 1. Sole caller: TickSpuPerFrame (drains _DAT_80076238 pending-keyon + _DAT_8007623c pending-keyoff masks). */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined SetSpuKeyMask(int param_1, uint param_2);

/* 0x8005c9d0: Find first set voice in 24-bit mask and return its key/envelope status. Walks bits 0..23 of param_1; returns -1 if no bit set. Otherwise probes DAT_800730ec (live key-on mask) and the voice's current envelope level at voice+0xc (PTR_VOICE_00_LEFT_RIGHT_80073554 + idx*0x10 + 0xc). Return codes match PSY-Q SpuGetKeyStatus: 0=SPU_OFF (no key, env=0), 1=SPU_ON (key held, env>0), 2=SPU_ON_ENV_OFF (no key, env still>0 in release), 3=SPU_OFF_ENV_ON (key held, env=0 in attack). Sole caller: TickActiveSoundVoices (detects natural-end of voices and re-syncs internal state with hardware). */
int GetSpuVoiceKeyStatus(uint param_1);

/* 0x8005ca64: PSY-Q SpuRead equivalent — public clamping entry for the SPU→CPU DMA read path. Clamps param_2 (byte count) to 0x7eff0 (SPU RAM size minus the 0x1010 reserved region), forwards to ReadSpuRamDma at the current transfer-addr shadow, then if the in-flight-DMA flag at SPU-state+0x4e is 0 (sync transfer completed) zeros the per-voice transfer-running shadow at SPU-state+0x38. Returns the (possibly clamped) byte count actually transferred. Pair: SetSpuTransferStartAddr sets the source, ReadSpuRam reads. */
uint ReadSpuRam(uint param_1, uint param_2, uint param_3);

/* 0x8005cac4: PSY-Q SpuWrite-equivalent public entry. Clamps param_2 (byte count) to 0x7eff0 (≈ SPU RAM size minus 0x1010 reserved region) then forwards to WriteSpuRamInternal. After the transfer, if the in-flight-DMA flag at SPU-state-struct+0x4e is 0 (sync transfer just completed), clears the per-voice transfer-running shadow at SPU-state-struct+0x38. Returns the (possibly clamped) byte count actually transferred. Sole caller: FUN_80014b70 (band-1 sample loader). Pair: SetSpuTransferStartAddr sets the destination, WriteSpuRam writes data. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
uint WriteSpuRam(undefined2 *param_1, uint param_2, uint param_3);

/* 0x8005cb24: PSY-Q SpuSetTransferStartAddr-equivalent. Clamps param_1 (byte address in SPU sound RAM) to [0x1010, 0x80000), encodes to SPU-register units via WriteSpuVoiceAddrRegister(-1, addr) (no-write sentinel returns encoded short), stashes encoded short into the SPU-state shadow at +0x2c (set as the next transfer address), returns the byte address re-shifted back from the encoded form. Out-of-range input returns 0. The shadow short is later stamped into SPU transfer-address register +0x1a6 by WriteSpuRamPio / FUN_8005c450. Callers: FUN_80014564, FUN_80014b70, FUN_80015370 (band-1 sound-loaders), GemPickup_Update. */
int SetSpuTransferStartAddr(uint param_1);

/* 0x8005cb7c: PSY-Q SpuSetTransferMode equivalent. Stores transfer mode (0=DMA, 1=PIO, else=0/0 default) into DAT_800730f0 + writes shadow word at SPU-state+0x4e consumed by WriteSpuRamInternal dispatcher. Sole caller InitSoundEngine. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined SetSpuTransferMode(int param_1);

/* 0x8005cbb0: PSY-Q SpuIsTransferCompleted equivalent. Polls (param_1==0) or spin-blocks (param_1==1) on g_nSpuDmaEventHandle via TestEvent; short-circuits to "done" when PIO mode (g_nSpuTransferMode!=1) and g_nSpuTransferDoneFlag already set. Returns 1 on completion, raw TestEvent code otherwise. Callers: band-1 CD-streamed sample loaders + GemPickup_Update. */
long GetSpuTransferStatus(int param_1);

/* 0x8005cc58: PSY-Q SpuSetCommonAttr equivalent. From SpuCommonAttr*: bit0/bit2 = mvol L/L_mode, bit1/bit3 = mvol R/R_mode (ADSR-style mode codes 1..7 select preset slopes 0x8000..0xe000 ORed into top nibble, raw values clamped to [0,0x7f]); bit0x40/0x80 = rvol L/R; bit0x100 = CD enable (SPUCNT bit2); bit0x200 = ext enable (SPUCNT bit0); bit0x1000/0x2000 = CD-input vol L/R reverb-on enable (SPUCNT bits3/1); bit0x400/0x800 = CD vol L/R. mask=0 in attr[0] applies every field (full-init path used by InitSoundEngine). */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined SetSpuCommonAttr(uint *param_1);

/* 0x8005cfec: Per-voice SpuVoiceAttr writeback worker over voice range [param_2..param_3]. param_1 points to a SpuVoiceAttr struct (PSY-Q libspu layout): [0]=voice mask, [1]=field mask, +0x08 volL, +0x0a volR, +0x0c volmodeL, +0x0e volmodeR, +0x14 pitch, +0x16 sample_note, +0x18 note, +0x1c sample addr, +0x20 loop addr, +0x24 a_mode, +0x28 s_mode, +0x2c r_mode, +0x30 ar/dr/sr/rr/sl, +0x3a adsr1, +0x3c adsr2. For each selected voice (bit set in param_1[0], or param_2==param_3 forces "all"), writes the fields permitted by param_1[1] into the voice register shadow at PTR_VOICE_00_LEFT_RIGHT_80073554+voice*0x10. ADSR mode values 1..7 select preset slope codes (0x8000..0xe000) ORed into the volume/sweep register top nibble. Pitch uses raw param_1[5] when note==sample_note, else ComputeSpuPitchFromNoteDelta. Sample/loop addresses go through WriteSpuVoiceAddrRegister. When param_4==0, finishes with DelaySpuRegisterWrite flush. Returns 0 on success, 0xfffffffd if range is invalid. Sole caller: ApplySpuVoiceAttr wraps with (attr, 0, 0x17, 0). */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined4 ApplySpuVoiceAttrRange(uint *param_1, uint param_2, uint param_3, int param_4);

/* 0x8005d6b8: Computes the SPU pitch register value from a note delta — PSY-Q SpuFsetNToP equivalent. Each note is packed as (semitone, fraction-of-semitone) pairs; delta = (param_3*128 + param_4) - (param_1*128 + param_2) gives 128ths of a semitone. Divides by 0x600 (=12 semitones * 128 = one octave) into octave count + intra-octave remainder. Base pitch 0x1000 shifts left (positive delta = higher) or right (negative) by octave count. Within the octave, multiplies repeatedly by 0x103b (=4155 ≈ 4096 * 2^(1/12), the semitone ratio in 12.0 fixed point) for each 32-unit chunk of fractional remainder, then linearly interpolates the last 5 bits. Result clamped to 0x3fff. Sole caller: ApplySpuVoiceAttrRange (when SpuVoiceAttr.note differs from sample_note). */
uint ComputeSpuPitchFromNoteDelta(uint param_1, uint param_2, uint param_3, uint param_4);

/* 0x8005d9ec: PSY-Q SpuGetAllKeysStatus equivalent. Fills param_1[0..23] with each voice's key/envelope status code: 0 = SPU_OFF (key clear, env=0); 1 = SPU_ON (key set, env>0); 2 = SPU_ON_ENV_OFF (key clear, env still > 0 — release tail); 3 = SPU_OFF_ENV_ON (key set, env=0 — attack ramp). Per voice: reads envelope short at (SPU register base + 0x10*idx + 0xc) and probes live key-on mask DAT_800730ec bit idx. Sole caller: SyncSoundVoicesWithHardware (drift-reconciliation probe). */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined GetAllSpuVoiceKeyStatus(undefined1 *param_1);

/* 0x8005da74: Write L/R volume registers for one SPU voice and flush. Masks each volume short to 0x7fff (clears phase-invert / sweep-mode high bit, keeping straight volume mode) then stores into the voice register shadow PTR_VOICE_00_LEFT_RIGHT_80073554 + param_1*0x10 (+0 = VolL, +2 = VolR). Calls FUN_8005c720 to flush to hardware. Sole caller: TickActiveSoundVoices (per-frame per-voice volume update after pan/falloff compute). */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined SetSpuVoiceVolumeLR(int param_1, ushort param_2, ushort param_3);

void SpuGetVoiceVolume(int vNum, short *volL, short *volR);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined S_GVV_OBJ_38(ushort param_1, undefined4 param_2, ushort *param_3);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined S_GVV_OBJ_58(void);

void InitHeap(ulong *param_1, ulong param_2);

long LoadExec(char *param_1, ulong param_2, ulong param_3);

void _96_init(void);

/* 0x8005db4c: BIOS A0:0x72 stub (_96_remove) — issues `li t2,0xa0 / jr t2 / _li t1,0x72`. PSY-Q convention: deinit CD-ROM filesystem (releases BIOS file-system event handlers + interrupt hooks). Called by ResetCallback during libapi master init so the callback subsystem can re-arm everything from a known-clear state. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined Bios96Remove(void);

void DeliverEvent(ulong param_1, ulong param_2);

long OpenEvent(ulong param_1, long param_2, long param_3, func *func);

long TestEvent(long param_1);

long EnableEvent(long param_1);

/* 0x8005dba4: PSY-Q EnterCriticalSection wrapper. BIOS syscall(0) with $a0=1 — masks interrupts and enters the BIOS critical section. Pairs with ExitCriticalSection. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined EnterCriticalSection(void);

/* 0x8005dbb4: PSY-Q ExitCriticalSection wrapper. BIOS syscall(0) with $a0=2 — restores interrupts and exits the BIOS critical section. Pairs with EnterCriticalSection. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined ExitCriticalSection(void);

int VSync(int mode);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined WaitForFrameDeadline(int param_1, int param_2);

void ChangeClearPAD(long param_1);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined ChangeClearRCnt(void);

/* 0x8005ddc8: libapi system-vector slot[3] thunk — dispatches via *(PTR_PTR_800749ac+0xc)(). The default installed handler is ResetCallback (libapi master init); callers (CdInitDriver, InitSpuSubsystem, ResetGraph, FUN_80065364) invoke this to (re)arm the BIOS event/IRQ subsystem before configuring their own hardware. SDK internal — plate-only. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined FUN_8005ddc8(void);

/* 0x8005ddf8: libapi system-vector slot[2] thunk — dispatches via *(PTR_PTR_800749ac+0x8)(). Called after each callback-table init (InitRootCounterCallbacks + InitDmaCallbackTable + CdInitDriver + FUN_80065364) to commit the freshly-zeroed table to the master interrupt entry. SDK internal — plate-only. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined FUN_8005ddf8(void);

/* 0x8005de28: PSY-Q DmaCallback equivalent. Dispatches via the system DMA function-pointer table at *PTR_PTR_800749ac+0x4 — registers (a0=dma_channel, a1=callback) with the BIOS DMA-completion machinery; the table is the system vector for libapi/libetc. Callers (EnqueueGpuOp / FlushGpuQueue / InstallSpuDmaCallback / FUN_80064050) each install their per-channel completion handler this way. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined DmaCallback(void);

/* 0x8005de58: PSY-Q VSyncCallback equivalent — installs a VBlank-driven periodic callback. Dispatches via *(PTR_PTR_800749ac+0x14)(channel=4, fn). Sole caller InitPadSystem registers PollPadAndDistributeInput as the per-VBlank pad/rumble driver. Channel 4 is the libapi RCnt slot bound to VBlank. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined InstallVSyncCallback(undefined4 param_1);

/* 0x8005de8c: libapi system-vector slot[5] no-arg thunk — dispatches via *(PTR_PTR_800749ac+0x14)(). Callers (MemCardOpenSession, MemCardCloseSession) use this as a CD-bus arbitration release / VSync-callback clear when handing the system off between memcard and CD subsystems. SDK internal — plate-only. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined FUN_8005de8c(void);

/* 0x8005df1c: libapi accessor returning DAT_80073926 — non-zero while HandleHardwareInterrupt is processing an interrupt (re-entrancy guard). Callers (CdCommandSync, CdSync) use this to detect "called from inside an ISR" and switch to a polling path instead of blocking. SDK internal — plate-only. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined2 FUN_8005df1c(void);

/* 0x8005df44: Atomic I_MASK swap — saves old PTR_I_MASK_800749b4 value, writes new mask, returns old. PSY-Q SetIntMask equivalent. Used by libgpu watchdog/DMA-queue code (CheckGpuTimeout, EnqueueGpuOp, FlushGpuQueue, ResetGraphImpl) to mask interrupts during critical hardware-register touches. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined2 SetInterruptMask(undefined2 param_1);

/* 0x8005df60: PSY-Q ResetCallback equivalent — libapi master init for the interrupt/callback subsystem. Guarded by DAT_80073924 install-once flag. Sequence: mask all IRQs (I_MASK=0, I_STAT cleared) → DPCR=0x33333333 (enable all DMA channels at priority 3) → zero 0x41a-word callback registry → setjmp into DAT_8007395c (unexpected-interrupt longjmp landing -> HandleHardwareInterrupt) → HookEntryInt() to install the BIOS exception entry → set vtable slot[5]=InitRootCounterCallbacks return + slot[1]=InitDmaCallbackTable return → Bios96Remove (CD-FS deinit) → ExitCriticalSection. Returns &DAT_80073924 (install-flag ptr) on first call, 0 on re-entry. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined2 *ResetCallback(void);

/* 0x8005e03c: Master hardware-interrupt dispatcher hooked at the BIOS exception vector by ResetCallback/HookEntryInt. Reads (I_MASK & DAT_80073954 enable-mask & I_STAT) and for each of up to 11 IRQ sources: ACKs the bit in I_STAT and calls DAT_80073928[i] callback. Re-loops while any source remains pending. Sets DAT_80073926=1 around dispatch (re-entrancy guard read by IsInsideInterruptHandler equivalent). Watchdog: 0x800-cycle "intr timeout(%04x:%04x)" then force-clears I_STAT. Calls ReturnFromException at the end. Unexpected pre-init entry warns "unexpected interrupt(%04x)". */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined HandleHardwareInterrupt(void);

/* 0x8005e224: PSY-Q InterruptCallback equivalent — install or uninstall a per-IRQ callback. param_1 = IRQ number (0..10), param_2 = callback fn (NULL to uninstall). Updates the dispatch entry at &DAT_80073928[irq], toggles the corresponding bit in I_MASK + DAT_80073954 enable shadow. Special cases: irq==0 (VBlank) also calls ChangeClearPAD(uninstall ? 1 : 0) + ChangeClearRCnt(); irq==4/5/6 (RCnt0/1/2) also call ChangeClearRCnt(). Returns previous callback. Bracketed by I_MASK=0 critical section. */
int InstallInterruptCallback(uint param_1, int param_2);

/* 0x8005e4ac: Zero-words helper used by ResetCallback to clear the 0x41a-word libapi callback registry at &DAT_80073924 (install flag + dispatch table + enable shadow + setjmp buffer + auxiliary state). Identical loop to ClearRootCounterCallbackTable + ClearDmaCallbackTable — three separate compiler emissions across libapi OBJs. SDK internal — plate-only. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined FUN_8005e4ac(undefined4 *param_1, int param_2);

void ReturnFromException(void);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined ResetEntryInt(void);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined HookEntryInt(void);

/* 0x8005e508: Initializes the libapi RCnt (root-counter) callback table. Writes TMR_HRETRACE_MODE=0x107 (one-shot, IRQ enabled, sync on HRETRACE), clears DAT_800749e0, zeros 8-entry callback table at &DAT_800749c0, commits via DispatchSystemInitVector (FUN_8005ddf8). Returns &LAB_8005e5d8 (the RCnt master IRQ handler) — ResetCallback stores this into system-vector slot[5]. Called once during libapi master init. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined1 *InitRootCounterCallbacks(void);

/* 0x8005e604: 8-word memzero for the RCnt callback table at &DAT_800749c0. Identical body to its FUN_8005e4ac / FUN_8005e8ac siblings (compiler emitted one per source OBJ). SDK internal — plate-only. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined FUN_8005e604(undefined4 *param_1, int param_2);

/* 0x8005e630: Initializes the libapi DMA callback table at &DAT_800749ec. Zeros the 8-entry per-channel callback array, sets PTR_DMA_DICR_800749e8=0 (clear DMA IRQ-control), commits via DispatchSystemInitVector. Returns &LAB_8005e804 (the DMA master IRQ handler) — ResetCallback stores this into system-vector slot[1]. Called once during libapi master init; the per-channel handlers installed by DmaCallback live in this table. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined1 *InitDmaCallbackTable(void);

/* 0x8005e8ac: 8-word memzero for the DMA callback table at &DAT_800749ec. Identical body to its FUN_8005e4ac / FUN_8005e604 siblings (compiler emitted one per source OBJ). SDK internal — plate-only. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined FUN_8005e8ac(undefined4 *param_1, int param_2);

/* 0x8005ea94: PSY-Q SetDefDrawEnv (ex-SetDefaultDisplayEnv — old name had Draw/Disp SWAPPED). Fills a DRAWENV head: clip rect (x,y,w,h), ofs=(x,y), tw cleared, tpage=10, dtd=1, dfe = h within display (NTSC h<=0x100 / PAL h<=0x120 via GetGraphType), isbg=0, r0/g0/b0=0. Does not touch dr_env. Called by InitGraphicsHardware on the DB DRAWENVs at 80076ee0/80076f64. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined InitDefaultDrawEnv(undefined2 *param_1, undefined2 param_2, undefined2 param_3, undefined2 param_4, int param_5);

/* 0x8005eb4c: PSY-Q SetDefDispEnv (ex-SetDefaultDrawEnv — old name had Draw/Disp SWAPPED). Fills a 0x14-byte DISPENV: disp rect (x,y,w,h), screen rect zeroed, isinter/isrgb24/pad cleared. Called by InitGraphicsHardware on the DB DISPENVs at 80076f3c/80076fc0. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined InitDefaultDispEnv(undefined2 *param_1, undefined2 param_2, undefined2 param_3, undefined2 param_4, undefined2 param_5);

/* 0x8005eba0: libgpu accessor returning DAT_80074a10 — the cached "graph type" / video-mode flag (0 = NTSC, non-zero = PAL). Set by libgpu init based on console region. Used by SetDefaultDisplayEnv to choose the 256-vs-288-scanline interlace boundary, and by PutDispEnv to pick the GP1 display-mode word. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined4 GetGraphType(void);

void DumpClut(u_short clut);

void SetLineF3(LINE_F3 *p);

void SetLineF4(LINE_F4 *p);

void SetDrawMove(DR_MOVE *p, RECT *rect, int x, int y);

int ResetGraph(int mode);

/* 0x8005f53c: PSY-Q SetGraphDebug equivalent — sets the libgpu debug verbosity level (DAT_80074a66), returns previous level. When non-zero, prints "SetGraphDebug:level:%d,type:%d reverse:%d\n" via PTR_WritePrintf_80074a60. Higher levels enable extra "%s:bad RECT" / "GPU timeout:..." diagnostics in libgpu primitive packers and watchdog paths. Called by InitGraphicsHardware (typically with level=0 to disable verbose mode in shipped builds). */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined1 SetGraphDebug(char param_1);

void SetDispMask(int mask);

int DrawSync(int mode);

/* 0x8005f7d0: libgpu debug helper called by ClearImage/LoadImage/StoreImage/MoveImage. Validates that RECT param_2 fits within the framebuffer bounds (DAT_80074a68/6a). Prints '%s:bad RECT' via the debug printf hook (PTR_WritePrintf_80074a60) when verbose mode (DAT_80074a66) is set. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined ValidateGpuRect(undefined4 param_1, short *param_2);

/* 0x8005f8f8: libgpu ClearImage. Fills VRAM rect param_1 with packed 24-bit color (R=param_2, G=param_3, B=param_4) via the GPU command-dispatch table at PTR_PTR_80074a5c. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined ClearImage(short *param_1, uint param_2, uint param_3, uint param_4);

/* 0x8005fa28: libgpu LoadImage. Transfers CPU memory at param_2 into VRAM rect param_1 via the GPU command-dispatch table at PTR_PTR_80074a5c. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined LoadImage(short *param_1, undefined4 param_2);

/* 0x8005fa8c: libgpu StoreImage. Transfers VRAM rect param_1 out to CPU memory at param_2 via the GPU command-dispatch table at PTR_PTR_80074a5c. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined StoreImage(short *param_1, undefined4 param_2);

/* 0x8005faf0: libgpu MoveImage. VRAM-to-VRAM rect copy: source rect (RECT *)param_1, destination (x=param_2, y=param_3). Returns -1 if source rect has zero width or height. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined4 MoveImage(short *param_1, uint param_2, int param_3);

void DrawOTag(u_long *p);

DRAWENV *PutDrawEnv(DRAWENV *env);

DISPENV *PutDispEnv(DISPENV *env);

/* 0x80060670: libgpu SetDrawMode primitive. Initializes DR_MODE at param_1 (tag length = 2 words): code[0] = GP0(0xe1) draw-mode word built from dfe (force-bg, param_2), dtd (dither, param_3) and tpage bits (param_4); code[1] = clip-area word from optional RECT* param_5 (NULL = no clip). */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined SetDrawMode(int param_1, int param_2, int param_3, uint param_4, byte *param_5);

/* 0x800608e0: libgpu SetDrawEnv: packs a DRAWENV (RECT clip + offset + tpage + dtd + isbg + RGB clear color) into a primitive chain at param_1: GP0(0xe3) area-TL + GP0(0xe4) area-BR + GP0(0xe5) offset + GP0(0xe1) drawmode + GP0(0xe2) texwin + GP0(0xe6) + optional 0x02 background-fill rect. Length byte set to 7 (or 10 with bg fill). */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined SetDrawEnv(int param_1, ushort *param_2);

/* 0x80060b70: libgpu draw-mode word builder. Returns GP0(0xe1) command: texture page (low 11 bits), dither (bit 9/10 depending on PAL vs NTSC mode), draw-to-display flag. Sub-builder of SetDrawMode and SetDrawEnv. Mode-bit handling via DAT_80074a64 (1/2=NTSC packed mask, else PAL). */
uint BuildDrawModeWord(int param_1, int param_2, uint param_3);

/* 0x80060bc8: libgpu draw-area top-left builder. Returns GP0(0xe3) command with X/Y clamped to display res (DAT_80074a68/6a) and bit-packed per GPU mode (12+12 in NTSC mode 1/2 vs 10+10 in PAL/24-bit). Used by SetDrawEnv. */
uint BuildDrawAreaTopLeft(uint param_1, uint param_2);

/* 0x80060c94: libgpu draw-area bottom-right builder. Returns GP0(0xe4) command with X/Y clamped + bit-packed per GPU mode. Same shape as BuildDrawAreaTopLeft. Used by SetDrawEnv. */
uint BuildDrawAreaBottomRight(uint param_1, uint param_2);

/* 0x80060d60: libgpu draw-offset builder. Returns GP0(0xe5) command: signed 12+12-bit drawing offset (or 11+11 in PAL/24-bit mode via DAT_80074a64). Used by SetDrawEnv. */
uint BuildDrawOffsetCommand(uint param_1, uint param_2);

/* 0x80060da4: libgpu texture-window builder. Returns GP0(0xe2) command from a RECT* (mask X/Y from p[0]/p[2] >> 3; offset X/Y from -p[4]/-p[6] >> 3). NULL → 0. Used by SetDrawMode (as the optional clip-rect packer) and SetDrawEnv. */
uint BuildTextureWindow(byte *param_1);

/* 0x80060e28: libgpu display-Y coordinate adjuster (SDK internal). Reads a RECT* and returns the Y coordinate in display-line space, accounting for interlace flag DAT_80074a67 and PAL/NTSC mode DAT_80074a64. Used by display-env primitives that need to translate draw-Y → screen-Y. */
int FUN_80060e28(ushort *param_1);

/* 0x800616f4: libgpu GP1 (GPU status/control register) write. Writes the 32-bit command word to PTR_GPU_REG1_80074b38 and snapshots the low byte into DAT_800759bc[cmd] so callers can read back the last value per status sub-command (display enable, DMA direction, video mode, etc.). */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined WriteGpuControl(uint param_1);

/* 0x80061780: libgpu GPU DMA linked-list kickoff: writes GP1(0x04000002) to set DMA direction = GPU-read-linked-list, points DMA channel 2 (GPU) MADR at the OT/cmd-chain head, and writes CHCR=0x01000401 to start the transfer (linked-list mode, from RAM). Used as the queue-callback that DrawOTag/PutDrawEnv/MoveImage pass to EnqueueGpuOp; also stored as a function pointer at DAT_80074a34. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined SendGpuLinkedList(undefined4 param_1);

/* 0x800617cc: libgpu GetGPUInfo: issues GP1((param | 0x10000000)) "get GPU info" sub-command, then reads GP0 status register and returns it masked to 24 bits. param 0..7 selects which info field (0=draw-area-tl, 2=draw-area-br, 3=draw-offset, 4=texture-window, 5=draw-mode, 7=GPU version). Function pointer also stored at PTR_PTR_80074a4c. */
uint GetGpuInfo(uint param_1);

/* 0x80061820: libgpu DMA-queue enqueue. Takes (func, args, args_bytes, arg4) and either invokes func(args, arg4) immediately if the GPU + DMA + 64-entry ring at DAT_80078ea0 (stride 0x60, head DAT_80074b68, tail DAT_80074b6c) are all idle, OR appends the call to the ring for later replay by FlushGpuQueue when the GPU finishes the in-flight transfer. If args_bytes > 0 the inline args are copied into &DAT_80078eac+slot*0x60 and the slot's arg-ptr is rebased to point there; otherwise the caller's pointer is stored verbatim. Returns 0 on immediate execute, otherwise the new ring depth. Used by DrawOTag, ClearImage, LoadImage, etc. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
uint EnqueueGpuOp(undefined *param_1, undefined4 *param_2, int param_3, undefined4 param_4);

/* 0x80061b00: libgpu DMA ring drain. Called whenever the GPU might have completed an op. If DMA channel 2 (GPU CHCR bit 0x1000000) is idle and there are queued slots, spins polling GP1 ready-bit (0x4000000) and invokes each pending slot's func(args, arg4) in FIFO order until the ring drains or the GPU goes busy again. Final invariant: if the ring is empty AND DMA is idle AND a one-shot completion callback (DAT_80074a70) is registered, fires it once. Returns the remaining ring depth. */
uint FlushGpuQueue(void);

/* 0x80061dec: libgpu ResetGraph impl dispatched from the public ResetGraph thunk at 8005f2a4. mode bit pattern: 0/2 = full reset (DMA-2 CHCR=0x401, GP1(0)=display reset, zero GP1 shadow at DAT_800759bc[256] and the 64-entry DMA queue at DAT_80078ea0); 1/3/5 = cancel-only (GP1 soft reset 0x02000000 + 0x01000000). Brackets work in EnterCriticalSection/ExitCriticalSection (FUN_8005df44). mode==0 also runs FUN_80062230 (display-env reinit). */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined4 ResetGraphImpl(uint param_1);

/* 0x80061f48: libgpu DrawSync impl (dispatched via PTR_PTR_80074a5c). param=0 blocks until 64-entry DMA queue + DMA-2 + GP1 ready-bit all idle (with CheckGpuTimeout watchdog); param!=0 returns queue depth (0=idle, >=1=pending). */
uint DrawSyncImpl(int param_1);

/* 0x80062090: libgpu watchdog reset: sets the GPU-timeout VSync deadline to VSync(-1)+0xf0 (240 ticks ahead) and clears the spin counter at DAT_80074b80. Called by EnqueueGpuOp / FlushGpuQueue siblings whenever fresh work is queued, so CheckGpuTimeout's deadline rolls forward instead of firing on long-but-not-stuck pipelines. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined ResetGpuTimeoutDeadline(void);

/* 0x800620c4: libgpu GPU watchdog: called from EnqueueGpuOp when the 64-entry DMA ring is full. Returns 0 if there is still time on the current frame budget (DAT_80074b7c VSync deadline + DAT_80074b80 spin counter under 0xf0000), -1 if the GPU has wedged. On timeout WritePrintf("GPU timeout:que=%d,stat=%08x,chcr=%08x,madr=%08x,…") then hard-resets DMA channel 2 (GPU CHCR=0x401, DPCR bit 0x800), then GP1 reset (0x02000000, 0x01000000), and clears the ring head/tail. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined4 CheckGpuTimeout(void);

/* 0x80062230: libgpu display-env init (called by ResetGraphImpl mode==0). Applies initial GP1(0x10000007) get-info command, then if GPU mode is 24-bit picks a sub-mode; otherwise writes initial GP0(0xe1) draw-mode word (bit 0x1000 = display-disable check) and optionally GP1(0x20000504) for interlace setup. Returns a mode code 0..4 indicating display configuration. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined4 InitDisplayEnv(uint param_1);

/* 0x8006230c: libc memset: byte-fill (dest, value, count) with simple decrement loop. NULL/zero-count safe. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined memset(undefined1 *param_1, undefined1 param_2, int param_3);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined GPU_cw(void);

/* 0x80062350: libgte InitGeom: enables GTE (Cop0 SR bit 0x40000000), patches the BIOS exception vector for GTE coproc (via FUN_80062648), then loads default GTE constants — ZSF3=0x155, ZSF4=0x100, H=1000, DQA=-4194, DQB=0x1400000, OFX/OFY=0. Stores return address at DAT_80074d04. */
uint InitGeom(void);

MATRIX *MulMatrix0(MATRIX *m0, MATRIX *m1, MATRIX *m2);

MATRIX *MulMatrix(MATRIX *m0, MATRIX *m1);

void SetTransMatrix(MATRIX *m);

/* 0x80062618: libgte SetGeomOffset: load OFX/OFY GTE registers (param_1 &lt;&lt; 16, param_2 &lt;&lt; 16). Typically (screen_w/2, screen_h/2) so projected vertices are screen-centered. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined SetGeomOffset(int param_1, int param_2);

/* 0x80062638: libgte SetGeomScreen: load H (projection plane Z) GTE register. Smaller H = wider FOV. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined SetGeomScreen(undefined4 param_1);

/* 0x80062648: Sony SDK internal — libgte BIOS exception-vector patcher. Bracketed by FUN_8005dba4/bb4 (interrupt arbitration), calls BIOS B0 jumptable, copies a code stub from DAT_800626b0..FlushCache into the exception vector returned by *(int *)(iVar1+0x18), then FlushCache. Installs the GTE coprocessor-unusable handler so GTE instructions don't trap. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined FUN_80062648(void);

void FlushCache(void);

/* 0x800626f8: libc memcpy: byte copy of param_3 bytes from param_2 to param_1. Returns dest (or NULL if dest is NULL). */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined1 *memcpy(undefined1 *param_1, undefined1 *param_2, int param_3);

uint GetRandomU32(void);

/* 0x8006275c: libc srand: sets g_dwRandSeed. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined srand(uint param_1);

/* 0x8006276c: libc strlen clone. Returns 0 for NULL pointer; otherwise length of null-terminated string at param_1. */
int strlen(char *param_1);

int WritePrintf(char *fmt, ...);

int FormatAndWrite(int fd, char *fmt, void *args);

void *memchr(uchar *param_1, uchar param_2, int param_3);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined4 MEMCHR_OBJ_20(char *param_1, char param_2, int param_3);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined MEMCHR_OBJ_48(void);

/* 0x80062f10: PSn00bSDK-style _putchar: writes one byte to fd 1 (write syscall) and tracks column position at DAT_80075ac8. Special handling: tab (0x09) recurses on space until the column is 8-aligned; newline (0x0a) recurses on '\r' and resets column. Used by WriteString / WritePrintf for all debug output. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined WriteChar(byte param_1);

long write(long param_1, void *param_2, long param_3);

/* 0x80062fd4: libc sprintf clone (PSY-Q libapi). Writes formatted output to param_1 from format string param_2 with varargs starting at param_3. Supports %d/%i/%u/%s/%x/%X/%o/%p/%c/%n with flags '-' '+' ' ' '#' '0' and width.precision (use '*' for varargs). Calls strlen, memchr, and FUN_80063830 (memcpy-like byte copy). */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
int sprintf(int param_1, byte *param_2, uint param_3, undefined4 param_4);

/* 0x80063830: libc memmove: overlap-safe byte copy. Picks ascending vs descending direction by comparing src<dst. Called from sprintf's %s/%c padding paths. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined1 *memmove(undefined1 *param_1, undefined1 *param_2, int param_3);

/* 0x8006389c: PSn00bSDK-style _print: iterates a null-terminated string and calls WriteChar per byte. NULL input is replaced by DAT_80011c98 (likely "(null)"). No newline appended. Building block for WritePrintf's %s. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined WriteString(byte *param_1);

int setjmp(__jmp_buf_tag *__env);

void longjmp(__jmp_buf_tag *__env, int __val);

void exit(void);

/* 0x8006397c: libcd CdInit: up to 4 retries of CdReset(1) (full controller init via CdInitDriver + internal default volume setup). On success installs the three callback trampolines: CdSyncCallback(LAB_80063a14), CdReadyCallback(LAB_80063a3c), CdReadCallback(LAB_80063a64), and clears the read-break callback slot at DAT_80075178. Returns 1 on success, 0 + "CdInit: Init failed" after 5 attempts. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined4 CdInit(void);

/* 0x80063a8c: libcd CdStatus: returns the latest CD-ROM status byte (DAT_80074e44). Bit 0x10 = "data fetch in progress"; CdControlForce checks this to decide whether to abort the prior command. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined1 CdStatus(void);

/* 0x80063a9c: Sony SDK internal — libcd accessor returning DAT_80074e54 (1 byte, zeroed at CdInit). Likely CdLastCom or CdLastInt (canonical libcd state byte). */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined1 FUN_80063a9c(void);

/* 0x80063aac: Sony SDK internal — libcd accessor returning DAT_80074e55 (1 byte adjacent to DAT_80074e54, zeroed at CdInit). Likely the second of the CdLastCom/CdLastInt pair. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined1 FUN_80063aac(void);

/* 0x80063abc: libcd CdLastPos: returns a pointer to the last seek position (3-byte BCD CdlLOC stored at DAT_80074e50). Use CdPosToInt to convert to LBA. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined1 *CdLastPos(void);

/* 0x80063acc: libcd CdReset mode dispatcher. mode==2 → callback-only reset via FUN_80065364 (clears DAT_80074e34/e38/e44/e48). Otherwise calls CdInitDriver (the controller bring-up + response-queue drain); mode==1 additionally runs FUN_80065270 (initial CD volume/buffer setup). Returns 1 on success. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined4 CdReset(int param_1);

/* 0x80063b38: Sony SDK internal — pure thunk to CdResetController. Compiler-emitted relocation shim. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined FUN_80063b38(void);

/* 0x80063bd8: Sony SDK internal — pure thunk to public CdSync(mode, result). Compiler-emitted relocation shim; do not rename — the real implementation is CdSync (already labeled). */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined FUN_80063bd8(int param_1, undefined1 *param_2);

/* 0x80063c18: libcd CdSyncCallback: installs a callback at DAT_80074e34 invoked from CdCommandSync's response-queue drain when status bit 0x02 (CdlDiskReady / command complete) fires. Returns the previous callback for save/restore. CdControl/CdControlBlocking transparently park this to NULL during the CdlSetloc preamble and restore it for the actual command. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined4 CdSyncCallback(undefined4 param_1);

/* 0x80063c30: libcd CdReadyCallback: installs a callback at DAT_80074e38 invoked from CdCommandSync's response-queue drain when status bit 0x04 (CdlDataReady) fires. Returns the previous callback for save/restore. NULL disables. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined4 CdReadyCallback(undefined4 param_1);

/* 0x80063c48: libcd CdControl: 3-retry wrapper around CdCommandSync. Saves the ready callback (DAT_80074e34), optionally issues a CdlSetloc (sub-cmd 2) for commands that need a location (gated by DAT_80074dac[cmd] non-zero), then issues the real command. Restores the callback before returning. Returns 1 on success, 0 if all 3 retries fail. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined4 CdControl(byte param_1, undefined1 *param_2, undefined1 *param_3);

/* 0x80063d80: libcd CdControlF: like CdControl but aborts any in-progress command first by issuing CdlNop (sub-cmd 1) when DAT_80074e44 & 0x10 is set. Suppresses the sync callback during the abort + optional CdlSetloc preamble, restores it for the actual command issue, and uses CdCommandSync mode=1. Returns 1 on success, 0 after 3 retries. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined4 CdControlForce(byte param_1, undefined1 *param_2);

/* 0x80063eac: libcd CdControlB: same 3-retry CdlSetloc + command pattern as CdControl, but on success also CdSync(0, result) waits for completion (status 2 = CdlComplete). Returns true only if both the command issue and the post-command sync succeed. The blocking variant used when the caller wants the command finished before returning. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
bool CdControlBlocking(byte param_1, undefined1 *param_2, undefined1 *param_3);

/* 0x80063ff0: libcd CdMix: sets CD-DA volume mixing (CdlATV*). Wraps FUN_80065108 which writes 4 bytes (L→Lspu, L→Rspu, R→Lspu, R→Rspu) into CDIO mixer registers via index-2/index-3 register switching. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined4 CdMix(undefined1 *param_1);

/* 0x80064010: libcd CdGetSector: synchronous DMA read of one sector to (buf, nword). Wraps FUN_8006570c which programs DMA channel 3 with CHCR=0x11000000 (sync read), waits for status bit 0x40, then DICR ACK. Returns true (iVar1 == 0). */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
bool CdGetSector(undefined4 param_1, uint param_2);

/* 0x80064030: libcd CdGetSector2: chained-mode DMA read (handle for continuous read). Wraps FUN_8006580c which programs DMA channel 3 with CHCR=0x11400100 (linked-list / chain mode). Returns true on issue. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
bool CdGetSector2(undefined4 param_1, uint param_2);

/* 0x80064050: Sony SDK internal — pure thunk to FUN_8005de28 (libcd CD-bus arbitration). Compiler-emitted shim. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined FUN_80064050(void);

/* 0x80064074: Sony SDK internal — pure thunk to CdDataSync. Compiler-emitted relocation shim. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined FUN_80064074(int param_1);

/* 0x80064094: libcd CdIntToPos: converts a signed sector LBA to a 3-byte BCD CdlLOC (minute, second, frame). +0x96 = 150-sector lead-in offset; /0x4b = /75 frames per second; /0x3c = /60 seconds per minute. Each MSF component is BCD-encoded via the `(x + (x/10)*6)` trick so the result can be written directly to the libcd command FIFO. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined CdIntToPos(int param_1, char *param_2);

/* 0x80064198: libcd CdPosToInt: 3-byte BCD CdlLOC (M:S:F) -> signed LBA. Inverse of CdIntToPos. Decodes each BCD byte as (hi*10 + lo), recombines minutes*60*75 + seconds*75 + frames - 150 (lead-in offset). */
int CdPosToInt(byte *param_1);

/* 0x80064218: libcd CD-ROM interrupt handler / response-queue drain. Reads interrupt code from CD controller (*DAT_80075108 & 7), pulls up-to-8 response bytes via *DAT_80075100 into a local 8-byte buffer, ACKs via DAT_800750fc/108/104, then dispatches by interrupt code: 1=DataReady (sets DAT_80075115; copies response to DAT_80075ad8; returns 4), 2=Complete (DAT_80075114=2 or 5 on err; returns 2), 3=Acknowledge (returns 1 if callback armed at DAT_80074efc[chan], else 2), 4=DataEnd (returns 4), 5=DiskError (DAT_80075115/114=5; prints "DiskError: com=%s,code=(%02x:%02x)"; returns 6). Default: prints "CDROM: unknown intr(%d)". Polled by CdCommandSync. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined4 CdProcessInterrupt(void);

int CdSync(int mode, u_char *result);

/* 0x80064cec: libcd CdControl primitive: parks the optional ready-callback (DAT_80074e34) to NULL, optionally pre-issues sub-command 1 (CdlNop) if previous mode bit 0x10 is set, then writes the command + N parameters into the libcd command FIFO (count from DAT_8007507c[cmd]). If blocking flag (param_4) == 0 also drives a VSync-bounded wait for DAT_80075114 != 0 (the libcd "ready" status); on timeout calls FUN_80065190 and prints "CD timeout: %s (%s) Sync=%s, Ready=%s". Per-iteration polls FUN_80064218 to drain the response queue and dispatches the stored callbacks DAT_80074e34/e38 if present. Used by CdControl / CdControlBlocking and game-side wrappers. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
int CdCommandSync(byte param_1, undefined1 *param_2, undefined1 *param_3, int param_4);

/* 0x80065108: Sony SDK internal — libcd CD-DA volume register writer used by CdMix. Pokes 4 mixer values (L→L, L→R, R→L, R→R) via CDIO index-2/3 register switching at *DAT_800750fc/100/104/108. Always returns 0. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined4 FUN_80065108(undefined1 *param_1);

/* 0x80065190: libcd hard CD-ROM controller reset. Drains response queue via *DAT_800750fc/_DAT_80075108/_DAT_80075104 register pokes until status bits 0..2 clear, then re-arms DAT_80075114 = 2 (state=ready), clears DAT_80075115/116, writes _DAT_8007510c = 0x1325 (CDIO config). Called as the recovery path from CdCommandSync timeout and from CdSync/CdDataSync after persistent error. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined CdResetController(void);

/* 0x80065270: Sony SDK internal — libcd initial CD volume/buffer defaults applied during CdReset(mode=1). Writes 0x3fff into four 16-bit slots of *DAT_80075110 (mixer base) and a 0xc001 sentinel, then runs the same 4-byte CDIO mixer poke as CdMix(0x80, 0, 0x80, 0). */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined4 FUN_80065270(void);

/* 0x80065364: Sony SDK internal — libcd callback-only reset (dispatched from CdReset mode==2). Zeros the four callback slots DAT_80074e34/e38/e44/e48, then calls FUN_8005ddc8 + FUN_8005ddf8 (CD bus / interrupt arbitration teardown). */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined FUN_80065364(void);

/* 0x800653b4: libcd low-level CD-ROM controller initialization (the "CD_init: addr=%08x" worker dispatched by CdReset). Clears callbacks (DAT_80074e34/e38/e44/e48 + status bytes), drains the response queue at *DAT_80075108 until bits 0..2 are clear, resets state (DAT_80075114 = 2 / ready), sets CDIO config _DAT_8007510c = 0x1325, then issues CdCommandSync(1=CdlNop), (10=CdlGetTN), (12=CdlGetTD) and a final CdSync(0). Returns 0 on success, -1 on failure. */
int CdInitDriver(void);

int CdDataSync(int mode);

/* 0x8006570c: Sony SDK internal — libcd DMA-channel-3 sync sector read. Configures CDIO command 0x80, DPCR = 0x20943, CDIO config = 0x1323, latch=0x8000, programs MADR=param_1 BCR=(param_2|0x10000), waits for *DAT_800750fc bit 0x40, kicks DICR=0x11000000 then waits ACK bit 0x1000000. Wrapped by CdGetSector. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined4 FUN_8006570c(undefined4 param_1, uint param_2);

/* 0x8006580c: Sony SDK internal — libcd DMA-channel-3 chained-mode read. Same shape as FUN_8006570c but DPCR = 0x21020843 and DICR = 0x11400100 (linked-list / chain DMA). Wrapped by CdGetSector2. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined4 FUN_8006580c(undefined4 param_1, uint param_2);

/* 0x80065dbc: libcd internal: kicks the CD read state machine. Disables sync/ready callbacks, optionally aborts in-flight via CdControl(CdlPause)+CdControl(CdlSetloc), issues CdControl(CdlSetmode), installs LAB_800659f0 as sector-ready callback, then CdControlForce(CdlReadN). param_1 != 0 = retry path with 'CdRead: retry...' message. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined4 CdReadStart(int param_1);

/* 0x8006606c: libcd CdRead public API. Configures per-sector size (0x200/0x246/0x249 by mode bits 0x30), saves prior callbacks, pauses any in-flight command, then calls CdReadStart(0). Returns true on success. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
bool CdRead(undefined4 param_1, undefined4 param_2, uint param_3);

/* 0x8006623c: libcd CdReadCallback: installs the data-arrival callback for libcd reads at DAT_80075144. Distinct from CdSyncCallback/CdReadyCallback — this one is invoked per sector by the CdRead state machine (CdReadStart saves and restores it around the actual read). Returns previous; NULL disables. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined4 CdReadCallback(undefined4 param_1);

/* 0x80066254: Sony SDK internal — libcd read-break callback install (sets DAT_80075178). Returns previous. Called by CdInit with 0 to clear the slot. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined4 FUN_80066254(undefined4 param_1);

/* 0x8006626c: libmcrd one-shot reset + init. Zeros DAT_80075b4c/b90 (session retry counters) and calls MemCardBuInit (InitCARD2 + StartCARD2 + _bu_init). Called when the runtime decides the memcard driver needs a clean restart. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined MemCardReset(void);

/* 0x800662bc: libmcrd session open: resets per-op state (DAT_80075b50/54/58), arms 8 BIOS events via MemCardInit, releases CD bus via FUN_8005de8c. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined MemCardOpenSession(void);

/* 0x8006631c: libmcrd session close: spins until DAT_80075b50 == 0 (operation done), then releases CD bus + tears down event handles via FUN_800680a4. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined MemCardCloseSession(void);

/* 0x800665b8: libmcrd _card_load: opens a memcard channel for access (state=2, no data params). Registers LAB_80066634 as event handler. Fails with 'Access Denied. : event multipul open' if a prior op is still in flight. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
bool MemCardLoad(undefined4 param_1);

/* 0x80066e28: libmcrd _card_read: state=3. Requires size aligned to 0x80 (one BIOS sector). Stores file ID+filename+src+dst+size into DAT_80075b5c..6c and registers LAB_80066f34 as event handler. Caller polls via MemCardSync. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined4 MemCardRead(int param_1, char *param_2, undefined4 param_3, undefined4 param_4, uint param_5);

/* 0x800670e4: libmcrd _card_write: state=4. Same param layout as MemCardRead. Registers LAB_800671f0 as event handler. Caller polls via MemCardSync. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined4 MemCardWrite(int param_1, char *param_2, undefined4 param_3, undefined4 param_4, uint param_5);

/* 0x80067628: libmcrd sync/poll: out-params (param_2,param_3) = (state, result). Returns 0 while busy (blocks if param_1==0); returns 1 when done and consumes the completion at DAT_80075b58. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined4 MemCardSync(int param_1, int *param_2, undefined4 *param_3);

/* 0x80067c7c: libmcrd channel-to-event-index map. Maps SIO0 access codes to libmcrd event-table indices: 0→0, 1→2, 2→1, 4→3. Any other input returns (code | 0x8000) — high bit signals "invalid channel" to the caller. Called by MemCardOpenSession + the three public memcard APIs (Load/Read/Write) when looking up which BIOS event handle to wait on. */
uint MapMemCardAccessCode(uint param_1);

/* 0x80067d74: libmcrd path-prefix formatter. Writes the 6-byte template "bu00:" (loaded from DAT_800121c0..c5) into param_2's buffer, then overwrites bytes [2..3] with param_1 formatted as 2 decimal digits — producing "bu00:", "bu01:" … "bu15:" memcard file paths. Called by MemCardRead and MemCardWrite to construct the filename argument to _card_read/_card_write. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined FormatMemCardPath(int param_1, uint param_2);

/* 0x80067dd0: Sony SDK internal — libmcrd BIOS event handler stub. Sets per-slot status flag at DAT_80075b2c = 1. Registered via RegisterMemCardEvent for one of the 8 BIOS event handles (DAT_80075b0c..b28). PollMemCardEvents / WaitMemCardEventSlot1 read these flags. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined4 FUN_80067dd0(void);

/* 0x80067de4: Sony SDK internal — libmcrd BIOS event handler stub. Sets per-slot status flag at DAT_80075b30 = 1. Sibling of FUN_80067dd0. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined4 FUN_80067de4(void);

/* 0x80067df8: Sony SDK internal — libmcrd BIOS event handler stub. Sets per-slot status flag at DAT_80075b34 = 1. Sibling of FUN_80067dd0. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined4 FUN_80067df8(void);

/* 0x80067e0c: Sony SDK internal — libmcrd BIOS event handler stub. Sets per-slot status flag at DAT_80075b38 = 1. Sibling of FUN_80067dd0. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined4 FUN_80067e0c(void);

/* 0x80067e20: Sony SDK internal — libmcrd BIOS event handler stub. Sets per-slot status flag at DAT_80075b3c = 1. Sibling of FUN_80067dd0. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined4 FUN_80067e20(void);

/* 0x80067e34: Sony SDK internal — libmcrd BIOS event handler stub. Sets per-slot status flag at DAT_80075b40 = 1. Sibling of FUN_80067dd0. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined4 FUN_80067e34(void);

/* 0x80067e48: Sony SDK internal — libmcrd BIOS event handler stub. Sets per-slot status flag at DAT_80075b44 = 1. Sibling of FUN_80067dd0. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined4 FUN_80067e48(void);

/* 0x80067e5c: Sony SDK internal — libmcrd BIOS event handler stub. Sets per-slot status flag at DAT_80075b48 = 1. Sibling of FUN_80067dd0. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined4 FUN_80067e5c(void);

/* 0x80067e70: libmcrd one-shot card-driver init: MemCardInitCard (InitCARD2) + MemCardStartCard (StartCARD2) + _bu_init (BIOS-managed memcard filesystem). Returns 0. Higher-level than MemCardInit (which only arms BIOS event handles). */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined4 MemCardBuInit(void);

/* 0x80067ea0: libmcrd event setup: OpenEvent x8 (per-slot ReadEnd/WriteEnd/Done/Error for slots 0xf4000001 + 0xf0000011) + EnableEvent x8 + PollMemCardEvents to drain stale events. Bracketed by FUN_8005dba4/bb4 (CD-bus arbitration). */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined MemCardInit(void);

/* 0x800680a4: libmcrd session teardown: CloseEvent on the 8 BIOS event handles at DAT_80075b0c..b28 (per-slot ReadEnd/WriteEnd/Done/Error). Bracketed in FUN_8005dba4/bb4 (CD-bus arbitration). Counterpart to MemCardInit. Called by MemCardCloseSession. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined MemCardTeardownEvents(void);

/* 0x8006815c: libmcrd event drain: calls TestEvent on the 8 BIOS event handles tracked at DAT_80075b0c..DAT_80075b28 (per-memcard slot ReadEnd/WriteEnd/Done/Error events) to consume pending fires, then zeroes the 8 result-status words at DAT_80075b2c..DAT_80075b48. Called by libmcrd Read/Write/Format/Sync entry points to clear stale events before issuing a new request. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined PollMemCardEvents(void);

/* 0x80068264: libmcrd slot-1 event wait. Busy-spins on the combined slot-1 status mask (DAT_80075b2c + DAT_80075b30*2 + DAT_80075b34*4 + DAT_80075b38*8) until any bit is set, then drains the four slot-1 BIOS event handles (DAT_80075b1c/20/24/28) via TestEvent and clears the status words. Returns the combined value >>1 (drops the "request issued" bit so callers see only completion/error bits). */
int WaitMemCardEventSlot1(void);

/* 0x80068340: libmcrd slot-2 event wait. Busy-spins on combined slot-2 status (DAT_80075b3c + DAT_80075b40*2 + DAT_80075b44*4 + DAT_80075b48*8) until any bit set, then TestEvent on slot-2 BIOS handles (DAT_80075b0c/10/14/18), clears status. Returns combined&gt;&gt;1 (drops the "request issued" bit). Counterpart to WaitMemCardEventSlot1. */
int WaitMemCardEventSlot2(void);

/* 0x8006841c: libmcrd non-blocking slot-1 status read. Returns the combined slot-1 event-status mask (DAT_80075b2c + DAT_80075b30*2 + DAT_80075b34*4 + DAT_80075b38*8) without clearing or TestEvent. Polled by each public memcard API (MemCardInit/Load/Read/Write) before deciding whether to enter the WaitMemCardEventSlot1 spin. */
int GetMemCardEventStatusSlot1(void);

/* 0x80068458: libmcrd non-blocking slot-2 status read. Returns combined slot-2 event-status mask without clearing or TestEvent. Polled by each public memcard API before deciding whether to enter WaitMemCardEventSlot2 spin. Counterpart to GetMemCardEventStatusSlot1. */
int GetMemCardEventStatusSlot2(void);

void _bu_init(void);

long CloseEvent(long param_1);

long open(char *param_1, ulong param_2);

long lseek(long param_1, long param_2, long param_3);

long read(long param_1, void *param_2, long param_3);

long close(long param_1);

long format(char *param_1);

DIRENTRY *nextfile(DIRENTRY *param_1);

long erase(char *param_1);

DIRENTRY *firstfile(char *param_1, DIRENTRY *param_2);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined FIRST_OBJ_D0(void);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined FIRST_OBJ_174(void);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined FIRST_OBJ_180(void);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined FIRST_OBJ_19C(int *param_1, undefined4 param_2, undefined4 param_3);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined FIRST_OBJ_25C(void);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined firstfile2(void);

/* 0x800687d4: libc strcat: appends src (param_2) to dst (param_1). NULL-safe — returns NULL if either is NULL or if buffers overlap end-to-end. Walks dst to find its terminator, then byte-copies src including its null. */
char *strcat(char *param_1, char *param_2);

/* 0x8006887c: libc strcmp: null-terminated byte compare. Returns (unsigned char) diff at first mismatch, 0 on equal, with NULL handling: NULL == NULL = 0, NULL < non-NULL. */
int strcmp(byte *param_1, byte *param_2);

long _card_info(long chan);

long _card_load(long chan);

long _card_write(long chan, long block, uchar *buf);

void _new_card(void);

/* 0x80068920: libmcrd block-format helper. Calls _new_card() (BIOS card format/zero), then _card_write(in_a0, 0x3f, NULL) to write 63 (= max blocks) sectors of nulls. in_a0 is the channel handle. Used during memcard initialization / file delete. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined MemCardFormatBlocks(void);

/* 0x80068958: libmcrd InitCARD2 wrapper: ChangeClearPAD(0) + InitCARD2(initial pad state) bracketed by FUN_8005dba4/bb4 (CD-bus arbitration). Called by MemCardBuInit and MemCardInit. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined MemCardInitCard(void);

/* 0x800689b0: libmcrd StartCARD2 wrapper: StartCARD2() bracketed by FUN_8005dba4/bb4 (CD-bus arbitration), then ChangeClearPAD(0). Called by MemCardBuInit. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined MemCardStartCard(void);

void SetInitPadFlag(int flag);

/* 0x80068a1c: Possible GS_125.OBJ/GsGetWorkBase */
int ReadInitPadFlag(void);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined PAD_init(void);

long InitPAD(char *param_1, long param_2, char *param_3, long param_4);

long StartPAD(void);

void StopPAD(void);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined4 PAD_OBJ_1A4(void);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined4 PAD_OBJ_21C(void);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined4 PAD_OBJ_254(void);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined4 PAD_OBJ_2BC(void);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined InitPAD2(void);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined StartPAD2(void);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined StopPAD2(void);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined PAD_init2(void);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined SysEnqIntRP(void);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined SysDeqIntRP(void);

void EnablePAD(void);

void DisablePAD(void);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined _patch_pad(void);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined _remove_ChgclrPAD(void);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined InitCARD2(void);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined StartCARD2(void);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined StopCARD2(void);

/* 0x80068f30: libmcrd event-handler stack reset: sets _DAT_800751b0 = 0xffffffff (= -1, "empty stack"). Called by MemCardOpenSession before any RegisterMemCardEvent calls. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined ResetMemCardEventStack(void);

/* 0x80068f44: libmcrd event-tracking registration: pushes the BIOS event handle in param_1 onto the 4-slot tracking ring at DAT_80075c48 and zero-fills the matching 0x10-byte state record at DAT_80075c14+i*0x10. Index counter at DAT_800751b0; overflow (>=4 slots) prints "libmcrd: event overflow" via WritePrintf instead of trampling the ring. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined RegisterMemCardEvent(undefined4 param_1);

/* 0x80068fc4: libmcrd event-handler stack dispatcher. If stack is non-empty (_DAT_800751b0 &gt;= 0), calls the top handler at DAT_80075c48[top] with context address &amp;DAT_80075c08 + top*0x10 (the per-slot 16-byte buffer registered by RegisterMemCardEvent). If the handler returns non-zero, pops the stack. Called from MemCardSync polling loops to drive request completion. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined PopMemCardEventHandler(void);

/* 0x80069030: libmcrd "is event-handler stack empty" predicate. Returns 1 iff _DAT_800751b0 is negative (= -1, init value from ResetMemCardEventStack). */
uint IsMemCardEventStackEmpty(void);

/* 0x80069040: Possible _OP_VDEL.OBJ/__builtin_vec_delete Possible _OP_VNEW.OBJ/__builtin_vec_new Possible GS_106.OBJ/GsSetProjection Possible SSNOFF.OBJ/SsSetNoiseOff Possible SSQUIT.OBJ/SsQuit */
int PadChkVsync(void);

/* 0x80069060: Possible _OP_VDEL.OBJ/__builtin_vec_delete Possible _OP_VNEW.OBJ/__builtin_vec_new Possible GS_106.OBJ/GsSetProjection Possible SSNOFF.OBJ/SsSetNoiseOff Possible SSQUIT.OBJ/SsQuit */
void PadStartCom(void);

/* 0x80069080: Possible _OP_VDEL.OBJ/__builtin_vec_delete Possible _OP_VNEW.OBJ/__builtin_vec_new Possible GS_106.OBJ/GsSetProjection Possible SSNOFF.OBJ/SsSetNoiseOff Possible SSQUIT.OBJ/SsQuit */
void PadStopCom(void);

uint PadChkMtap(int param_1);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined PADENTRY_OBJ_A4(void);

int PadGetState(int param_1);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined1 PADENTRY_OBJ_158(int param_1);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined PADENTRY_OBJ_15C(void);

int PadInfoMode(int param_1, int param_2, int param_3);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined PADENTRY_OBJ_250(void);

int PadInfoAct(int param_1, int param_2, int param_3);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined PADENTRY_OBJ_2E4(void);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined PADENTRY_OBJ_2F0(void);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined PADENTRY_OBJ_2FC(void);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined PADENTRY_OBJ_308(void);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined PADENTRY_OBJ_314(void);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined PADENTRY_OBJ_324(void);

int PadInfoComb(int param_1, int param_2, int param_3);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined PADENTRY_OBJ_3CC(void);

int PadSetActAlign(int param_1, uchar *param_2);

int PadSetMainMode(int socket, int offs, int lock);

void PadSetAct(int param_1, uchar *param_2, int param_3);

/* 0x80069608: Sony SDK internal — libmcrd SIO0 step-table init. Installs the two-step driver functions DAT_80075c5c = &LAB_8006969c (step 1) and DAT_80075c60 = &LAB_80069634 (step 0), and zeroes DAT_80075c58 / DAT_80075c64 (step counter + sentinel). Counterpart of the SIO0 step-dispatcher at 0x80069b40. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined FUN_80069608(void);

/* 0x80069808: Sony SDK internal — libmcrd accessor that returns &amp; clears _DAT_80075224 (last memcard error/result code). Polled by error-recovery paths. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined4 FUN_80069808(void);

/* 0x8006981c: Sony SDK internal — libmcrd SIO0/RCnt interrupt-handler installer. Bracketed by FUN_8005dba4/bb4 (CD-bus arbitration); SysDeqIntRP+SysEnqIntRP rotates the RCnt entry, ChangeClearRCnt, then calls *_DAT_800751c0 twice to register the two SIO0 driver callbacks at offsets 0 and 0xf0 from DAT_800751f0. Sets DAT_800751f4 = 1 (driver active). */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined FUN_8006981c(void);

/* 0x800698e8: Sony SDK internal — libmcrd SIO0/RCnt interrupt-handler uninstall counterpart to FUN_8006981c. ChangeClearRCnt + SysDeqIntRP under CD-bus arbitration. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined FUN_800698e8(void);

/* 0x8006992c: Sony SDK internal — libmcrd SIO0 init/issue. Resets SIO0 CTRL/MODE bytes, sets SIO0 BAUD (0x1003 / 0x3003 by slot), then runs the per-slot callback function ptr at DAT_800751dc over the per-device queue indexed by DAT_800751fc, decrementing the queue counter at &DAT_80075214[slot]. On final iteration also calls completion fn at DAT_800751e0. Not investigated — pure libmcrd internal. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined8 FUN_8006992c(int param_1);

/* 0x80069b40: Sony SDK internal — libmcrd SIO0 step-machine dispatcher. Calls the function pointer at DAT_80075240[DAT_80075200 * 4], advances step counter, on negative return calls error handler at DAT_800751bc, otherwise calls FUN_8006a014 finalize (with SetSioTimeoutDeadline(0x3c)). Step counter capped at 5. Not investigated — pure libmcrd internal. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined FUN_80069b40(void);

/* 0x80069c08: Sony SDK internal — libmcrd SIO0 byte exchange variant. param_2<0 = END-of-frame path (writes ~param_2 after waiting for ACK rise, then for timeout via IsSioTimeoutExpired); otherwise same as FUN_80069de0 but without the cmd-0x88 acknowledge gating. Returns received byte or 0xffffffec on timeout. Not investigated — pure libmcrd internal. */
uint FUN_80069c08(int param_1, int param_2);

/* 0x80069de0: Sony SDK internal — libmcrd SIO0 byte exchange. Waits for SIO0 STAT TX-ready bit, optionally pauses for cmd-0x88 acknowledge, writes byte, waits for STAT RX-ready (with SetSioTimeoutDeadline/IsSioTimeoutExpired guard), stores received byte into request struct at param_1+0x44/45/3c. Returns received byte or 0xfffffffe on timeout. Not investigated — pure libmcrd internal. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
uint FUN_80069de0(int param_1, undefined1 param_2);

/* 0x8006a014: Sony SDK internal — libmcrd SIO0 abort/finalize. Writes 0xffffff7f to SIO0 CTRL (clears ACK), spins on STAT RX-ready bit until clear with timeout via IsSioTimeoutExpired, then sets bit 0x10 in SIO0 CTRL. Returns 1 on clean abort, 0 on timeout. Not investigated — pure libmcrd internal. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined4 FUN_8006a014(void);

/* 0x8006a0a4: Sony SDK internal — libmcrd SIO0 TX-ready spin. Busy-waits for SIO0 STAT bit 2 (TX buffer empty) to assert. Not investigated — pure libmcrd internal. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined FUN_8006a0a4(void);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined _padSetAct(int param_1, undefined4 param_2, undefined1 param_3);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined _padSetCmd(int param_1, undefined1 param_2, undefined4 param_3, undefined1 param_4);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined _padSendAtLoadInfo(int param_1);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined PADCMD_OBJ_90(void);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined4 _padRecvAtLoadInfo(int param_1);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined4 PADCMD_OBJ_18C(void);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined4 PADCMD_OBJ_1D8(void);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined PADCMD_OBJ_1DC(void);

int _padGetActSize(int param_1);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined4 _padLoadActInfo(int *param_1, int param_2);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined PADCMD_OBJ_2E4(void);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined PADCMD_OBJ_2F8(int param_1);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined PADCMD_OBJ_390(void);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined4 PADCMD_OBJ_3A0(int *param_1);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined PADCMD_OBJ_538(int param_1, int param_2, undefined1 *param_3, int param_4);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined4 PADCMD_OBJ_560(int param_1, int param_2, undefined1 *param_3);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined4 PADCMD_OBJ_5C4(void);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined4 PADCMD_OBJ_608(void);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined PADCMD_OBJ_60C(void);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
bool _padSetActAlign(int param_1, undefined4 param_2);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined PADCMD_OBJ_67C(int param_1);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined4 PADCMD_OBJ_698(int param_1);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined4 PADCMD_OBJ_72C(int param_1, byte *param_2, int param_3, int param_4);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
bool _padSetMainMode(int param_1, char param_2, undefined1 param_3);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined PADCMD_OBJ_7F8(int param_1);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined PADCMD_OBJ_844(void);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined4 PADCMD_OBJ_84C(int param_1);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined4 PADCMD_OBJ_898(void);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined PADCMD_OBJ_89C(void);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined _padCmdParaMode(int param_1, undefined1 param_2);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined PADCMD_OBJ_8CC(int param_1);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined PADCMD_OBJ_8E0(int param_1, undefined1 param_2);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined PADCMD_OBJ_900(int param_1, undefined1 param_2);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined PADCMD_OBJ_920(int param_1, undefined1 param_2);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined PADCMD_OBJ_940(int param_1);

void PadInitDirect(uchar *param_1, uchar *param_2);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined PADPORTD_OBJ_160(undefined4 *param_1);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined PADPORTD_OBJ_1C8(int param_1);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined PADPORTD_OBJ_23C(void);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined PADPORTD_OBJ_298(undefined4 param_1, int param_2);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined PADPORTD_OBJ_2BC(int param_1);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined1 PADPORTD_OBJ_2CC(int param_1);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined PADPORTD_OBJ_330(void);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined1 PADPORTD_OBJ_35C(int param_1);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined PADPORTD_OBJ_380(void);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined PADPORTD_OBJ_388(int param_1);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined PADPORTD_OBJ_45C(byte *param_1, byte *param_2, int param_3);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined PADPORTD_OBJ_4A8(undefined4 param_1, undefined4 param_2, int param_3);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined PADPORTD_OBJ_5F4(void);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
int PADPORTD_OBJ_604(undefined *param_1);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined *PADPORTD_OBJ_63C(uint param_1);

void *bzero(uchar *param_1, int param_2);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined BZERO_OBJ_28(void);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined _padInitDirSeq(void);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined4 PADSEQD_OBJ_34(int param_1);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined4 PADSEQD_OBJ_D0(int param_1);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined4 PADSEQD_OBJ_F8(void);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined PADSEQD_OBJ_108(int param_1);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined PADSEQD_OBJ_1A0(void);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined PADSEQD_OBJ_30C(void);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined PADSEQD_OBJ_320(void);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined PADSEQD_OBJ_348(void);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined PADSEQD_OBJ_358(void);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined _dirFailAuto(int param_1);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined PADSEQD_OBJ_438(void);

/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined4 PADSEQD_OBJ_448(int param_1);

/* 0x8006bb20: libcomb/libcard timeout-deadline setup. Stores param_1 (tick budget) at DAT_80078e9c and the current free-running TMR_SYSCLOCK_VAL at DAT_80076b78 — IsSioTimeoutExpired (8006bb40) reads the same pair to test if (now - start) (optionally >>3 when TMR_SYSCLOCK_MODE bit 0x200 is clear) has reached the budget. Used by the SIO0 controller/memcard byte exchange (FUN_80069de0) to bound the response-ready spin. */
/* FIXME: Ghidra-inferred prototype (contains undefined) */
undefined SetSioTimeoutDeadline(undefined4 param_1);

/* 0x8006bb40: libcomb/libcard timeout-deadline predicate. Reads current TMR_SYSCLOCK_VAL, accounts for the 16-bit timer rollover using TMR_SYSCLOCK_MAX, and returns true when (now - DAT_80076b78) (or >>3 when TMR_SYSCLOCK_MODE bit 0x200 is clear) >= DAT_80078e9c — the budget written by SetSioTimeoutDeadline. */
bool IsSioTimeoutExpired(void);

#endif /* OPEN_SPYRO_FUNCS_H */
